# 운행 예정 목록 조회 모달

기사용 대시보드에서 **`운행예정목록 조회`**로 연 **「운행 예정 목록」** 모달(`UpcomingTripsModal`)의 UI·데이터·API 명세이다. 테이블·컬럼 정본은 **[`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md)** (`TB_AUCTION_REQ`, `TB_BUS_RESERVATION`)를 따른다.

---

## 1. 관련 소스 파일

| 구분 | 경로 · 설명 |
|------|-------------|
| **프론트 모달** | `busTaams_web/src/components/UpcomingTrips/UpcomingTripsModal.jsx` |
| **진입·연동** | `busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx` — `QuickMenu`의 「운행예정목록 조회」, 모달 상태·`driverId`·`onTravelerQuoteDetail` 전달 |
| **상위 라우팅** | `busTaams_web/src/App.jsx` — `openTravelerQuoteDetail`로 `{ reqId, reqBusSeq }` 수신 후 `TravelerQuoteRequestDetails` 오픈 |
| **REST 구현** | `busTaams_server/server.js` — `app.get('/api/upcoming-trips', …)` |

환경 변수: 프론트는 `import.meta.env.VITE_API_BASE_URL`을 사용하고, 값이 비어 있으면 `http://127.0.0.1:8080`. **후행 슬래시(`/`)는 제거** 후 API 경로와 결합한다.

---

## 2. 진입·닫기

| 항목 | 내용 |
|------|------|
| **진입** | `DriverDashboard` → 바로가기 메뉴 **「운행예정목록 조회」** (`key: 'upcomingTrips'` → `setShowUpcomingTripsModal(true)`) |
| **식별자** | `driverId = currentUser?.custId \|\| currentUser?.userId` (문자열로 trim되어 쿼리에 사용) |
| **닫기** | 우상단 `close` 버튼, 반투명 오버레이 클릭, **`Esc`** 키 |
| **접근성** | `role="dialog"`, `aria-modal="true"`, 제목 `aria-labelledby="upcoming-trips-title"` |
| **스크롤** | 모달이 열리면 `document.body.style.overflow = 'hidden'`, 언마운트 시 복구 |

모달은 **앱 전역 좌·상단 네비게이션을 포함하지 않는** 독립 오버레이이다.

---

## 3. 컴포넌트 입·출력 (Props)

### 3-1. 입력 (Props)

| Prop | 타입(의도) | 설명 |
|------|------------|------|
| `open` | `boolean` | `true`일 때만 DOM에 마운트·표시 |
| `onClose` | `() => void` | 닫기 시 호출 |
| `driverId` | `string \| number` 등 | 기사 ID. `String(driverId).trim()` 후 비어 있으면 **`/api/upcoming-trips` 호출 안 함** |
| `onTravelerQuoteDetail` | `(payload) => void` (옵션) | 상세 화면으로 넘길 때 호출. 권장 payload: **`{ reqId: string, reqBusSeq: number }`** |

### 3-2. 출력 (부모 콜백)

| 이벤트 | 페이로드 | 비고 |
|--------|----------|------|
| `onClose` | 없음 | 오버레이·닫기·Esc |
| `onTravelerQuoteDetail` | `{ reqId, reqBusSeq }` | `DriverDashboard`에서 모달을 먼저 닫은 뒤 `App.jsx`의 `openTravelerQuoteDetail`로 전달. `reqBusSeq`가 없거나 유효하지 않으면 프론트에서 **1**로 대체 |

---

## 4. 데이터 로딩 동작

| 조건 | 동작 |
|------|------|
| `open && sessionDriverId` | `GET ${API_BASE}/api/upcoming-trips?driverId=...` 1회 호출 |
| `driverId` 공백 | fetch 생략, 목록은 이전 상태 유지(또는 초기 빈 배열) |
| HTTP 오류 | `loadError`에 메시지(`j.error` 우선, 없으면 `서버 오류 (status)`), `items` 비움 |
| 네트워크 예외 | `loadError = e.message \|\| '네트워크 오류'` |
| 성공 시 | `items = d.items` (배열 아니면 `[]`), `emptyMessage`는 응답 문자열 또는 기본 문구 |

**날짜/시간 표시:** 서버에서 내려오는 `startDt`, `endDt`는 브라우저 `Date`로 파싱되며, **UTC/로컬 해석은 DB·드라이버 반환 형식에 따름**.

---

## 5. 화면 구성 (프론트 표시 항목)

레이아웃: **큰 화면(`lg`)에서 상단 한 줄**에 좌측 타이틀·부제, 우측 「주간 요약」 카드(`flex-row` + `justify-between`). 목록 영역은 그 아래 단일 플로우.

### 5-1. 헤더 (좌측)

| UI 요소 | 내용 · 형식 |
|---------|-------------|
| **제목** | 「운행 예정 목록」 (`h3`, `text-5xl`) |
| **부제 문구** | 상태에 따라 3종 (아래 표) |

| 상태 | 표시 문자열 |
|------|-------------|
| `loading` | `일정을 불러오는 중입니다…` |
| `loadError` | 오류 문자열 그대로 (서버 메시지·HTTP 안내 등) |
| 그 외 | ``이번 주 ${total}건의 운행이 확정되었습니다. 안전 운행을 위해 세부 사항을 확인해주세요.`` (`total = items.length`) |

참고: 백엔드 필터는 **「이번 주」 한정이 아니라 출발일 `>=` 오늘**이다. 부제의 「이번 주」는 현재 카피만의 표현이다.

### 5-2. 주간 요약 카드 (우측)

| UI 항목 | 데이터 출처 | 형식/비고 |
|---------|-------------|-----------|
| 섹션 라벨 | 고정 | 「주간 요약」 (`h6`) |
| **예상 주간 수익** | **API `items[]`의 `contractAmount` 합** | `₩` + `toLocaleString('ko-KR')` |
| 총 운행 거리 | **고정 더미** | `1,240 km` (DB/API 없음) |
| 진행 바·목표 달성률 | **고정 더미** | 너비 75%, 라벨 `75%` (DB/API 없음) |

### 5-3. 빈 목록

| 조건 | 표시 |
|------|------|
| `!loading && items.length === 0` | 둘러싼 박스 안에 `emptyMessage` 중앙 정렬 |

기본 문구: `등록된 운행 예정 일정이 없습니다.` 서버가 `emptyMessage`를 주면 그 값을 사용.

### 5-4. 메인 카드 (`items[0]` — 첫 행만)

히어로형 큰 카드. 이미지는 **항상 고정 URL** (`HERO_BUS_IMG`), 「프리미엄 버스」 alt.

| 영역 | 항목 | 형식 |
|------|------|------|
| 배지 | 「운행 예정」 | 애니메이션 점 + 그라데이션 pill |
| 본문 | 출발일 | `formatDateKorean(startDt)` → `YYYY/MM/DD (요일)` 잘못된 날짜는 `—` |
| | 노선 제목 | `scheduleRouteTitle`: `tripTitle` trim 후 비면 `시작 ↔ 종료`, 한쪽만 있으면 한쪽만 |
| | 계약 금액 | 라벨 「계약 금액」, 값 `₩` + `contractAmount` 숫자 포맷 |
| | 인원 | 라벨 「인원」, `passengerCnt` > 0 이면 `N인승 (만차)`, 아니면 `—` |
| | 출발 시간 | 라벨 「출발 시간」, `formatTimeKorean` (로캘 `ko-KR`, 12시간제) |
| 버튼 | 「운행 상세 확인」 | `reqId` 있을 때 `onTravelerQuoteDetail({ reqId, reqBusSeq })` |
| | 「기사 배정」 | **클릭 핸들러 없음** (UI만) |

### 5-5. 그리드 카드 (`items[1]` 이후)

2열 그리드(`md:grid-cols-2`). **짝수 인덱스(`idx % 2 === 0`)와 홀수 인덱스로 스타일만 번갈아** 가며, **DB의 실제 상태와 무관**하다.

**짝수 스타일 (`isSecondStyle === true`)**

| 항목 | 내용 |
|------|------|
| 배지 | 「운행 확정」 |
| 금액 | 우측 `₩` + `contractAmount` |
| 제목 | `scheduleRouteTitle(it)` |
| 부제 | `날짜 \| HH:mm 출발` (`formatDepartureHm` — 24시간 `HH:mm 출발`) |
| 하단 좌측 | 샘플 아바타 이미지 + `+2` 배지 (데모) |
| 하단 우측 | 「상세보기」 → `onTravelerQuoteDetail` |

**홀수 스타일**

| 항목 | 내용 |
|------|------|
| 배지 | 「대기 중」 |
| 금액·제목·부제 | 짝수와 동일 규칙 |
| 하단 좌측 | 경고 아이콘 + 「잔금 결제 대기 중」 (고정 카피, DB 미연동) |
| 하단 우측 | 「상세보기」 동일 |

**키:** `key={it.reqId || idx}` — 동일 `reqId` 중복 시 React 경고 가능.

### 5-6. 제외된 UI (현재 구현 없음)

- 「전체 일정 / 완료된 운행」 탭
- 「운행 요약」 등 제목 위 보조 라벨
- 하단 푸터(개인정보처리방침·이용약관·쿠키·저작권 문구 등)

---

## 6. REST API — `GET /api/upcoming-trips`

### 6-1. 요청

| 항목 | 값 |
|------|-----|
| 메서드 | `GET` |
| 경로 | `/api/upcoming-trips` |
| 쿼리 | **`driverId`** (필수, 문자열 trim) |

**검증:** `driverId`가 비어 있으면 **400** `{ "error": "driverId가 필요합니다." }`

### 6-2. 성공 응답 (`200`)

Content-Type: JSON.

| 필드 | 타입 | 설명 |
|------|------|------|
| `total` | `number` | `items.length` |
| `items` | `array` | 행 목록 (아래 행 스키마) |
| `emptyMessage` | `string` | **선택.** `items.length === 0` 일 때만 서버가 설정 (`등록된 운행 예정 일정이 없습니다.`) |

#### `items[]` 각 객체 (서버 매핑)

| JSON 키 | DB 출처 · 비고 |
|---------|----------------|
| `reqId` | `TB_AUCTION_REQ.REQ_ID` |
| `reqBusSeq` | `TB_BUS_RESERVATION.REQ_BUS_SEQ`, null/공백이면 응답에서 **숫자 1** |
| `tripTitle` | `r.TRIP_TITLE` |
| `startAddr`, `endAddr` | `r.START_ADDR`, `r.END_ADDR` |
| `startDt`, `endDt` | `r.START_DT`, `r.END_DT` (직렬화 형태는 드라이버에 따름) |
| `passengerCnt` | `r.PASSENGER_CNT` |
| `contractAmount` | `COALESCE(res.DRIVER_BIDDING_PRICE, 0)` 숫자로 정규화 |

`endDt`는 응답에 포함되나 **현재 모달 본문에서는 미표시**.

### 6-3. 오류 응답

| 코드 | 본문 |
|------|------|
| 400 | `{ "error": "driverId가 필요합니다." }` |
| 500 | `{ "error": "<메시지>" }` |

---

## 7. 백엔드 SQL 요약 (`server.js`)

**테이블**

- **`TB_BUS_RESERVATION`** `res`
- **`TB_AUCTION_REQ`** `r`  
  `INNER JOIN` — `r.REQ_ID = res.REQ_ID`

**조건**

- `res.DRIVER_ID = ?` ← 쿼리 `driverId`
- `res.DATA_STAT = 'CONFIRM'`
- `DATE(r.START_DT) >= CURDATE()` — **출발일이 오늘 이전인 행은 제외**

**정렬**

- `ORDER BY r.START_DT ASC` — 목록 첫 카드가 **가장 빠른 출발** 건

---

## 8. 구현상 주의·정합성 메모

1. **「이번 주」 vs SQL:** 부제·「주간 요약」 카피는 주간 뉘앙스이나, API는 **주 범위가 아닌 «오늘 이후 확정 예약»** 전체이다.
2. **그리드 카드 배지·「잔금 결제 대기」:** 목록 인덱스 기반 UI 분기이며 **결제/상태 필드와 연동되지 않음**.
3. **「기사 배정」:** 동작 없음 — 추후 API 연동 시 버튼에 핸들러 추가 필요.
4. **동일 `reqId` 다행:** 조인 결과에 따라 같은 `REQ_ID`가 여러 줄일 수 있음. UI는 고유 키로 `reqId`만 쓰므로 **중복 시 React key 충돌** 가능 — 필요 시 `reqId`+`reqBusSeq` 복합 키 검토.

---

이 문서는 위 경로의 소스를 기준으로 작성되었으며, 코드 변경 시 본 절의 표·문구를 동기화하는 것이 좋다.
