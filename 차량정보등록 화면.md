# 차량 정보 등록 (BusInformationSetup) — 개발 명세

기사가 보유 차량 **1대**를 신규 등록·수정하는 모달의 **UI·API·DB·스토리지**를 한 문서에서 재구현 가능하도록 정리합니다. 테이블·컬럼 정본은 **[`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md)** 의 **`TB_BUS_DRIVER_VEHICLE`**, **`TB_FILE_MASTER`**, **`TB_COMMON_CODE`** 입니다.

---

## 1. 소스·설정

| 구분 | 경로 |
|------|------|
| 모달 컴포넌트 | `busTaams_web/src/components/BusInformationSetup/BusInformationSetup.jsx` |
| CommonView (서류 3종) | `busTaams_web/src/components/CommonView/CommonView.jsx` (기본 메타/스트림/다운로드: 버스 서류 API) |
| 앱 연동 | `busTaams_web/src/App.jsx` — `BusInformationSetup close currentUser` |
| 진입 | `busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx` — 「버스 정보 관리」→ `onBusInfoSetup` |
| 백엔드 | `busTaams_server/server.js` (`insertBusFileMaster`, `canAccessBusFile`, `fetchBusRowForUser`, `orgFileNmAndExt`, `parseDataUrlPayload` 등) |

**API 베이스 URL:** `VITE_API_BASE_URL` (끝 슬래시 제거). 비어 있으면 상대 경로 `/api/...` (Vite 프록시 전제).

---

## 2. 세션·식별

- **`currentUser.custId`** → 모든 API의 `custId` (문자열 트림). 없으면 로드 단계에서 `loadError`, 제출 차단.
- **한 계정당 차량 1건:** 서버가 `TB_BUS_DRIVER_VEHICLE`을 `WHERE CUST_ID = ? LIMIT 1` 로 조회. 신규 `POST` 시 이미 행 있으면 **409**.

---

## 3. 「차량정보등록」모달 화면 구성 (입력항목·형식·배치 순서)

**구현:** `busTaams_web/src/components/BusInformationSetup/BusInformationSetup.jsx`

### 3.1 요약

| 구분 | 설명 |
|------|------|
| **입력** | 텍스트·날짜·체크박스·파일(서류 3종 + 차량 사진 최대 8)·서비스 등급 **버튼 그리드** |
| **`custId` 없음** | 로드 종료 후 `loadError` 표시, 제출 버튼 `disabled`(로딩·오류 시에도 비활성) |
| **보조 패널** | 각 블록 **좌 4컬럼 sticky 제목**(데스크톱)·**우 8컬럼 폼**(구간별 `grid-cols-12`) |

---

### 3.2 레이어·외곽·스크롤·타이포

| 항목 | 값(구현) |
|------|----------|
| **오버레이** | `fixed inset-0 z-[100]`, `flex min-h-0 items-center justify-center`, `overflow-y-auto`, 배경 반투명 박스 `absolute inset-0` 분리 배치, 카드 영역 전체 `backdrop-blur-sm`, 외곽 `p-4` |
| **카드** | `max-w-6xl`, `max-h-[95vh]`, `flex-col overflow-hidden`, `rounded-3xl`, `animate-in fade-in / zoom-in-95` |
| **폰트** | 루트 `Manrope` + `Plus Jakarta Sans`; 블록 제목 `h1`/`h2` 등에 Plus Jakarta 일부 인라인 |
| **본문 영역** | 헤더 아래 **`flex-1 min-h-0 overflow-y-auto`**, 패딩 `px-8 md:px-12 py-8`, `touch-pan-y`, `overscroll-contain`, `no-scrollbar`(유틸) |
| **`body` 스크롤 잠금** | 마운트 시 `document.body.style.overflow = 'hidden'`, 언마운트 `unset` (기사정보 모달처럼 `html`까지는 건드리지 않음) |

---

### 3.3 헤더

| 순서 | 요소 | 형식 |
|------|------|------|
| H-1 | 상단 라벨(영문 느낌) | `existingBusId` 있으면 **「자산 정보」**, 없으면 **「신규 자산 등록」** — `text-[10px] uppercase tracking-widest text-secondary` |
| H-2 | 제목 | **「차량 정보 등록」** 또는 **「차량 정보 수정」** (`text-2xl`, primary, Plus Jakarta) |
| H-3 | 부제 | 「busTaams에 차량 정보를 등록·수정합니다. 저장 시 관리자 검토 후 반영됩니다.」 |
| H-4 | 닫기 | 원형 버튼 `close` 호출 (**취소와 동일 핸들러**) |

하단 테두리 `border-b`, 반투명 헤더 배경.

---

### 3.4 상태 분기 (본문 스크롤 안)

| 조건 | UI |
|------|-----|
| `loadError` | 상단 빨간 박스 `rounded-2xl` |
| `loading` | 중앙 스피너 `progress_activity` + 「불러오는 중…」 |
| 정상 | `<form className="space-y-20" onSubmit={handleSubmit}>` 아래 §3.5~3.8 섹션 |

---

### 3.5 폼 블록 — **위에서 아래** 순서 (`space-y-20`)

각 섹션 공통 패턴: **`grid grid-cols-12 gap-8`**

- **좌 (`md:col-span-4`):** 블록 번호·제목 (`text-3xl`), 설명 단락, `sticky top-0 md:top-8` 배경 반투명
- **우 (`md:col-span-8`):** 실제 입력 UI

---

#### 블록 **01 — 기본 정보**

**좌측(sticky):** 「차량의 핵심 식별 정보를 입력해 주세요.」  
**우측 카드:** `bg-surface-container-low p-8 rounded-3xl space-y-6`  
내부 **`grid grid-cols-2 gap-6`** (일부 행은 `col-span-2`; **주행 거리는 2열 그리드에서 한 칸만** 차지해 `차량번호·모델명` 다음 행에 `제작 연도(full width)` 다음에 위치)

| 순서(#) | 라벨 | 컨트롤 | 형식·제약 |
|---------|------|--------|-----------|
| 1 | 차량 번호 | `<input name="vehicleNo">` | `required`, placeholder `예: 70아 1234`, `rounded-xl p-4` |
| 2 | 모델명 | `<input name="modelNm">` | `required`, placeholder `예: 유니버스 노블 EX` |
| 3 | 제작 연도 | `col-span-2`: `<select manufactureYear>` (옵션 `YYYY년형`) + 같은 행 **「직접 입력」** `input` 숫자 4자리 `maxLength={4}`, 포커스 시 `yearDigitsInput` 편집 / blur 시 상태 초기화. 안내 문구 `yearMin~yearMax년형`. |
| 4 | 주행 거리(km) | `<input name="mileage">` | `type="text"` `inputMode="numeric"` — **표시는** `mileageStoredToFormatted`(콤마), 상태는 숫자만·상한 9_999_999 |

---

#### 블록 **02 — 서비스 등급**

**좌측(sticky):** `GRP_CD=BUS_TYPE` · `USE_YN=Y` · `DISP_ORD` 정렬 안내 및 `DTL_CD` → `SERVICE_CLASS` 저장 요약 문구 (코드와 동일).  
**우측:** `grid grid-cols-3 gap-2`, `max-w-[21rem]` · 버튼 `aspect-square`, `min-h-[2.75rem]` · 이모지 🚌 좌상·하단 중앙 `cdNmKo` (`break-keep`, 작은 화면 `text-[10px]` 등).

- `busCodes` 비었을 때 「버스 종류 코드를 불러오지 못했습니다.」
- 선택: `serviceClass === c.dtlCd` 일 때 강조 ring/배경 primary.

---

#### 블록 **03 — 편의 및 사양**

**좌측(sticky):** 「편의 옵션과 최신 ADAS 여부를 설정합니다.」

1. **ADAS 카드** (`flex-wrap`, 한 줄 블록)  
   - 아이콘 `security` · 제목 「최신 ADAS」·설명 한 줄 · **`checkbox` `name="hasAdas"`** + 라벨 「장착」

2. **어메니티 카드** (`rounded-3xl`)  
   - 제목 「제공 가능한 어메니티」  
   - `flex flex-wrap gap-4` 에 5개 pill 라벨, 각각 **`checkbox`** `wifi` · `usb` · `screen` · `fridge` · `table` (표시명: 와이파이, USB 충전, 개별 스크린, 냉장고, 테이블)

---

#### 블록 **04 — 검사 및 서류**

**좌측(sticky):** 「정기 검사 일정과 필수 운영 서류를 업로드해 주세요.」

1. **날짜 2개** (`grid grid-cols-2 gap-8` 카드 안)  
   - **최근 정기점검일** `input type="date"` `lastInspectDt`  
   - **보험 만료 예정일** `input type="date"` `insuranceExpDt`

2. **서류 3칸** (`grid-cols-1 sm:grid-cols-3 gap-4`) — 순서 고정:

| 순서 | 키 | 제목(UI) | 힌트·아이콘 |
|------|-----|----------|-------------|
| 1 | `biz` | 사업자 등록증 | `upload_file`, PDF/JPG 최대 10MB |
| 2 | `trans` | 운송사업 허가증 | `assignment_turned_in`, 인증 안내 문구 |
| 3 | `ins` | 보험증권 | `verified`, 대인/대물 |

각 칼럼 구조:

- 상단 dashed 박스: `<label>` + 숨김 `<input type="file" accept=ACCEPT_DOC>` → 선택 시 `docPreview`(원격 이미지/ PDF 아이콘 / 로컬 미리보기) + **「제거」** 버튼
- 하단 고정 줄: **「파일보기」** — `mode === 'remote'` 일 때만 활성 → `CommonView`(기본 `metaPath` 등 `CommonView` 기본 버스 서류 경로 사용, `docTitle`은 카드 제목)

---

#### 블록 **05 — 차량 사진**

**좌측(sticky):** 「최대 8장까지 등록할 수 있습니다.」

- 우측: **`grid grid-cols-2 sm:grid-cols-4 gap-3`** 고정 **8슬롯**
- 각 슬롯: `aspect-[4/3]`, dashed 테두리
  - 빈 칸: `label` 덮개 + 숨김 `input file` `accept=ACCEPT_IMAGE` + `add_a_photo` 아이콘
  - 채워짐: `photoPreview`(원격/로컬 `object-cover`) + 우상단 원형 **`×`** `clearPhoto`

**(문서 참고)** 차량 사진용 **별도 「파일보기」 버튼 없음** — 썸네일만 노출.

---

#### 푸터 (`<footer>`) — 폼 최하단, `sticky bottom-0`

| 순서 | 요소 |
|------|------|
| F-1 | 좌: 펄스 점 + 「모든 정보는 관리자 승인 후…」 |
| F-2 | 우: **취소** `type="button"` → `close` |
| F-3 | **제출** `type="submit"` — 라벨: 신규 **「차량 등록 완료」**, 기존 **「차량 수정 완료」** / 처리 중 **「처리 중…」** / 로딩 중 **「불러오는 중…」** · `disabled={isSubmitting \|\| loading \|\| !!loadError}` |

---

### 3.6 오버레이 — `CommonView`

- **`commonViewTarget`** 가 있을 때 렌더, 모달 카드 **밖**(형제 프래그먼트) 동일 화면 전체 레이어에 포개짐.
- `fileId`, `custId`, `docTitle` 전달 (**서류 블록** 전용).

---

### 3.7 제출 검증(UI 관점 요약)

- `handleSubmit`: `custId`, `serviceClass`, 제작 연도 파싱·신규 시 연도 구간, 이후 서버 호출 (상세는 **§6 제출 플로우 요약**).

---

### 3.8 §4 절과의 연계

- **§3** = 모달 **레이아웃·입력 순서·컨트롤 타입**.  
- **§4** = 같은 항목의 **React state·POST/PATCH 필드명·DB** 요약표.

---

## 4. 화면 섹션·필드 (프론트 ↔ API — §3 UI와 대응)

### 01. 기본 정보

| UI | React state (`formData`) | POST/PATCH 바디 키 | DB 컬럼 |
|----|---------------------------|---------------------|---------|
| 차량 번호 | `vehicleNo` | `vehicleNo` (**POST 필수**) | `VEHICLE_NO` |
| 모델명 | `modelNm` | `modelNm` | `MODEL_NM` |
| 제작 연도 | `manufactureYear` 문자열 (**`YYYY년형`** 권장) | 그대로 | `MANUFACTURE_YEAR` (varchar 표시 문자열) |
| 주행거리(km) | `mileage` — 내부는 숫자만 문자열 | `mileage` — `Math.min(Number, 9999999)` | `MILEAGE` (int, 없으면 0) |

**주행거리 UX**

- 상태: 숫자만 저장, **상한 9_999_999**.
- 표시: `mileageStoredToFormatted` → `toLocaleString('ko-KR')` (세 자리 콤마).
- 입력: `type="text"`, `inputMode="numeric"`, `name="mileage"` → `mileageClampFromInput`.

**제작 연도 규칙**

- 선택지: 당해(`currentYear`) 기준 **`currentYear - 20` ~ `currentYear`** 내림차순, 라벨 `toManufactureYearLabel(y)` = `"{y}년형"`.
- 기존 차량 불러오기 시 DB 연도가 위 범위 밖이면 옵션 목록에 **해당 연도 추가**해서 표시.
- **직접 입력:** 4자리 숫자. `blur` 시 `yearDigitsInput` 초기화. 입력 중 포커스 시 네 자리 편집 모드.
- **드롭다운 변경:** 즉시 `manufactureYear`를 해당 `YYYY년형`으로 덮음(직접 입력 상태는 `yearDigitsInput` 리셋).
- 제출 검증:** 신규**(`existingBusId` 없음)이면 해석된 연도가 `[yearMin, yearMax]` 에 있어야 함. **수정**이면 범위 밖 저장된 차량도 유지 가능(검증 완화).

### 02. 서비스 등급

- 소스:** `GET /api/common-codes?grpCd=BUS_TYPE`** → `{ items: [ { dtlCd, cdNmKo, cdFnum? }, … ] }`.
- 표시:** `DISP_ORD ASC, DTL_CD ASC`(서버 SQL). 버튼 **3열 그리드**, `aspect-square`, 선택 값은 **`serviceClass === c.dtlCd`**.
- 백엔드 저장:** `DTL_CD` 문자열 그대로 → `SERVICE_CLASS`.
- 초기값: 데이터 없으면 목록 **`items[0]?.dtlCd`**, 없으면 `'NORMAL'`(코드 존재 가정 시 조정 필요).

### 03. 편의 및 사양

| UI | 저장 |
|----|------|
| 체크박스 `hasAdas` | `hasAdas` boolean → 서버에서 `HAS_ADAS` `'Y'`/`'N'` |
| 어메니티 `wifi`, `usb`, `screen`, `fridge`, `table` | `amenities` 객체 JSON → `AMENITIES` |

**주의 (서버):** `adas: (hasAdas ? true : false)` 를 **`amenities` JSON 안에 병합**해 저장한다(`POST`/`PATCH` 동일). 화면 체크박스와 JSON `adas`가 **이중 표현**이 됨.

**초기 어메니티:** 신규 로드 시 `defaultAmenities()` — 전부 `false`.

### 04. 검사 및 서류

| UI | 로컬 state 키 | POST 필드 | PATCH documents 필드 | `TB_BUS_DRIVER_VEHICLE` | `TB_FILE_MASTER.FILE_CATEGORY` |
|----|-----------------|------------|----------------------|---------------------------|--------------------------------|
| 사업자 등록증 | `docSlots.biz` | `businessLicenseBase64`, `businessLicenseFileName` | 동일 | `BIZ_REG_FILE_ID` | `BIZ_REG` |
| 운송사업 허가 | `docSlots.trans` | `transportationLicenseBase64`, `transportationLicenseFileName` | 동일 | `TRANS_LIC_FILE_ID` | `TRANSPORT_PERMIT` |
| 보험증권 | `docSlots.ins` | `insurancePolicyBase64`, `insurancePolicyFileName` | 동일 | `INS_CERT_FILE_ID` | `INSURANCE` |

- 허용 MIME: 이미지(JPEG/PNG/WEBP/GIF) + PDF. **10MB**.
- 신규 `POST`: Data URL 문자열만 본문에 실어 보냄 (`readFileAsDataUrl`).
- **수정 플로우:** 스칼라만 `PATCH /api/driver/bus`. **로컬로 새로 고른 서류만** `PATCH /api/driver/bus/documents`(하나라도 있을 때만 호출).
- 미리보기: `remote` 슬롯은 `GET /api/driver/bus-documents/file` 썸네일/아이콘.
- **「파일보기」:** `mode === 'remote'` 일 때만 활성 → `CommonView` (`custId`, `fileId`, 기본 `metaPath=/api/common-view/bus-document/meta`, 스트림·다운로드 동일 패턴).

### 05. 차량 사진

- **슬롯 8개** 배열 `photoSlots[i]`: `null` | `{ mode:'remote', fileId }` | `{ mode:'local', file, previewUrl }`.
- 이미지만 허용(동일 MIME 제한). **10MB**.
- 신규 `POST`: `vehiclePhotos`: `[ { base64, fileName }, … ]`(로컬만, 순서 유지).
- **수정:** 항상 `PATCH /api/driver/bus/photos` 호출 · 페이로드 `vehiclePhotos`: 슬롯 순서대로  
  - `remote` → `{ fileId }`  
  - `local` 새 파일 → `{ dataUrl, fileName }`(코드에서는 `readFileAsDataUrl` 결과)  
  - 빈 슬롯은 배열에서 **omit**(앞쪽부터 채워진 연속 목록처럼 보냄 — 빈 칸 없이 압축되지 않고 `for ... if (!s) continue` 로 스킵하므로 **중간 빈 칸은 사라질 수 있음** 구현 주의).

**구현 참고:** 루프는 `slice(0, 8)`이며 빈 슬롯은 건너뜀 ⇒ 사용자가 중간 칸만 비우면 남은 ID가 당김된다. 재구현 시 UX 정책(고정 8칸 vs 압축)을 명시적으로 결정할 것.

---

## 5. 최초 로드 (마운트 `useEffect`)

`Promise.all`:

1. `GET /api/common-codes?grpCd=BUS_TYPE`
2. `GET /api/driver/bus?custId=…`

실패 시 `fetchJson`이 HTML 응답 등이면 사용자 친화적 에러 메시지.

**`bus` 없음**

- `existingBusId = null`
- `serviceClass = items[0]?.dtlCd || 'NORMAL'`
- `amenities = defaultAmenities()`
- 문서/사진 슬롯 초기화.

**`bus` 있음**

- 폼 채우기(`mileageFromServer`), `amenities`에 기본값 머지 후 **`delete am.adas`** (화면 어메니티 객체에서 `adas` 제거 — 체크는 `hasAdas` 로만 표시).
- `docSlots` / `photoSlots` 를 `remote` 로 구성하고 라벨은 `ORG_FILE_NM` + `FILE_EXT`.

---

## 6. 제출 플로우 요약

| 단계 | 조건 | API |
|------|------|-----|
| 1 | `!existingBusId` | `POST /api/driver/bus` (본문 **§7 REST**) · 성공 시 `busId`로 `existingBusId` 설정(알림만, 자동 재조회는 현재 없음). |
| 2 | 수정 | `PATCH /api/driver/bus` → (서류 로컬 있으면) `PATCH /api/driver/bus/documents` → `PATCH /api/driver/bus/photos` |

검증: `custId`, `serviceClass`, 제작연도(신규 시 범위), `vehicleNo` required(폼 속성).

---

## 7. REST API 레퍼런스 (`server.js`)

### `GET /api/common-codes?grpCd=BUS_TYPE`

- 응답: `{ items: [ { dtlCd, cdNmKo, cdFnum } ] }` (`USE_YN='Y'` · `ORDER BY DISP_ORD, DTL_CD`).
- 선택적: `BUSTAAMS_AUTO_ENSURE_TABLES` 가 true 등이면 `TB_COMMON_CODE` 생성 시도(Cloud SQL 앱 계정은 보통 DDL 거부 · `.env.example` 참고).

### `GET /api/driver/bus?custId=`

- 차량 없음: `{ bus: null }`.
- 있음: `busId`, `custId`, 스칼라 필드, `amenities`(파싱된 객체), `hasAdas` boolean, 파일 메타 `bizRegFile`/`transLicFile`/`insCertFile`, `vehiclePhotoFileIds[]`.
- `ER_NO_SUCH_TABLE`: `{ bus: null }` 로 폴백.

### `POST /api/driver/bus`

- 필수:`custId`, `vehicleNo`, `serviceClass`. 선택: `modelNm`, `manufactureYear`, `mileage`, `amenities`, `hasAdas`, 날짜, 서류 Base64 필드 세트, `vehiclePhotos[]`.
- `BUS_ID`:`generateNextNumericId(MAX(BUS_ID), 10)` (**varchar(10) 0패딩**).
- 트랜잭션: `INSERT TB_BUS_DRIVER_VEHICLE` → 선택 서류마다 **`insertBusFileMaster`** + 해당 `*_FILE_ID` UPDATE → 선택 사진 동일 패턴 후 `VEHICLE_PHOTOS_JSON` UPDATE.
- 응답 **201**: `busId`, `fileIds`(문서 업로드 시), `vehiclePhotoFileIds`.

### `PATCH /api/driver/bus`

- 필수:`custId`, `busId`, **`vehicleNo`**, **`serviceClass`**. 행 존재·소유 검증.
- `AMENITIES` JSON + 서버측 `adas` 병합, `HAS_ADAS` 문자.

### `PATCH /api/driver/bus/documents`

- 필수:`custId`, `busId`.
- 세 필드 중 **하나라도** Data URL 문자열 없으면 **400** (`갱신할 서류(base64)가 없습니다`).
- 각 문서 새 `FILE_ID`(MAX+1, **20자 0패딩**), `insertBusFileMaster`, 컬럼 UPDATE. **기존 FILE_MASTER/GCS 정리(삭제) 없음** — 새 ID로 덮어쓰기만.

### `PATCH /api/driver/bus/photos`

- 필수:`custId`, `busId`. `vehiclePhotos` 최대 8건.
- 엔트리: `{ fileId }` 만 있으면 기존 재사용; `base64`|`dataUrl` 있으면 새 업로드.
- 결과 배열 전체를 `VEHICLE_PHOTOS_JSON` 에 저장.

### `GET /api/driver/bus-documents/file?custId=&fileId=`

1. **`canAccessBusFile`** — 해당 기사 차량 행의 `BIZ_REG_FILE_ID`/`TRANS_LIC_FILE_ID`/`INS_CERT_FILE_ID` 또는 `VEHICLE_PHOTOS_JSON` 배열 원소와 `fileId` 일치(strict `===`; 패딩 불일치 시 실패 가능).
2. `TB_FILE_MASTER` 로 `GCS_PATH` 조회 후 **기본 `bucket`** 으로 스트림(현 코드는 행의 `GCS_BUCKET_NM` 미사용 — 운영 시 버킷 통일 필요).

### CommonView 버스 서류 (기본 prop)

| 용도 | 경로 |
|------|------|
| 메타 | `GET /api/common-view/bus-document/meta?custId=&fileId=` |
| 스트림 | `GET /api/driver/bus-documents/file?custId=&fileId=` |
| 다운로드 | `GET /api/common-view/bus-document/download?custId=&fileId=` |

권한: 내부적으로 `canAccessBusFile` 과 동일 기준.

---

## 8. GCS · `TB_FILE_MASTER` 규격

### `insertBusFileMaster`

1. `bucket.file(gcsPath).save(buffer, { contentType, resumable:false })`
2. `INSERT TB_FILE_MASTER (FILE_ID, FILE_CATEGORY, GCS_BUCKET_NM, GCS_PATH, ORG_FILE_NM, FILE_EXT, FILE_SIZE, REG_DT)`  
   - `GCS_BUCKET_NM`: 서버 환경의 **`bucketName`** (예 `bustaams-secure-data`)
   - `GCS_PATH`: 아래 규칙의 **객체 키(상대 경로)**

### 경로 규칙 (현행 코드)

| 구분 | `FILE_CATEGORY` | `GCS_PATH` 패턴 |
|------|-----------------|----------------|
| 서류 3종 | `BIZ_REG` 등 | `{FILE_CATEGORY}/{custId}/{fileId}.{ext}` |
| 차량 사진 | `VEHICLE_PHOTO` | `VEHICLE_PHOTO/{custId}/{fileId}.{ext}` |

- **`fileId`:** `TB_FILE_MASTER` 채번 `generateNextNumericId(maxId, **20**)`.
- **`orgFileNm`/`fileExt`:** 업로드 파일명 힌트 + Data URL MIME 기반 (`orgFileNmAndExt`).

### 업로드 본문

- `parseDataUrlPayload`:** `data:...;base64,...`** 형식 필요. 순수 raw base64만 오면 신규 사진 처리에서 JPEG로 감싸는 분기 등이 **`POST`** 와 **`PATCH`** 에 혼재 — 프론트는 **항상 Data URL**을 권장.

---

## 9. 차량 사진과 CommonView (요약)

- **별도 「문서 보기」 버튼 없음.** 썸네일은 동일 **`bus-documents/file`** URL 사용.
- 자격증·버스 서류와 달리 `CommonView` 모달 진입 버튼이 사진에는 없음.

---

## 10. 재구현 체크리스트

1. `custId` 필수 및 `fetchJson`/HTML 에러 처리.
2. 로드 시 공통코드 + 차량 병렬, `exists` 에 따른 분기.
3. 주행거리 포맷·상한 일치.
4. 제작연도 목록·직접입력·수정 모드 검증 차이.
5. 신규 **409**(이중 등록)·수정 시 **PATCH 3종** 순서 및 문서 PATCH 조건부 호출.
6. 파일 MIME/10MB 검증 및 Object URL revoke(언마운트 cleanup).
7. `canAccessBusFile` 과 `FILE_ID` 표기(패딩) 일관성 확인.
8. 사진 PATCH 시 **슬롯 스킵**으로 인한 배열 축약 동작 검토 제품 의도 확인.

---

*작성 기준: `BusInformationSetup.jsx`, `server.js` 해당 라우트, `BusTaams_Project 테이블 설계.md` 차량·파일 마스터·공통코드 절.*
