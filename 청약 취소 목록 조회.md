# 청약 취소 목록 조회 — 동작 정리

운전기사 대시보드의 **「청약 취소 목록 조회」** 버튼을 눌렀을 때 어떤 API가 호출되고, DB에서는 어떤 조건으로 행이 걸러지는지 정리한다.

---

## 1. UI 진입

| 항목 | 내용 |
|------|------|
| 화면 | 기사 **메인 대시보드** (`DriverDashboard.jsx`) |
| 메뉴 위치 | 「등록/변경 메뉴」 그리드 — **청약 취소 목록 조회** (`id: 'cancel'`) |
| 동작 | `showDriversCancellationList === true` 로 **`DriversListOfBids`** 모달 표시 |
| 전달 props | `driverId` = `currentUser.custId` 또는 `currentUser.userId`, **`variant="cancelled"`** |

**거래 제한:** 「여행 요청 목록 조회」만 `tradeRestrictYn === 'Y'` 일 때 비활성화된다. **「청약 취소 목록 조회」는 거래 제한과 무관하게 클릭 가능**하다.

모달 제목: **청약 취소 목록** (활성 목록과 동일 컴포넌트이나 `variant` 만 다름).

---

## 2. 프론트엔드 API 호출

**컴포넌트:** `busTaams_web/src/components/DriversListOfBids/DriversListOfBids.jsx`

모달이 열리고(`open === true`) `driverId` 가 있으면 다음을 호출한다.

| 메서드 | URL |
|--------|-----|
| `GET` | `{API_BASE}/api/DriversListOfBids` |

**Query 파라미터**

| 이름 | 필수 | 설명 |
|------|------|------|
| `driverId` | ○ | 기사 식별자(`CUST_ID` 등, 서버에서 `TB_BUS_RESERVATION.DRIVER_ID` 와 매칭) |
| `page` | △ | 페이지 번호, 기본 `1` |
| `pageSize` | △ | 페이지 크기, 컴포넌트 기본 **`6`** (서버는 1~100으로 클램프) |
| `mode` | △ | **`cancelled`** 일 때만 «청약 취소 목록» 모드. 생략 시 기본은 활성 청약/여행 목록(`active`) |

청약 취소 목록일 때만 URL에 **`mode=cancelled`** 가 붙는다.

---

## 3. 서버 조회 조건 (핵심)

**파일:** `busTaams_server/routes/driversListOfBids.js`  
**경로:** `GET /api/DriversListOfBids`

`mode=cancelled` 인 경우 **`baseWhere`** 는 다음 한 줄과 동치이다.

```sql
r.DRIVER_ID = ? AND r.DATA_STAT = 'DRIVER_CANCEL'
```

- **`r`**: `TB_BUS_RESERVATION` 별칭  
- **바인딩 `?`**: 요청의 **`driverId`** (문자열 trim 후 그대로 전달)  
- **`DATA_STAT = 'DRIVER_CANCEL'`**: 기사 사유 청약 취소로 처리된 예약만 포함한다.

즉, **해당 기사가 DRIVER 로 묶인 예약 중, 상태가 기사 청약 취소인 행만** 목록에 나온다. 다른 취소 코드(여행자 취소 등)는 이 화면 조건에 포함되지 않는다.

---

## 4. 조인·정렬·페이지네이션

| 항목 | 내용 |
|------|------|
| 조인 | `TB_BUS_RESERVATION r` **`LEFT JOIN`** `TB_AUCTION_REQ ar ON ar.REQ_ID = r.REQ_ID` |
| 목적 | 견적 요청일(`quoteRequestDt`), 여행 제목, 출발·도착일 등 표시용 컬럼 보강 |
| 정렬 | **`ORDER BY r.RES_ID DESC`** (최근 예약 ID 우선) |
| 건수 | 동일 `WHERE` 로 `COUNT(*)` 후 `LIMIT` / `OFFSET` 적용 |

`mode=cancelled` 에서도 활성 목록과 같은 조인·정렬 규칙을 쓴다. 활성 목록 전용 필터(당일 출발 `CONFIRM` 제외 등)는 **`cancelled` 모드에는 적용되지 않는다.**

---

## 5. 활성 목록과의 차이 (참고)

| 구분 | `mode` 없음 / 기본 (`active`) | `mode=cancelled` |
|------|-------------------------------|------------------|
| `DATA_STAT` | `IN ('BIDDING','CONFIRM')` 추가로 **`CONFIRM` 이면서 견적 `START_DT` 가 오늘인 행 제외** | **`DRIVER_CANCEL` 만** |
| UI | 제목 «기사님 청약/여행 목록», 행당 **청약취소** 버튼 | 제목 «청약 취소 목록», 취소 버튼 컬럼 없음 |

---

## 6. 응답 요약

성공 시 본문 최상위 키 **`DriversListOfBids`**:

- `screenId`: `"DriversListOfBids"`
- `items`: 행 배열 (`resId`, `reqId`, `reqBusSeq`, `travelerId`, `driverId`, `busId`, `driverBiddingPrice`, `dataStat`, `statusLabelKo`, `statusCategory`, `quoteRequestDt`, `regDt`, `tripTitle`, `tripStartDt`, `tripEndDt` 등)
- `page`, `pageSize`, `total`

`DRIVER_CANCEL` 행은 표시용으로 **`statusLabelKo`: «청약 취소»**, **`statusCategory`: `cancelled`** 로 매핑된다.

---

## 7. 관련 구현 파일

| 경로 | 역할 |
|------|------|
| `busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx` | 버튼 → `DriversListOfBids` `variant="cancelled"` |
| `busTaams_web/src/components/DriversListOfBids/DriversListOfBids.jsx` | `mode=cancelled` 쿼리스트링·목록 UI |
| `busTaams_server/routes/driversListOfBids.js` | SQL·페이지네이션·JSON 조립 |
| `busTaams_server/server.js` | 라우트 등록 |

---

## 8. 운영 시 확인 포인트

- 목록에 나오려면 **`TB_BUS_RESERVATION.DRIVER_ID`** 와 로그인 기사 `driverId` 가 일치하고 **`DATA_STAT = 'DRIVER_CANCEL'`** 이어야 한다.
- 기사 청약 취소 플로우는 `기사님 청약 취소.md` 의 API(`CancellationOfBid`)·`executeDriverBidCancellation` 과 연결된다.
