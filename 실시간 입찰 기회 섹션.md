# 실시간 입찰 기회 섹션 — 구현 명세

기사 대시보드(`DriverDashboard`)의 **실시간 입찰 기회** 영역과, 이를 채우는 **`GET /api/auction-list`** 및 상세 모달 연동을 정리한다.  
테이블·컬럼 정본은 **[`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md)**.

---

## 1. 범위: 섹션과 모달

| 구분 | 설명 |
|------|------|
| **실시간 입찰 기회** | `DriverDashboard` 하단의 인페이지 **섹션** (`AuctionList`). URL 라우트가 따로 없다. |
| **상세(조회·입찰)** | 카드/버튼 클릭 시 `App.jsx`에서 **`TravelerQuoteRequestDetails`** 모달. 상세 API·화면 필드는 [`여행자 견적 요청 상세.md`](./여행자%20견적%20요청%20상세.md) 참고. |

---

## 2. 관련 파일

| 역할 | 경로 |
|------|------|
| UI·목록·카드 | `busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx` (`AuctionList`, `quoteDetailFromAuctionItem`, `refetchAuctionList`) |
| 앱·모달 | `busTaams_web/src/App.jsx` (`travelerQuoteSel`, `openTravelerQuoteDetail`, `TravelerQuoteRequestDetails`) |
| REST (실행 중) | `busTaams_server/server.js` — `app.get('/api/auction-list', …)` |
| 분리 모듈(미등록) | `busTaams_server/routes/auctionList.js` — `server.js`에서 `require` 되지 않아 **동일 경로에 연결되지 않음**. 참고용. |

---

## 3. FrontEnd — `AuctionList`

### 3-1. 입력(Props)

| Prop | 타입·기본값 | 설명 |
|------|-------------|------|
| `items` | 배열 | `GET /api/auction-list` 응답의 `items` |
| `loading` | boolean | 목록 조회 중 |
| `loadError` | string \| null | 오류 메시지 |
| `emptyMessage` | string, 기본 `'등록된 입찰이 없습니다'` | 0건일 때 카드 영역 문구 |
| `onBidClick` | `(sel: { reqId, reqBusSeq }) => void` | 입찰 제한 통과 시 상위로 전달 |
| `onRetry` | `() => void` | 오류 시 「다시 시도」 |
| `currentUser` | 객체 | 취소 패널티·식별에 사용 |

부모(`DriverDashboard`)는 `currentUser?.custId \|\| currentUser?.userId` 로 `driverId` 쿼리를 보내며, **`custId` 없으면** 목록을 비우고 로딩만 종료한다.

### 3-2. 내부 상태

| 상태 | 설명 |
|------|------|
| `currentIndex` | 다건일 때 **현재 보이는 1건** 인덱스. `items` 변경 시 0으로 리셋. |

### 3-3. 카드에 쓰는 API 필드(출력 매핑)

응답 객체 키 기준(서버 `server.js` 별칭과 일치).

| UI 위치 | 필드 | 형식·처리 |
|---------|------|-----------|
| 카드 제목 | `tripTitle` | 문자열 trim 후 비어 있으면 `` `${startAddr} ↔ ${endAddr}` `` |
| 노선 한 줄 | `startAddr`, `endAddr` | ` → ` 연결, 없으면 `—` |
| 상태 배지 | `reqStat` | `DATA_STAT`; 없으면 **`AUCTION`** 가정 후 **대문자** pill |
| 등록 시각 | `regDt` | `formatCardDateTime` → `YYYY/MM/DD HH:mm` 또는 `—` |
| 출발 / 도착 | `startDt`, `endDt` | 동일 포맷 |
| 탑승 | `passengerCnt` | 숫자 + `명`, 없으면 `—명` |
| 버스 | `busType`, `busCnt` | `busType` 없으면 `—`; `busCnt` ≤0 이면 표시 `1`대 |
| 예산 | `reqAmt` | `Number` 후 `toLocaleString('ko-KR')`, `예산 총액: ₩…` |
| CTA 버튼 라벨 | `myBidStat` | 아래 [3-5](#3-5-입찰-버튼-취소-안내) |

**썸네일 이미지**는 사용하지 않는다.

### 3-4. 날짜·시간 표기 (`formatCardDateTime`)

- 입력: API datetime(문자열·Date 가능).
- 유효하지 않으면 원문 문자열 또는 `—`.
- 출력: `YYYY/MM/DD HH:mm` (로컬).

### 3-5. 입찰 버튼·취소 안내

- **`myBidStat`** 이(대소문자 무시) **`REQ` · `BIDDING` · `AUCTION`** 이면 버튼 문구 **「입찰 제시 변경」**(윤곽 스타일), 아니면 **「입찰 참여」**(채움).
- **`currentUser.cancelManage.cancelBusDriverCnt >= 3`** 이면 카드·버튼 클릭 시  
  **`안내: 취소 건수가 {n}회 누적되어, 현재 더 이상의 버스 입찰에 참여하실 수 없습니다.`** (`alert`) 후 모달로 넘기지 않음.
- 상세 오픈 payload: `quoteDetailFromAuctionItem(item)` → `{ reqId: String, reqBusSeq: number }`  
  - `reqBusSeq`: 응답의 `reqBusSeq`가 유효한 숫자가 아니면 **1**.  
  - `reqId` 비면 `onBidClick` 호출 안 함.

### 3-6. 목록 순환 UI

| 조건 | 동작 |
|------|------|
| `items.length <= 1` | 자동 순환 **없음**, 해당 건만 표시 |
| `items.length >= 2` | **5초**마다 `currentIndex = (i+1) % len` |
| 표시 | `items.slice(currentIndex, currentIndex+1)` — **항상 1카드** |
| 전환 클래스 | 카드에 `animate-auction-slide-up` |

### 3-7. 화면 분기·안내 문구(고정 문자열)

| 분기 | 사용자에게 보이는 문구·요소 |
|------|------------------------------|
| 로딩 | 스피너 아이콘 `progress_activity` + **「불러오는 중…」** |
| 오류 | 빨간 박스에 `loadError` + 버튼 **「다시 시도」**(`onRetry` → `refetchAuctionList`) |
| 0건 | 흰 카드 영역에 `emptyMessage`(기본 **「등록된 입찰이 없습니다」**) |
| 섹션 제목 | **「실시간 입찰 기회」** (`h3`, `id="driver-auction-heading"`) |

`GET` 응답에 `emptyMessage`가 없어도(현재 `server.js`는 미부가) 프론트가 위 기본값을 쓴다.

### 3-8. 접근성·인터랙션

- 카드: `role="button"`, `tabIndex={0}`, Enter/Space로 `tryOpenDetail`.
- 하단 우측 **chevron_right**는 장식(`aria-hidden`).

---

## 4. FrontEnd — 부모 `DriverDashboard`에서의 데이터 흐름

| 항목 | 내용 |
|------|------|
| API 베이스 | `VITE_API_BASE_URL` 없으면 `http://127.0.0.1:8080` |
| 호출 | `GET ${API_BASE}/api/auction-list?driverId=${encodeURIComponent(driverCustId)}` |
| 초기 로드 | `stats`, `schedule/today`, `auction-list` 를 `Promise.allSettled` 로 병렬 |
| 재시도 | `refetchAuctionList` — 입찰 목록만 다시 요청 |
| 모달 연결 | `<AuctionList onBidClick={(sel) => onTravelerQuoteDetail?.(sel)} />` — 취소 3건 차단은 `AuctionList` 내부에서 처리 |

---

## 5. FrontEnd — `App.jsx` 모달 연동

| 항목 | 내용 |
|------|------|
| 상태 | `travelerQuoteSel`: `{ reqId, reqBusSeq } \| null` |
| 오픈 | `openTravelerQuoteDetail(payload)` — 객체는 `reqId`·`reqBusSeq`, 문자열이면 `reqBusSeq: 1` |
| 렌더 | `travelerQuoteSel && <TravelerQuoteRequestDetails reqId reqBusSeq close currentUser />` |
| 상세 API | 보통 `GET /api/traveler-quote-request-details?reqId=&custId=` (`server.js`) |

---

## 6. REST API — `GET /api/auction-list` (`server.js`)

### 6-1. 요청

| 항목 | 값 |
|------|-----|
| 메서드 | `GET` |
| 경로 | `/api/auction-list` |
| 쿼리 | **`driverId` 필수**. 없으면 **400** `{ error: 'driverId가 필요합니다.' }` |

### 6-2. SQL 조건(마스터 `TB_AUCTION_REQ` `r`)

1. `r.DATA_STAT = 'AUCTION'`
2. `DATE(r.START_DT) > CURDATE()`
3. **NOT EXISTS**: 해당 기사(`DRIVER_ID` = `driverId`)가 **다른** 요청(`ar.REQ_ID <> r.REQ_ID`)에서 `TB_BUS_RESERVATION.DATA_STAT = 'CONFIRM'` 이고, 그 요청의 **`DATE(ar.START_DT)`** 가 현재 행의 **`DATE(r.START_DT)`** 와 같으면 현재 행 **제외**.

정렬: `ORDER BY r.REG_DT DESC`.

### 6-3. 사용 테이블(설계서 기준)

- `TB_AUCTION_REQ` — 필터·스칼라
- `TB_AUCTION_REQ_BUS` — 첫 `BUS_TYPE_CD`, `COUNT(*)` 대수, 첫 `REQ_BUS_SEQ` → `reqBusSeq`
- `TB_BUS_RESERVATION` — 동일일 확정 제외, 기사별 최신 `myBidStat`

### 6-4. 응답 본문

성공 **200**:

```json
{
  "total": <number>,
  "items": [ /* 아래 행 객체 배열 */ ]
}
```

행 객체 필드(SELECT 별칭):

| 키 | 의미 |
|----|------|
| `reqId` | `REQ_ID` |
| `tripTitle` | `TRIP_TITLE` |
| `startAddr`, `endAddr` | 주소 |
| `startDt`, `endDt` | 출발·도착 일시 |
| `passengerCnt` | 탑승 인원 |
| `reqStat` | `DATA_STAT` |
| `reqAmt` | `COALESCE(REQ_AMT,0)` |
| `expireDt` | `EXPIRE_DT` |
| `regDt` | `REG_DT` |
| `busType` | 첫 요청 버스 `BUS_TYPE_CD` |
| `busCnt` | 해당 `REQ_ID`의 버스 행 개수 |
| `reqBusSeq` | 가장 작은 `REQ_BUS_SEQ` |
| `myBidStat` | 이 기사·이 `REQ_ID` 예약 **최신 1건** `DATA_STAT` (`ORDER BY REG_DT DESC LIMIT 1`) |

- **0건일 때** `emptyMessage` 필드는 **`server.js` 구현상 붙이지 않음** (프론트 기본 문구).
- 오류 **500**: `{ error: "<메시지>" }`.

### 6-5. `routes/auctionList.js` 참고

레포에 동일 경로용 모듈이 있으나 **`server.js`에 등록되지 않음**. 분기 SQL·`driverUuid`·`myBidStat` 매핑 등은 **현행 동작과 다를 수 있음**.

---

## 7. 요약 체크리스트 (재구현 시)

1. `driverId` = 기사 `custId`/`userId` 문자열, `TB_BUS_RESERVATION.DRIVER_ID` 와 매칭.
2. 카드 1건 뷰·5초 순환·취소 3건 `alert`·버튼 `REQ`/`BIDDING`/`AUCTION` 처리.
3. 상세 모달은 `{ reqId, reqBusSeq }`; `server.js` 목록에 `reqBusSeq` 포함.
4. 실제 배포 기준 API는 **`server.js`** 의 `GET /api/auction-list`.

---

*Last updated: 2026-05-06*
