# 기사정보등록 (Driver Profile Setup) — 개발 명세

본 문서는 **`기사 정보 관리` 모달**을 처음부터 동일하게 구현·연동하기 위한 **단일 레퍼런스**입니다. 테이블·컬럼 타입은 **[`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md)** 를 정본으로 합니다.

---

## 1. 코드·설정 레퍼런스

| 구분 | 경로 |
|------|------|
| 프론트 모달 | `busTaams_web/src/components/DriverProfileSetup/DriverProfileSetup.jsx` |
| 대시보드 진입 | `busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx` — 바로가기 「기사 정보 관리」 → `onProfileSetup()` |
| 앱 마운트 | `busTaams_web/src/App.jsx` — `showProfileSetupModal && <DriverProfileSetup close={…} currentUser={user} />` |
| CommonView | `busTaams_web/src/components/CommonView/CommonView.jsx` |
| 백엔드 API·DB | `busTaams_server/server.js` |
| 면허·자격 진위 | `busTaams_server/driverVerification.js` |
| 채번·FILE_ID 패딩 | `busTaams_server/lib/bustaamsIds.js` (`canonicalFileMasterFileId`, `fileIdMatchCandidates`, `custIdMatchCandidates`) |
| DDL 가드(CI) | `scripts/ci/check-bustaams-no-ddl.sh`, `.github/workflows/bustaams-schema-ddl-guard.yml` |

**프론트 API 베이스 URL:** `import.meta.env.VITE_API_BASE_URL || 'http://127.0.0.1:8080'` (`DriverProfileSetup.jsx` 의 `API_BASE`).

---

## 2. 진입·목적·세션

- **목적:** 기사 추가 인적 정보(성명 등), 주소, 운전면허, 운수종사 자격번호·사본 파일, 자기소개 저장. **주민등록번호는 본 모달에서 저장·수정하지 않으며** `TB_USER.RESIDENT_NO_ENC` 를 **조회·표시만** 한다(§4.3).
- **접근:** 드라이버 대시보드 → 「기사 정보 관리」(QuickMenu의 `driverProfile`) → 부모에서 모달 표시(`DriverProfileSetup`).
- **필수 세션 필드:**
  | 용도 | 필드 | 설명 |
  |------|------|------|
  | 모든 `GET`/스트림/CommonView 쿼리 | `custId` | `TB_USER.CUST_ID` (문자열, 트림). 서버가 `custIdMatchCandidates`로 패딩 변형까지 허용. |
  | `POST /api/driver/profile-setup` 본문 | `userId` | **`TB_USER.USER_ID`(로그인 ID, varchar)**. 서버가 이 값으로 회원 행 조회 후 내부 `CUST_ID` 결정. |

`App.jsx` 등에서 `normalizeUserSession` 결과 객체에 **`custId`** 와 **`userId`** 가 모두 있어야 본 플로우가 끝까지 동작합니다.

---

## 3. 「기사정보등록」모달 화면 구성 (입력항목·형식·배치 순서)

**구현:** `DriverProfileSetup.jsx` — 화면 문구 「운전 기사 기본 정보」.

### 3.1 요약: 입력 가능 범위

| 구분 | 설명 |
|------|------|
| **직접 편집 가능** | 성명·주소 구분·명칭(OTHER)·상세 주소·면허 전 필드·자격번호·자기소개·자격증 파일 선택 |
| **읽기 전용** | **주민등록번호**(TB_USER 조회·마스킹 표시), 휴대전화, 우편번호·기본 주소(카카오 우편번호 팝업으로만 채움), 프로필 사진 표시 영역 |
| **업로드 UI 없음** | **프로필 사진** — 서버 저장분만 표시(§4.1)·본 모달에서 변경 불가 |
| **보조 패널** | 좌측(데스크톱) 또는 상단 칩(모바일) **단계 네비** — 입력이 아니라 앵커 스크롤 |

---

### 3.2 레이어·외곽·z-index·타이포

| 항목 | 값(구현) |
|------|----------|
| **루트 오버레이** | `fixed inset-0 z-[100]`, `flex` 가운데 정렬, `overflow-y-auto`, `bg-gray-900/50 backdrop-blur-sm`, `p-4` |
| **메인 카드** | `max-w-6xl`, `max-h-[95vh]`, `rounded-3xl`, `flex-col overflow-hidden`, 애니메이션 `fade-in` / `zoom-in-95` |
| **타이포** | 루트 `style`: `'Manrope', 'Plus Jakarta Sans', sans-serif` |
| **성공 안내 모달** | 조건부, 동일 `z-[100]`·전체 덮는 `bg-black/45` — 본 모달과 **같은 층**이므로 DOM 순서상 나중에 그려짐 |
| **자격증 CommonView** | 모달 내부 조건부 렌더, 별도 상세는 `CommonView.jsx` |

---

### 3.3 헤더·닫기

| 순서 | 요소 | 형식·동작 |
|------|------|-----------|
| H-1 | 제목 | **「운전 기사 기본 정보」** `text-3xl md:text-4xl font-headline font-extrabold text-primary` |
| H-2 | 부제 | 「busTaams의 프리미엄 캡틴이 되기 위한 필수 정보를 입력해주세요.」 |
| H-3 | (조건부) 안내 | `profileExistsOnServer` 이면 저장된 정보 로드·면허·자격 「수정」 안내 문단 |
| H-4 | 닫기(X) | `onClick={close \|\| onBack}` — **둘 중 하나** 필요 |

`border-b`, 반응형 패딩 `px-4 md:px-8 lg:px-10`.

---

### 3.4 본문 2단 구조 (데스크톱 `lg`)

**행:** `lg:flex-row` — **좌 72(고정 네비)** | **우 스크롤 폼**(`max-w-3xl` 중앙 느낌, `flex-1`, `overflow-y-auto`).

#### A. 좌측 사이드 네비 (`hidden lg:flex`, `w-72`)

| 순서 | 요소 |
|------|------|
| A-1 | `STEPS` 반복 버튼 3개 — 번호 원(1–3) + 라벨 **「기본 인적사항」「운전면허 정보」「운송종사자 자격」** |
| A-2 | 클릭 시 `scrollToSection(id)` — 우측 `scrollRef` 안 해당 `h2`(title1Ref~3Ref)로 스무스 스크롤, `activeStep` 갱신 |
| A-3 | 하단 **「주의사항」** 카드 — 관리자 승인·허위 기재 제한 문구 |

#### B. 모바일 상단 칩 (`lg:hidden`)

- 가로 스크롤 `overflow-x-auto` 줄에 동일 3단계를 **캡슐 버튼**으로 표시, 동작은 앵커 스크롤과 동일.

#### C. 우측 스크롤 영역 (`scrollRef`)

- **ScrollSpy:** 스크롤 위치에 따라 `activeStep` 1~3 자동 동기화(`section1Ref`~`section3Ref`의 `offsetTop` 기준).
- **터치:** `touch-pan-y`, `overscroll-y-contain`, `[scrollbar-gutter:stable]`.

---

### 3.5 스크롤 영역 안 — **위에서 아래** 배치 순서 (필드 트리)

아래 번호는 **DOM·시각적 순서**이며, §4~§7의 DB·API 설명과 대응한다.

#### 블록 P — 프로필 사진 카드 (섹션 래퍼, `section1Ref` 내부 최상단)

| 순서 | UI | 형식·비고 |
|------|-----|-----------|
| P-1 | 썸네일 박스 | 128×128 둥근 사각, `profilePhoto` URL 있으면 `<img>`, 없으면 `account_circle` 아이콘 |
| P-2 | 호칭 `<h3>` | `profileModalTitle` 우선(`{userName} 기사님`), 없으면 `formData.name` 기반 「○○ 기사님」, 둘 다 없으면 「기사님」 |
| P-3 | (조건부) 경고 | `hasProfilePhotoOnServer && !profilePhoto` 일 때 amber 문구(미리보기 실패) |

#### 블록 S1 — 「기본 인적사항」(`title1Ref`, `id="driver-step-title-1"`)

행 상단: 제목 + 「* 필수 입력」.

**그리드 `grid-cols-1 md:grid-cols-2 gap-6` 안 순서:**

| # | 라벨 | 컨트롤 | 형식·제약 |
|---|------|--------|-----------|
| 1 | 성명 | `<input name="name" type="text">` | placeholder「홍길동」, `h-14 rounded-xl`, 전폭 편집 |
| 2 | 주민등록번호 | **`<input readOnly>`** 한 줄 · 라벨에 「조회 전용」 표시 | `GET profile-setup` 의 **`residentNoDisplay`**. 서버가 `TB_USER.RESIDENT_NO_ENC` 복호 후 **`YYMMDD-C●●●●●●`** 로 마스킹. 미등록 시 placeholder 안내. **입력·수정·POST 미포함.** |
| 3 | 휴대전화 번호 | `<input readOnly>` | `formatHpKrDigits(phoneNo)` 표시만, 회색 테두리, 안내 「회원 정보 메뉴에서…」 — **`md:col-span-2`** |
| 4 | 주소 구분 | `<select name="addrType">` | 옵션: `HOME` 자택 · `OFFICE` 회사 · `OTHER` 이외 |
| 5 | 주소구분명칭 | `<input>` | `OTHER`일 때만 활성·**그레프렘 최대 10자** 클램프, 라벨에 OTHER 필수 표시 |
| 6 | (행) 주소 검색 버튼 | `<button>` | 카카오 다음 우편번호 스크립트 팝업 — **`md:col-span-2`** |
| 7 | 우편번호 | `readOnly input` | 팝업 결과 `zonecode` |
| 8 | 기본 주소 | `readOnly input` | 도로명/지번 우선(`roadAddress` 등 팝업 데이터) |
| 9 | 상세 주소 | `<input name="detailAddress">` | **최대 100자**(그레프렘 클램프), `md:col-span-2` |

주소 블록은 내부적으로 `grid`/`flex-wrap`으로 4~9가 한 섹션에 묶여 있다.

#### 블록 S2 — 「운전면허 정보」(`title2Ref`, `section2Ref`, 상단 `pt-8`)

| 순서 | UI | 형식 |
|------|-----|------|
| S2-0 | (조건부) `profileExistsOnServer` | 제목 줄 우측 **「수정」/「수정 완료」** 토글 → `licenseFieldsLocked` 반전 → 아래 필드 `readOnly`/`disabled` |
| S2-1 | 면허 종류 | `<select>` — **`1종 대형`**, **`1종 보통`** 하드코딩 옵션 2개 |
| S2-2 | 면허 번호 | text |
| S2-3 | 발급 일자 | `type="date"` |
| S2-4 | 유효 기간 | `type="date"` |
| S2-5 | 암호일련번호 | text, **`md:col-span-2`**, 진위 안내 문단 포함 |

신규(`exists=false`)일 때 초기값 예: `licenseIssueDt` `'2023-01-01'`, `licenseExpiryDt` `'2033-01-01'` (코드 초기 state).

#### 블록 S3 — 「운송종사자 자격 정보」(`title3Ref`, `section3Ref`, 카드 패널 내부)

| 순서 | UI | 형식 |
|------|-----|------|
| S3-0 | (조건부) | 제목 줄 **자격 「수정」/「수정 완료」** → `qualFieldsLocked` |
| S3-1 | 버스운전 자격번호 | text, 잠금 시 readOnly |
| S3-2 | 검증 상태 문구 | `profileExistsOnServer`일 때만: `VERIFIED` / `SKIPPED` / 기타(UNVERIFIED)별 메시지+아이콘 |
| S3-3 | 안내 문구 | 서버에 자격증이 있어도 **점선 영역 클릭으로 언제든 다른 파일 선택 가능**; **`등록`/`수정` 제출 시 반영**. 자격번호 입력만 「수정」 잠금과 연동(`qualFieldsLocked`). |
| S3-4 | 업로드 영역 | dashed 박스, 클릭 시 숨김 `<input type="file" accept="ACCEPT_DOC">`(제출 중 `isSubmitting`일 때만 비활성). 허용 MIME: JPEG·PNG·WEBP·GIF·PDF, **10MB 초과 불가**(alert). 상태: 새 파일 미리보기 / 서버 등록 파일 썸네일·파일명·용량·PDF 아이콘 / 빈 상태 안내 문자열 |
| S3-5 | 「문서 보기」 | `hasQualCertFileOnServer`일 때만 활성 → `CommonView` |

#### 블록 BIO — 「자기소개 및 강점」(섹션3 아래 `pt-8`)

| 순서 | UI | 형식 |
|------|-----|------|
| BIO-1 | 제목 `<h2>` | 고정 문자열 |
| BIO-2 | `<textarea name="bioText">` | `h-40`, `resize-none`, 장문 placeholder |

#### 블록 F — 하단 액션 (`<footer>`)

| 순서 | 버튼 | 동작 |
|------|------|------|
| F-1 | **취소** | `onClick={onBack}` **만** 호출 (**`close` 미사용**). `App.jsx` 에서 `onBack` 미넘길 경우 **취소 무동작** 가능 — **`close={…} onBack={…}` 동일 핸들러 권장** |
| F-2 | **등록** / **수정** | `handleSubmit`; 로딩 시 라벨 **「처리 중…」**, `disabled={isSubmitting}` |

---

### 3.6 성공 모달 (저장 후)

- 전체 어두운 배경 클릭 또는 **「확인」** → 닫힘.
- 메시지: 신규 **「버스 기사 기본 정보가 등록되었습니다.」** / 기존 **「…수정 되었습니다.」**

---

### 3.7 스크롤 잠금(문서 레벨)

- 마운트 시 `html`/`body`/`#root` 에 `overflow: hidden`, 높이 100%, `overscrollBehavior: none`; 언마운트 시 원복.
- **의도:** 배경 페이지 스크롤 차단, **우측 `scrollRef`만** 세로 스크롤.

---

### 3.8 모바일 vs 데스크톱 배치 요약

| 폭 | 네비 | 폼 폭 |
|----|------|-------|
| `< lg` | 상단 가로 칩 | 전폭 (`max-w-3xl` 제한 내) |
| `≥ lg` | 좌 세로 고정 패널 + 주의사항 | 우단 스파이 컬럼 |

### 3.9 기존 절 번호와의 연계

- **§3** = 모달 **화면·입력 순서**. **§4~§7**은 동일 항목의 **DB·API·검증** 관점 서술.
- **헤더 닫기:** `close || onBack`(§3.3). **푸터 취소:** **§3.5의 `onBack`만** 호출 코드 — 부모에서 `onBack={close}` 까지 맞춰 줄 것(`App.jsx`는 현재 누락 가능성 있음).

---

## 4. ① 기본 인적사항 (섹션 1)

### 4.1 프로필 사진 카드

- **이 모달에서는 프로필 사진 업로드 UI 없음.** `<img>` 는 **서버에 등록된 사진 스트림**만 표시.
- 마운트 후 `GET /api/driver/profile-setup` 응답에서 `hasProfilePhoto`, `profilePhotoFileId`(또는 `profilePhotoId`)를 받음.
  - **`data.exists` 와 무관하게** (`exists === false` 여도) `fileId`가 있으면 `GET /api/driver/profile-photo?custId=&fileId=` 로 blob 받아 Object URL 렌더.
- 상단 호칭: 응답 `userName`이 있으면 `「{userName} 기사님」`, 없으면 폼 `name` 또는 기본 「기사님」.

**저장 테이블:** 프로필 사진은 **`TB_USER.PROFILE_FILE_ID` → `TB_FILE_MASTER`** (다른 플로우에서 등록). 본 **`POST profile-setup`** 은 **`TB_USER`**의 프로필 파일을 바꾸지 않음.

### 4.2 성명 (`name`)

- 로컬 state `formData.name`. 초기값: `currentUser.userNm` 등.
- **주의:** 현재 **`POST profile-setup`** 은 성명으로 `TB_USER.USER_NM`을 갱신하지 않으며, 진위 검증 시 **`driverName`** 으로만 사용됩니다. 화면 표시용·검증용 입력으로 이해.

### 4.3 주민등록번호 (`residentNoDisplay`)

- **본 「기사정보등록」모달에서는 주민번호를 입력·수정하지 않는다.** 저장 원본은 **`TB_USER.RESIDENT_NO_ENC`** 이며, 회원가입·본인인증 등 **다른 플로우**에서 등록·갱신한다.
- **`GET /api/driver/profile-setup`:** 해당 회원 행의 `RESIDENT_NO_ENC` 를 복호화한 뒤, 화면에 **`residentNoDisplay`** 문자열로 내린다. 표시 형식은 **전체 미노출**을 원칙으로 **`YYMMDD-C●●●●●●`** (생년월일 앞자리 + 뒷번호 첫 자리 + 마스크 6). 레거시 **`XXXXXX-Y`** 저장분도 같은 패턴으로 표시한다. 값이 없거나 파싱 불가면 빈 문자열·placeholder.
- **`POST /api/driver/profile-setup`:** 요청 본문에 **`rrn` 필드를 받지 않으며**, `RESIDENT_NO_ENC` 를 **갱신하지 않는다.** 면허·자격 진위에 필요한 주민 형식 문자열은 서버가 **`TB_USER.RESIDENT_NO_ENC`** 를 복호·파싱해 내부적으로 조합한다. 자격번호를 변경하는 등 **완전한 뒷 7자리가 필요한 분기**에서는 DB 에 **13자리(앞6+뒤7)** 형태가 저장되어 있어야 하며, 없으면 **400** 과 안내 메시지로 차단한다. 자격번호 미변경 시에는 기존처럼 `TB_DRIVER_DETAIL` 의 `BIRTH_YMD`·`SEX` 로 합성 검증 데이터를 쓸 수 있다.
- **`TB_DRIVER_DETAIL.BIRTH_YMD`·`SEX`:** `POST` 시 서버가 위 규칙으로 계산·UPSERT(기존과 동일 목적).
- 진위 검증 생년월일 계산은 `driverVerification.js` 의 `birthYmdFromRrn` 등(주민 규칙 단순화).

### 4.4 휴대전화

- **읽기 전용.** `formatHpKrDigits`로 `000-0000-0000` 표시.
- 값 출처: `GET profile-setup`의 `phoneNo`(내려준 복호화 HP) 또는 `currentUser.hpNo`.
- 번호 수정은 본 모달 책임이 아님(회원 정보 메뉴 안내 문구 존재).

### 4.5 주소 (카카오 우편번호 스크립트)

| UI | state / POST 필드 | DB (`TB_DRIVER_DETAIL`) |
|----|-------------------|-------------------------|
| 주소 구분 | `addrType`, `OTHER` 등 | `ADDR_TYPE` |
| 주소구분명칭 | `addrOtherLabel` ↔ `addrName`(POST) — **OTHER 일 때만** 입력·필수, **최대 10글자**(그레프렘 코드포인트 기준 클라 클램프) | `ADDR_NAME`(OTHER만, 그 외 `null`/정리 로직 따름) |
| 주소 검색 | Daum Postcode 팝업 | — |
| 우편번호 | `zipcode` | `ZIPCODE` |
| 기본 주소 | `streetAddress` ↔ `address` | `ADDRESS` |
| 상세 주소 | `detailAddress` — 최대 **100글자**(그레프렘) | `DETAIL_ADDRESS` |

서버: `OTHER` 일 때 명칭 없으면 **400**.

---

## 5. ② 운전면허 정보 (섹션 2)

폼 필드 → `POST` 본문: `licenseType`, `licenseNo`, `licenseSerialNo`(선택·undefined 허용), `licenseIssueDt`, `licenseExpiryDt`(날짜 문자열).

- **UI 선택지:** 현재 「1종 대형」「1종 보통」 하드코딩 `<select>`.

### 수정 잠금

- 서버에 상세 레코드 존재 시(`exists === true`) 최초 진입 후 `licenseFieldsLocked === true`(필드 `readOnly`/`disabled`).
- 섹션 제목 옆 「수정」 토글로 잠금 해제 후 필드 수정 가능 → 다시 「수정 완료」로 잠금.

### DB

- 테이블: **`TB_DRIVER_DETAIL`**. UPSERT 키는 구현상 **`CUST_ID`**(문자열 정규화)를 사용한다. 설계 문서에 `USER_ID` PK만 적혀 있어도, **현행 `server.js`** 는 커넥션 DB의 실제 컬럼명을 `INFORMATION_SCHEMA`로 읽어 동적 SQL을 생성한다.

**컬럼 미존재 시(구 스키마):**

- 서버가 존재하는 컬럼만 넣어 **UPSERT 분기**(주소만 / 면허까지 / 자격 문자열까지 등). **애플리케이션에서 `ALTER TABLE`로 컬럼을 추가하지 않는다.**
- 면허·자격 **문자열 컬럼을 DB에 두려면** 별도 승인 DDL(예: 저장소의 참고 SQL)을 운영에서만 적용한다.

---

## 6. ③ 운송종사자 자격 (섹션 3)

### 입력

- **`qualCertNo`** → 면허처럼 존재 시 잠금; 「수정」으로 해제 후 편집.
- **`qualCert`(로컬):** 새로 고른 파일의 Data URL. 허용 MIME: JPEG, PNG, WEBP, GIF, PDF · **최대 10MB**.

### 업로드·파일 교체

- **신규**(`exists === false`): 항상 파일 선택 가능.
- **기존**(`exists === true`): **자격증 파일 교체는 `qualFieldsLocked`와 무관**하게 항상 선택 가능. 다만 **화면에 반영·저장**하려면 하단 **`등록`/`수정`** 으로 `POST` 해야 한다.
- **자격번호 필드**만 「수정」/「수정 완료」(`qualFieldsLocked`)으로 잠금·편집이 제어된다.

### 검증 배지 (`qualCertVerifyStatus`)

- `VERIFIED`, `SKIPPED`, 기타(UNVERIFIED). **표시 목적**(업로드 차단 안 함).

### 서버 저장 — 자격증 **파일**(사본 이미지/PDF)

본 플로우에서 자격증 사본은 **`TB_FILE_MASTER`를 거치지 않는다.** 바이너리는 **전용 GCS 버킷**에 두고, 메타·소유 관계는 **`TB_DRIVER_DOCS`** 에만 기록한다.

| 항목 | 처리 |
|------|------|
| **테이블** | **`TB_DRIVER_DOCS`** |
| **`CUST_ID`** | 요청 회원의 `TB_USER.CUST_ID` |
| **`DOC_TYPE`** | `QUALIFICATION` (코드 상수 `QUALIFICATION_DOC_TYPE`) |
| **`DOC_TYPE_SEQ`** | 해당 `CUST_ID`·`DOC_TYPE` 조합에서 **`MAX(DOC_TYPE_SEQ)+1`** 로 채번 |
| **`GCS_BUCKET_NM`** | `bustaams-secure-data` (`DRIVER_QUAL_GCS_BUCKET`) |
| **`GCS_PATH`** | **`QUALIFICATION/{DOC_TYPE_SEQ 를 20자리 0패딩}.{확장자}`** (예 `QUALIFICATION/00000000000000000001.pdf`). Cust ID 경로 접두 없음. |
| **`ORG_FILE_NM`**, **`ORG_FILE_EXT`**, **`FILE_SIZE`** | 업로드 파일 메타. 스키마에 **`FILE_SIZE` 컬럼이 없으면** 서버는 해당 컬럼 없이 INSERT/조회 분기. |
| **`REG_DT`** | 등록 시각 |

**동작 요약:** `POST /api/driver/profile-setup` 요청 본문에 **`qualCertBase64`**(`data:…;base64,…`) 가 있을 때만 위 INSERT + GCS `save` 가 트랜잭션 안에서 실행된다. 새 파일을 올릴 때마다 **새 `DOC_TYPE_SEQ` 행이 추가**된다(동일 행 덮어쓰기가 아님). 클라이언트에 내려주는 **`qualCertFileId`** 는 보통 **가장 최근 순번**을 20자리 패딩한 값.

### 서버 저장 — 자격 **번호·검증 상태**(텍스트)

- **`TB_DRIVER_DETAIL`** 에 **`QUAL_CERT_NO`**, **`QUAL_CERT_VERIFY_STATUS`**, (있으면) **`QUAL_CERT_VERIFY_DT`** 등이 정의되어 있을 때만 같은 `POST` 의 UPSERT 에 포함된다.
- **`INFORMATION_SCHEMA`** 로 실제 컬럼 존재·**표기(대소문자)** 를 읽어 동적으로 SELECT/UPSERT 하며, **런타임에 테이블을 ALTER 하지 않는다.** 해당 컬럼이 없으면 자격번호·상태는 DB에 남지 않을 수 있으나, 자격증 **파일**은 위 `TB_DRIVER_DOCS` 경로로만 저장된다.

### 클라 표시 로직 요약

- 서버 파일 있음(`hasQualCertFileOnServer`): `qualCertOrgFileNm`·`qualCertFileExt`·크기 라벨, 이미지면 `GET /api/driver/qual-cert/file`로 썸네일 blob; PDF면 아이콘·확장자.
- 새 파일 선택 후: 업로드 박스에 로컬 미리보기; 제출까지는 서버 반영 안 됨.
- 「문서 보기」→ `CommonView` (`custId`, `fileId`, 아래 경로 오버라이드).

---

## 7. 자기소개 (`bioText`)

- `POST` 필드 **`bioText`** → `TB_DRIVER_DETAIL.SELF_INTRO`.
- 존재하는 상세 레코드와 무관하게 일반 입력(잠금 UI 없음 — 면허 블록과 별개).

---

## 8. 제출 (`POST /api/driver/profile-setup`)

### 8.1 클라 검증 순서 (`handleSubmit`)

1. `currentUser.userId` 필수 → 없으면 alert.
2. 성명 비었으면 alert.
3. `addrType` ∈ `HOME|OFFICE|OTHER`.
4. `OTHER` 일 때 명칭 비었으면 alert.
5. 우편번호·기본 주소(도로명) 비었으면 alert.
6. 회원등급(`FEE_POLICY`) 선택 필수.

### 8.2 요청 본문 (JSON)

| 필드 | 필수 | 설명 |
|------|------|------|
| `userId` | 예 | 로그인 ID (`TB_USER.USER_ID`). |
| `driverName` | 진위 시 | 성명(트림). `KOROAD` 또는 `TS` 켠 경우 필수 검사. |
| `addrType`, `zipcode`, `address`, `detailAddress` | 예 | 서버 검증 포함. |
| `addrName` | OTHER 시 | 명칭(서버 최대 10글자). |
| `licenseType`, `licenseNo`, `licenseIssueDt`, `licenseExpiryDt` | 로직별 | 진위 또는 DB 저장용. |
| `licenseSerialNo` | 선택 | undefined 가능. |
| `qualCertNo` | 진위 시 | TS 켜면 등. |
| `bioText` | 선택 | 문자열. |
| `feePolicy` | 예 | 회원등급 `TB_COMMON_CODE` `FEE_POLICY.DTL_CD`. |
| `qualCertBase64` | 선택 | `data:mime;base64,...` 만 서버 업로드 분기 처리. |

### 8.3 서버 처리 순서 요약 (`server.js`)

1. `userId`로 `TB_USER` 조회 → `CUST_ID`, **`RESIDENT_NO_ENC`** 포함.
2. 주소 형식 검증.
3. **`driverDetailColumnMap`**: `INFORMATION_SCHEMA`로 `TB_DRIVER_DETAIL` 실제 컬럼명·존재 여부 로드(대소문자 포함 식별자 이스케이프). **`TB_DRIVER_DOCS.FILE_SIZE`** 유무는 `tableColumnExists` 로 분기.
4. **`RESIDENT_NO_ENC` 복호·파싱**으로 진위용 주민 문자열 결정(본 API에서는 **`RESIDENT_NO_ENC` 갱신 안 함**).
5. `runDriverVerificationsForProfileSetup` (§9) 실패 시 **400** 후 종료(DB 트랜잭션 전).
6. 트랜잭션: **`executeDriverDetailProfileUpsert`** 로 **`TB_DRIVER_DETAIL`** UPSERT — 존재하는 컬럼 조합에 따라 주소만 / 면허까지 / `QUAL_CERT_*` 포함 등으로 분기.
7. `qualCertBase64` 있으면 GCS 업로드 + **`TB_DRIVER_DOCS`** INSERT(`FILE_SIZE` 컬럼 있을 때만 해당 값 포함).
8. **200** JSON: `message`, `qualCertVerifyStatus`, `qualCertFileId`, `custId`, (자격 새/기존 시) `hasQualCertFile`, `qualCertOrgFileNm`, `qualCertFileExt`, `qualCertFileSize`.

실패 시 `500` / 검증 오류 시 `400`(본문 `{ error, detail? }`).

제출 후 클라는 잠금 갱신(신규 등록 분기에서는 `exists` 상태를 true처럼 취급·면허·자격 잠금), 자격증 로컬 파일 초기화, 성공 모달 표시.

---

## 9. 진위 검증 (`driverVerification.js`)

### 플래그

| ENV | 역할 |
|-----|------|
| `KOROAD_LICENSE_VERIFY_ENABLED` | 도로교통공단 연계(또는 대체 JSON API) **`KOROAD_LICENSE_VERIFY_URL`** 등 |
| `TS_QUAL_VERIFY_ENABLED` | 운수종사 자격 진위 **`TS_QUAL_VERIFY_URL`**, **`PUBLIC_DATA_SERVICE_KEY`** (또는 `DATA_GO_KR_SERVICE_KEY`) |
| 선택 | `TS_QUAL_PARAM_NAME/BIRTH/CERT`, `KOROAD_LICENSE_HTTP_METHOD`, `KOROAD_LICENSE_API_KEY` 등 |

두 플래그 모두 기본 미설정이면 외부 API 호출 **skip**. 자격 상태는 **`TS_QUAL_VERIFY_ENABLED`** 가 꺼지면 서버 로직상 **`SKIPPED`**, 진위 성공 시 **`VERIFIED`** 등으로 `QUAL_CERT_VERIFY_STATUS` 저장.

**주민번호 진위 입력 출처:** 클라이언트 **`rrn` 미전달**. 서버가 **`TB_USER.RESIDENT_NO_ENC`** 복호·파싱한 값(또는 자격번호 미변경 분기에서 **`TB_DRIVER_DETAIL` 생년월일·성별** 합성)을 `runDriverVerificationsForProfileSetup` 에 넘긴다.

### 생략 조건

- **면허:** DB 기존 행과 종류·번호·일련번호·발급·만료가 동일하면 KOROAD 검증 스킵.
- **자격:** DB에 저장된 자격번호 컬럼(예: `QUAL_CERT_NO`)이 있고, 그 값과 현재 번호가 동일(Norm: 하이픈·공백 무시)이면 TS 스킵(기존 검증 상태 유지). **컬럼이 없으면** 동일 비교가 불가하여 번호 변경 분기로 취급될 수 있다.

### 자격 상태 정리 (`POST` 분기 요약)

- TS 끔 → **`SKIPPED`**
- TS 켜짐·번호 변경 없음(스킵) → 기존 DB 상태 또는 **`VERIFIED`**
- TS 켜짐·호출 성공 → **`VERIFIED`**, 검증 일시 저장

---

## 10. REST API 상세

### `GET /api/driver/profile-setup?custId=` (필수)

- 회원 행 없음 → **404**.
- 처리: 복호화 `USER_NM` → `userName`, `HP_NO` → `phoneNo`; `PROFILE_FILE_ID` 패딩 정규화 → `profilePhotoFileId`; `TB_DRIVER_DETAIL` 조회는 **스키마에 존재하는 컬럼만** 동적 SELECT; `TB_DRIVER_DOCS` 최신 `QUALIFICATION`(조회 시 `FILE_SIZE` 컬럼 유무 분기).

**공통 포함 필드(요지):**

`exists`, `userName`, `phoneNo`, **`residentNoDisplay`** (`TB_USER.RESIDENT_NO_ENC` 기준 마스킹 표시 문자열),  
`hasProfilePhoto`, `profilePhotoFileId`, `profilePhotoId`,  
`hasQualCertFile`, `qualCertFileId`(20자 패딩), `qualCertOrgFileNm`, `qualCertFileExt`, `qualCertFileSize`.

`exists === false` 일 때 나머지 주소 필드 빈 문자열 등 기본.

`exists === true` 추가: 면허 필드 일체, `qualCertNo`, `bioText`, `qualCertVerifyStatus`, `qualCertVerifyDt`(ISO 문자열 또는 null).

### `POST /api/driver/profile-setup`

- 상세 §8.

### `GET /api/driver/profile-photo?custId=&fileId=` (필수)

- `TB_USER`: 해당 `custId`(후보 IN) 행에서 `PROFILE_FILE_ID`가 요청 `fileId` 후보 집합과 일치해야 **200** 스트림.
- **`TB_FILE_MASTER`:** 같은 `FILE_ID` 후보로 메타 후 GCS 존재 확인까지 수행(Content-Type 확장자 매핑).
- 불일치 **403**, 마스터 없음 **404**(JSON 에러 가능).

### 자격증 (소유 검증 후)

| 메서드 | 경로 | 용도 |
|--------|------|------|
| GET | `/api/driver/qual-cert/meta?custId=&fileId=` | CommonView 등 메타 |
| GET | `/api/driver/qual-cert/file?custId=&fileId=` | inline 스트림·썸네일 |
| GET | `/api/driver/qual-cert/download?custId=&fileId=` | `attachment` 다운로드 |

`canAccessQualCertFile`: `TB_DRIVER_DOCS`에서 `DOC_TYPE=QUALIFICATION` 이고 `LPAD(DOC_TYPE_SEQ,20,'0')=fileId` 인지 확인.

---

## 11. CommonView 연동 (자격증)

```jsx
<CommonView
  close={() => setShowQualCertViewer(false)}
  fileId={qualCertFileId}
  custId={String(currentUser.custId)}
  docTitle="버스운전 자격증 사본"
  metaPath="/api/driver/qual-cert/meta"
  streamPath="/api/driver/qual-cert/file"
  downloadPath="/api/driver/qual-cert/download"
/>
```

- 버튼 활성 조건:** `hasQualCertFileOnServer`** (등록된 `fileId` 있음).

---

## 12. 데이터 모델 요약 (`BusTaams_Project` 기준 + 현행 구현)

| 테이블 | 본 플로우 역할 |
|--------|----------------|
| **`TB_USER`** | `USER_ID`(POST 키), `CUST_ID`(소유 검증 키), `USER_NM`/`HP_NO` 표시 복호, `PROFILE_FILE_ID` 프로필 사진, **`RESIDENT_NO_ENC`** — **본 플로우에서는 조회·진위용으로만 사용**(GET 마스킹 표시, POST 에서 **컬럼 갱신 없음**). 주민 등록·변경은 다른 화면/API 책임. |
| **`TB_DRIVER_DETAIL`** | **실제 스키마에 있는 컬럼만** UPSERT. 일반적으로 주소(`ADDR_*`, `ZIPCODE`, `ADDRESS`, `DETAIL_ADDRESS`), `BIRTH_YMD`(YYMMDD), `SEX`, `SELF_INTRO`, `FEE_POLICY`, (있으면) 면허·`QUAL_CERT_*`. PK/식별 컬럼은 DB 정의에 맞춰 **`CUST_ID` 등 실제 컬럼명**으로 기록. |
| **`TB_DRIVER_DOCS`** | 자격증 **사본 파일**: `DOC_TYPE = QUALIFICATION`, 시퀀스 증가 + GCS 객체 경로·원본 메타.**`TB_FILE_MASTER`와 무연결.** |

---

## 13. 스키마 정책 (런타임 DDL 금지)

- **`bustaams` 스키마에 대한 `CREATE`/`ALTER`/`DROP`은 애플리케이션 코드에서 수행하지 않는다.** 필요한 컬럼은 `INFORMATION_SCHEMA`로 감지 후 쿼리·UPSERT에서 생략 또는 포함만 한다.
- 스키마 변경이 필요하면 **DBA·승인된 DDL** 로만 반영한다. CI에서는 `scripts/ci/check-bustaams-no-ddl.sh` 로 앱 소스 내 DDL 문자열을 차단한다.
- 참고용 SQL 파일은 저장소에 있을 수 있으나 **런타임 자동 적용 대상이 아니다.**

---

## 14. 레거시·별도 참고

- **`POST /api/driver/profile`** (`server.js` 앞부분): 프로필/면험이미지 다른 페이로드 포맷. **본 「기사정보등록」모달에서는 사용하지 않음.** 새 구현 시 혼선 방지 차원에서 구분 유지 권장.

---

## 15. 신규 화면 재구현 체크리스트

1. 세션 **`custId` + `userId`** 확보 후 모달 렌더.
2. 마운트 시 **`GET profile-setup`** → `exists`/자격 프로필/사진 `fileId` 반영 · **프로필 사진은 `exists` 무시** 재조회.
3. **§3** 모달 구성 입력 순서·날짜·그리드·자격 업로드·성공 보조 모달까지 구현 JSX와 일치.
4. 카카오 우편번호 로더·폼 상태·글자 길이 제한(OTHER 10자, 상세 100글자 등) 재현.
5. 면허·자격번호 **「수정」 잠금** UX(자격증 **파일**은 별도로 항상 교체 선택 가능).
6. 자격증 파일 MIME·10MB 검증 후 Data URL 또는 base64 포함 **POST**(자격증 생략 시 필드 미전송 허용).
7. 진위 활성 서버에서 자격번호 변경 시 **`TB_USER`에 완전한 주민번호(13자리 저장 형식)** 가 없으면 **400** — 회원가입 등에서 먼저 등록했는지 확인.
8. CommonView 세 경로·`custId`/`fileId` 쿼리 일관.
9. **헤더 `close`/푸터 `onBack`** 모두 부모에 연결되어 취소·닫기가 동작하는지 확인.

---

*문서 작성 기준: `DriverProfileSetup.jsx` UI 트리·`server.js`(profile-setup, 동적 `TB_DRIVER_DETAIL`/`TB_DRIVER_DOCS`)·`driverVerification.js` 일치 반영.*

*모달 레이아웃 전개: 2026-05-06 — §3 화면 구성 절 신설. 자격증 업로드 테이블 처리·DDL 정책: 2026-05-15 갱신.*
