# 카드 및 월회비 (기사 · `BillingSubscription`)

기사(버스 운전기사) 로그인 후 **메인 대시보드**에서 **결제 카드·월 회비 안내**를 보는 모달 화면과, 이를 위한 **REST API 스텁**에 대한 정리 문서입니다.  
화면·DOM·JSON이 모두 같은 문자열 식별자 **`BillingSubscription`** 을 공유합니다.

**물리 테이블 정의**(`TB_PAYMENT_CARD`, `TB_DRIVER_PAYMENT_HIST` 등 컬럼·타입·PK·FK)는 본 문서에 **중복하지 않는다.** 정본은 [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md)를 따르고, DDL 원문은 각각 `busTaams_server/sql/tb_payment_card.sql`, `busTaams_server/sql/tb_driver_payment_hist.sql` 을 참고한다.

---

## 1. 공통 식별자

| 구분 | 값 |
|------|-----|
| **화면 코드명 / 컴포넌트명** | `BillingSubscription` |
| **DOM `id` · `data-screen-id`** | `BillingSubscription` |
| **프론트 상수** | `BILLING_SUBSCRIPTION_SCREEN_ID === 'BillingSubscription'` (`BillingSubscription.jsx` export) |
| **REST 응답 최상위 키** | `BillingSubscription` (객체) |
| **객체 내부 `screenId`** | `BillingSubscription` (서버는 `busTaams_server/lib/billingSubscriptionId.js`의 `BILLING_SUBSCRIPTION_ID` 사용) |

---

## 2. 참고 HTML (레이아웃 원본)

| 항목 | 경로 |
|------|------|
| 정적 마크업 | [`busTaams_web/downloads/bustaams_web/카드및월회비_기사/BillingSubscription.html`](./busTaams_web/downloads/bustaams_web/카드및월회비_기사/BillingSubscription.html) |

React 모달 본문은 레퍼런스 HTML을 바탕으로 하되, 아래 **§2-1 대시보드형 2열 구조**를 반영해 확장한다.

기존 단일 흐름 요약(참고):

- 제목 **`카드 및 월회비`** + 회색 안내 문단  
- **월 회비** 영역: 배경 `#f0fdfa`, 테두리 `#99f6e4`, 소제목 색 `#0f766e`, 금액 `#115e59`, `/월` 표기  

### 2-1. 모달 화면 구조 (좌 · 우)

**전체:** 모달 본문을 **좌측 패널** / **우측 패널** 의 2열 그리드(데스크톱 기준 `min-width` 에서 가로 배치, 좁은 폭에서는 세로 스택)로 구성한다.

| 영역 | 역할 | 시각(가이드) |
|------|------|----------------|
| **좌측** | **멤버십(기사 닉네임·등급·월회비 금액)** + 정책 안내(빨간 작은 글씨) | 흰 카드 박스 |
| **우측** | **결제 수단**(`TB_PAYMENT_CARD` 기반 다크 카드 UI) | 연한 회색 배경 |
| **하단(전폭)** | **최근 결제 내역** 표 — 본문만 `max-height` + 세로 스크롤 | §3 참고 |

(구버전 레이아웃 설명 중 좌측에 이력이 있다고 한 문구는 폐기한다.)

우측 섹션 제목: **「결제 수단」**.

---

## 3. 좌측 — 카드 결제 이력 (조회 UI)

`TB_DRIVER_PAYMENT_HIST` 를 조회해 **기사·귀속 월 단위**로 리스트 또는 표 형태로 보여준다. 아래는 **화면에 꺼내볼 출력 항목**이다 (DB 컬럼명과 대응).

| 화면 라벨(예) | 출처(논리 필드) | 표시 규칙 |
|---------------|-----------------|-----------|
| **귀속·청구 월** | `BILLING_YYYYMM` | `YYYYMM` → `YYYY년 M월` 또는 `YYYY-MM` |
| **청구 금액** | `PAY_AMT` | 원 단위, 천 단위 구분 |
| **상태** | `PAY_STAT` | `PENDING`·`SUCCESS`·`FAILED`·`REFUNDED`·`CANCELLED` 를 사용자용 한글 라벨로 매핑 (예: 대기·완료·실패·환불·취소) |
| **결제 요청 일시** | `PAY_REQ_DT` | 로컬 일시 포맷 |
| **승인 완료 일시** | `PAY_COMPLETED_DT` | 성공 건 위주 표시, 없으면 `-` |
| **당시 카드** | `CARD_NICKNAME_SNAPSHOT`, `CARD_LAST_FOUR_SNAPSHOT` | 별칭 + 마스킹 끝 4자리; 둘 다 없으면 「카드 정보 없음」 등 |
| (선택) **실패 사유** | `FAIL_MSG` | `FAILED` 등일 때만 접기/툴팁 등으로 노출 |
| (선택) **PG 참고** | `PG_TXN_ID` | 고객 CS·내부용이면 숨기거나 상세에만 |

**정렬:** 기본 **최신순** (`PAY_REQ_DT` 또는 `PAY_HIST_SEQ` 내림차순).  
**빈 목록:** 「아직 결제 이력이 없습니다.」 등 안내.

**모달 내 표시:** 상단 그리드 **아래** 전폭 섹션에 두며, **표 영역만** 세로 스크롤(`overflow-y: auto`, 예: `max-h-72`) — 모달 전체 스크롤과 분리해 목록만 위·아래로 탐색.

---

## 4. 우측 — 결제 수단

`TB_PAYMENT_CARD` 를 기사(동일 `CUST_ID`) 기준으로 조회해 목록으로 표시한다.

### 4-1. 정렬

1. **첫 줄:** `IS_PRIMARY = 'Y'` 인 카드 **한 건** (메인 카드).  
2. **그 아래:** 메인이 아닌 카드는 **`REG_DT` 오름차순**(등록이 빠른 순) 또는 동일 의미의 `CARD_SEQ` 오름차순으로 표시.

### 4-2. 구분 명칭 (시각)

메인 카드는 **다크 틸 카드 그래픽**으로, 부 카드는 **「다른 등록 카드」** 목록으로 구분한다. (과거 `check_circle` 혜택 3줄 리스트는 **사용하지 않음**.)

### 4-3. 행 내용 (표시 가능 범위)

- 별칭: `CARD_NICKNAME` (없으면 기본 문구)  
- 마지막 4자리: 서버에서 마스킹해 내려주는 값(평문 `CARD_NO_ENC` 는 클라이언트에 노출하지 않음)  
- 유효기간 표시: `EXP_MONTH` / `EXP_YEAR` (예: `MM/YY`)  
- (선택) 해당 카드의 자동결제 구간·결제일 요약: `AUTO_PAY_START_DT`, `AUTO_PAY_END_DT`, `AUTO_PAY_DAY` 는 월회비 정책에 맞춰 한 줄 요약 또는 툴팁

### 4-4. 메인 카드 변경

- 초록 섹션 안에 **`메인 카드 변경`** 버튼을 둔다.  
- 동작(안): 클릭 시 **다른 등록 카드 중 하나를 선택**하는 흐름(작은 모달·라디오 목록 등) → 확인 시 서버에 메인 지정 요청(`IS_PRIMARY` 일괄 정합: 선택 카드만 `Y`, 나머지 `N`).  
- 메인만 한 장인 규칙은 DB·API에서 보장한다.

**빈 목록:** 「등록된 카드가 없습니다.」 + 카드 등록 유도(별도 플로우가 있으면 링크·버튼).

---

## 5. 프론트엔드

### 5-1. 컴포넌트

| 항목 | 내용 |
|------|------|
| **파일** | [`busTaams_web/src/components/BillingSubscription/BillingSubscription.jsx`](./busTaams_web/src/components/BillingSubscription/BillingSubscription.jsx) |
| **export** | `export function BillingSubscription` · `export default BillingSubscription` · `export const BILLING_SUBSCRIPTION_SCREEN_ID` |
| **API 베이스** | `VITE_API_BASE_URL`, 없으면 `http://127.0.0.1:8080`, 후행 `/` 제거 |

### 5-2. Props

| Prop | 설명 |
|------|------|
| `open` | 모달 표시 여부 |
| `onClose` | 닫기 콜백 (`Esc`, 배경, 닫기 버튼) |
| `driverId` | 기사 세션 식별 — `String(driverId).trim()` 후 비어 있으면 **API 미호출**, 안내 배너만 표시 가능 |

### 5-3. 동작 (요약)

- `open && driverId` 일 때 **`GET /api/billing-subscription?driverId=...`** 호출.  
- 응답에 **`BillingSubscription` 객체 필수**. 없거나 비객체면 에러 처리.  
- 모달 오픈 시 **`body` 스크롤 잠금**, 언마운트 시 복구.  
- 패널 루트: `role="dialog"`, `aria-modal="true"`, 제목은 `aria-labelledby="BillingSubscription-title"` (`h1`).  
- **좌측(멤버십):** 제목은 API 의 **`membershipHeading`**(서버 조합 문자열)을 그대로 표시한다.  
  - 서버는 `TB_USER.USER_ID` 로 조회한 행의 **`USER_NM`** 을 Unicode 기준 **최대 10자**로 자른 뒤, **` : 기사님 (`** + 공통코드 **`CD_NM_KO`** + **`)`** 형식으로 만든다.  
  - `TB_COMMON_CODE`( `GRP_CD='FEE_POLICY'`, `DTL_CD=TB_DRIVER_DETAIL.FEE_POLICY` ) 에서 **`CD_NM_KO`** 가 없으면 괄호 안은 **`등급없음`**.  
  - 월회비 금액: `monthlyFeeKrw` 가 있으면 **`₩` + 천 단위 쉼표(`toLocaleString('ko-KR')`) + `/월`**. 없으면 문구 **`월회비 대상 회원 아님`**.  
  - 말일·익월 반영 등 정책 안내는 **작은 빨간 글씨** 블록으로 유지.  
  - **미포함:** `check_circle` 혜택 3줄, 「BusTaams 기사 회원을 위한 월 구독입니다.」 단일 안내, PCI-DSS 한 줄·**「보안 정책 확인」** 버튼 줄 전체.  
- **우측:** 결제 수단(다크 카드·메인 변경·새 카드 등록 자리).  
- **하단:** 최근 결제 내역 표(§3) — **표 바디 영역만** 세로 스크롤.

### 5-4. 진입 (기사 대시보드)

| 항목 | 내용 |
|------|------|
| **파일** | [`busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx`](./busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx) |
| **바로가기** | 「기사 정보 관리」**오른쪽** 인접 버튼 **「카드 및 월회비」** (견적 2열 트윈과 동일한 `col-span-2` flex 패턴의 첫 셀) |
| **상태** | `showBillingSubscription` — `BillingSubscription`에 `driverId={currentUser?.custId \|\| currentUser?.userId}` 전달 |

`App.jsx` 수정 없음 — 모달은 `DriverDashboard` 내부에서만 연다.

---

## 6. REST API — `GET /api/billing-subscription`

### 6-1. 요청

| 항목 | 값 |
|------|-----|
| 메서드·경로 | `GET /api/billing-subscription` |
| 쿼리 | **`driverId`** (필수, trim 문자열) |

**400:** `{ "error": "driverId가 필요합니다." }`  

**500:** `{ "error": "<message>" }`

### 6-2. 성공 응답 `200`

최상위에 반드시 **`BillingSubscription`** 키.

| 필드 | 타입 | 설명 |
|------|------|------|
| `screenId` | string | 고정값 `BillingSubscription` |
| `driverId` | string | 요청에 넘긴 기사 식별자 |
| `introText` | string | 제목 아래 안내 문구 |
| **`membershipHeading`** | string \| null | `{USER_NM 10자}: 기사님 ({CD_NM_KO 또는 등급없음})` — 서버에서 조립 |
| **`driverUserNmTrunc10`** | string \| null | `USER_NM` 10자(Unicode)까지 자른 값(표시·디버그용) |
| `monthlyFeeKrw` | number \| null | `CD_FNUM` 기준 월 회비(원). 없으면 `null` |
| `feePolicy` | string \| null | `TB_DRIVER_DETAIL.FEE_POLICY`(등급 코드) |
| `feePolicyLabelKo` | string \| null | 공통코드 한글명(있을 때) |
| `monthlyFeeUnsetReason` | string \| null | 금액을 못 올릴 때 사용자 안내 문구 |
| `nextBillingDt` | string \| null | 다음 청구일(ISO 등) |
| `subscriptionStat` | string | 구독 상태 코드 |
| `infoMessage` | string | 하단 부가 안내 |
| **`paymentHistory`** | array | `TB_DRIVER_PAYMENT_HIST` 기반 (camelCase 필드) |
| **`paymentCards`** | array | `TB_PAYMENT_CARD` 기준 정렬·메인 우선 |

**하위 호환:** `cardNickname`, `cardLastFour` 는 메인 카드 요약용으로 채울 수 있다.

**월 회비 산정:** `TB_COMMON_CODE` 에서 `GRP_CD = 'FEE_POLICY'` 이고 `DTL_CD = TB_DRIVER_DETAIL.FEE_POLICY` 인 행의 **`CD_FNUM`** 을 원 단위 금액으로 사용한다. 등급이 비어 있거나 코드 행이 없으면 `monthlyFeeKrw` 는 `null`.

### 6-3. 메인 카드 변경 — `PATCH /api/billing-subscription/primary-card`

| 항목 | 값 |
|------|-----|
| 본문(JSON) | `driverId` (필수), `cardSeq` (필수) |
| 동작 | 해당 기사 `CUST_ID` 의 모든 카드 `IS_PRIMARY='N'` 후 지정 `CARD_SEQ` 만 `'Y'` |

성공 시 `200` + 본문 `{ BillingSubscription: { ... } }` (GET 과 동형). 구현: [`busTaams_server/lib/billingSubscriptionPayload.js`](./busTaams_server/lib/billingSubscriptionPayload.js)

---

## 7. 백엔드 파일

| 경로 | 역할 |
|------|------|
| [`busTaams_server/lib/billingSubscriptionId.js`](./busTaams_server/lib/billingSubscriptionId.js) | `BILLING_SUBSCRIPTION_ID === 'BillingSubscription'` 단일 정의 |
| [`busTaams_server/lib/billingSubscriptionPayload.js`](./busTaams_server/lib/billingSubscriptionPayload.js) | GET 응답 조립·DB 조회(TB_USER, TB_DRIVER_DETAIL, TB_COMMON_CODE, TB_PAYMENT_CARD, TB_DRIVER_PAYMENT_HIST) |
| [`busTaams_server/server.js`](./busTaams_server/server.js) | `GET /api/billing-subscription`, `PATCH /api/billing-subscription/primary-card` |

별도 `routes/` 모듈 분리는 없음(필요 시 추출 가능).

---

## 8. 관련 문서·화면

- 테이블 정본: [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) — `TB_PAYMENT_CARD`, `TB_DRIVER_PAYMENT_HIST`  
- 기사 메인 레이아웃·바로가기: [`운전기사 MainDashBoard.md`](./운전기사%20MainDashBoard.md)
- 기사 **카드 입력·등록 UI·API** 상세(현재 설계): [`버스기사 신용카드 등록 화면.md`](./버스기사%20신용카드%20등록%20화면.md)

---

## 9. PG 빌링키(Billing Key) 기반 카드 등록 — 흐름 분석·개발 가능 여부

사용자가 **앱/웹에서 카드 정보를 입력**할 때, **결제 대행(PG)사의 빌링키(정기·재결제용 토큰) 발급 API**를 사용하는 일반적인 산업 표준 흐름을 아래에 정리한다.  
**본 절은 설계·운영 참고용이며**, 현재 코드베이스의 구현 상태와 다를 수 있다(구현은 별도 과제).

### 9-1. 1단계: 입력·검증·PG 전송·정합성 판단

| 단계 | 내용 |
|------|------|
| **입력** | 사용자가 **카드번호**, **유효기간**, **생년월일(개인) 또는 사업자번호(법인)** , **카드 비밀번호 앞 2자리** 등을 입력한다. (PG·카드사 정책에 따라 필수 항목은 다를 수 있음.) |
| **클라이언트 검증** | 오타 완화를 위해 **Luhn 체크섬**(카드번호 형식)** 등 **PG 호출 전** 클라이언트 검증을 두는 경우가 많다. 이는 **실제 발급 여부와 무관**하며, 최종 정합성은 PG·카드사가 판단한다. |
| **PG 전송** | 위 정보를 **설정한 PG사**(예: **KG이니시스** 또는 계약 PG)의 **「빌링키 발급 API」**(카드 등록·토큰화 API 등 상품명은 PG마다 상이)으로 전달한다. 전달 경로는 **① 모바일/웹 PG SDK가 직 PG 통신** 또는 **② 우리 서버가 PG REST API 호출**(PCI 범위·인증 방식에 따라 설계) 등으로 나뉜다. |
| **정합성 판단** | PG사가 카드사 네트워크와 연동해 **실시간 검증**한다. |
| **실패** | 번호·유효기간·본인정보·비밀번호 등이 맞지 않으면 **즉시 에러 응답**(코드·메시지는 PG 스펙 따름). |
| **성공** | 정상 응답과 함께 **Billing Key**(빌링키·카드 토큰 등 PG가 부여하는 **구분 키값**)가 응답에 포함되며, 이후 **실제 청구 시에는 이 키만으로 결제 요청**하는 패턴이 일반적이다. |

**개발 가능 여부:** **가능하다.** 전제는 **해당 PG와의 계약**(정기결제·빌링키 발급 상품), **MID·키·환경(테스트/운영)**, 그리고 **앱/웹별 연동 방식(SDK vs 서버)** 확정이다. 본 저장소에는 KG 이니시스 **일반 결제** 관련 코드가 일부 존재할 수 있으나, **기사 월회비용 빌링키 발급**은 **별도 API·파라미터**로 구현 과제가 된다.

### 9-2. 2단계: 키값 보관(DB 저장)

| 항목 | 내용 |
|------|------|
| **저장 대상** | PG가 준 **Billing Key**(비밀키 취급 권장), **관리·식별용 메타** 예: **카드사(브랜드)명**, **마스킹 카드번호**(예: `4518----1234`, 앞 6·뒤 4 등 정책에 따름), (선택) **별칭**, **등록일**, **만료 월/년**(표시용) 등. |
| **저장 금지(버려야 할 것)** | **카드번호 전체(PAN)**, **CVC/CVV**, **카드 비밀번호 평문** 등은 **절대 DB·로그·세션에 장기 저장하지 않는다.** 입력 직후 PG로만 넘기고 **서버 메모리에서도 즉시 폐기**하는 것이 원칙이다. |
| **결제 권한** | 이후 자동결제·재청구는 **오직 PG가 발급한 키**(와 PG측 계약 조건)를 통해 이루어지며, 우리 DB에는 **원카드 정보가 없어야** PCI 부담과 유출 피해 범위가 줄어든다. |

**개발 가능 여부:** **가능하다.** `TB_PAYMENT_CARD` 등 기존 테이블에 **빌링키·마스킹·브랜드** 컬럼을 두는 방식으로 모델링하거나, PG 문서에 맞춘 전용 컬럼명으로 확장하면 된다. 기존 설계에 **`CARD_NO_ENC`(PAN 암호화)** 가 있다면, **빌링키 모델로 전환 시 저장 정책을 재정의**(PAN 미저장)해야 한다.

### 9-3. 현재 프로젝트 문서·구현과의 관계 (참고)

| 구분 | 설명 |
|------|------|
| **`카드및월회비` 모달** | 조회·메인카드 변경·**목록** 중심이며, 본 문서 §6 API로 **등록 카드·이력**을 내려준다. |
| **`버스기사 신용카드 등록 화면.md`** | 기사 **신규 카드 등록** UI/API를 서술하며, 문서상 **서버에 PAN을 암호화(`CARD_NO_ENC`) 저장**하는 흐름이 포함되어 있다. 이는 사용자가 요약한 **「빌링키만 저장·PAN/CVC/비번 미저장」** 모델과 **저장 전략이 다르다.** |
| **향후 정렬 방향** | 운영 보안·PCI 관점에서는 사용자 서술과 같은 **PG 빌링키 발급 → 키·메타만 DB** 패턴으로 수렴하는 것이 일반적이다. 실제 반영 시에는 PG사 **공식 연동 가이드**, **키 로테이션**, **웹/앱 채널별 SDK**, **서버 사이드 검증**을 함께 설계한다. |

---

## 10. 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-05-06 | 카드 및 월회비 모달·API·레퍼런스 HTML·본 문서 정리 |
| 2026-05-08 | 멤버십 제목·요금: `TB_USER.USER_NM`(10자), `TB_DRIVER_DETAIL`+`TB_COMMON_CODE` 기반 `membershipHeading`·`monthlyFeeKrw`; 비대상 시 「월회비 대상 회원 아님」; 결제내역 표 **세로 스크롤**; PCI·보안 버튼·check_circle·구 안내 문구 **제거** |
| 2026-05-15 | §9 추가: 앱/웹 카드 입력 시 **PG 빌링키 발급·실시간 정합성·DB 보관(키·마스킹만)** 흐름 분석 및 개발 가능 여부; 기존 카드등록 문서와의 정책 차이 참고 |
