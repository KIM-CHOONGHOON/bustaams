# 여행자 견적 목록 모달 — 개발 명세

기사(운전자)가 **입찰 가능한 역경매 견적 요청**을 카드 목록으로 보는 모달이다. 화면은 `presentation`에 따라 제목만 **`여행자 견적 목록`** / **`견적 요청 목록`**으로 바뀌고, **API·목록 로직은 동일**하다.

데이터·DDL 정본은 `BusTaams 테이블.md`, `BusTaams_Project 테이블 설계.md`를 따른다.

---

## 1. 라우팅·백엔드 구조

| 항목 | 설명 |
|------|------|
| **Express 라우터 분리 파일** | 이 프로젝트에는 **`router.js` 없음**. HTTP 라우트는 **`busTaams_server/server.js`** 에 `app.get` / `app.put` 등으로 직접 등록된다. |
| **본 목록 API** | `GET /api/list-of-traveler-quotations` — **`server.js`** 내 단일 구현이 정본이다. |
| **참고(미연동)** | `busTaams_server/routes/driverQuotationOpportunitiesList.js` 는 **별도 쿼리/조건**을 담은 모듈이나, 현재 **`server.js`에서 require 하지 않음**. 구현·기준은 **항상 `server.js`** 를 본다. |

---

## 2. 관련 파일

| 구분 | 경로 |
|------|------|
| **모달 컴포넌트** | `busTaams_web/src/components/ListOfTravelerQuotations/ListOfTravelerQuotations.jsx` |
| **앱에서 오픈** | `busTaams_web/src/App.jsx` — `travelerQuotationModal` 상태, `ListOfTravelerQuotations` 렌더 |
| **진입(기사 대시보드)** | `busTaams_web/src/components/DriverDashboard/DriverDashboard.jsx` — `onQuotationList` 콜백 |
| **상세(중첩 모달)** | `busTaams_web/src/components/TravelerQuoteRequestDetails/TravelerQuoteRequestDetails.jsx` |
| **REST** | `busTaams_server/server.js` — `GET /api/list-of-traveler-quotations` |
| **유사 API(대시보드 카드)** | `GET /api/auction-list` — 마스터 `AUCTION` 조건은 비슷하나 **응답 필드·용도**가 다름 (§9 참고). |

---

## 3. 앱 연동 (`App.jsx`)

- 상태: `travelerQuotationModal: null | { presentation: 'traveler' | 'quoteRequest' }`
- **기사 대시보드**에서 `onQuotationList={() => setTravelerQuotationModal({ presentation: 'traveler' })}` 로 연다.
- 렌더 조건: `travelerQuotationModal` 이 truthy 일 때

```text
<ListOfTravelerQuotations
  key={travelerQuotationModal.presentation}
  close={() => setTravelerQuotationModal(null)}
  currentUser={user}
  presentation={travelerQuotationModal.presentation}
/>
```

- 현재 코드 기준 **`quoteRequest` 프레젠테이션을 여는 경로는 없음**(헤더·문구 분기만 구현됨). 추가 진입이 필요하면 동일 props로 `presentation: 'quoteRequest'` 를 넘기면 된다.

---

## 4. 컴포넌트 Props (`ListOfTravelerQuotations`)

| Prop | 타입 | 필수 | 설명 |
|------|------|------|------|
| `close` | `() => void` | 예 | 목록 모달만 닫기(상위에서 `setTravelerQuotationModal(null)`). |
| `currentUser` | `object` | 예 | `custId` 또는 `CUST_ID` 로 `driverId` 쿼리 조립. |
| `presentation` | `'traveler' \| 'quoteRequest'` | 아니오 | 기본 `'traveler'`. 제목·영문 서브타이틀·`aria-labelledby` id만 변경. |

---

## 5. 환경 변수·API 베이스

- `VITE_API_BASE_URL` — 끝 슬래시 제거 후 사용.
- 미설정 시 컴포넌트 기본값: `http://127.0.0.1:8080` (`TravelerQuoteRequestDetails`와 동일 패턴).

---

## 6. REST API — `GET /api/list-of-traveler-quotations`

**구현 위치:** `busTaams_server/server.js`

### 6.1 Query

| 파라미터 | 필수 | 설명 |
|-----------|------|------|
| `driverId` | 아니오 | 기사 `CUST_ID`(`TB_BUS_RESERVATION.DRIVER_ID`와 동일 체계로 쓸 값). 있으면 §6.3 **동일 출발일 확정 제외** 서브쿼리가 활성화된다. |

`driverId` 없이 호출하면 해당 필터 없이 **마스터 조건만** 적용된다(구현상 가능; 프론트는 로그인 시 거의 항상 넘김).

### 6.2 SQL 대상·조회 필드

- **FROM:** `TB_AUCTION_REQ` 별칭 `r`
- **SELECT (items 한 행):**

| JSON 필드 | 출처 |
|-----------|------|
| `reqId` | `r.REQ_ID` |
| `tripTitle` | `r.TRIP_TITLE` |
| `startAddr`, `endAddr` | `r.START_ADDR`, `r.END_ADDR` |
| `startDt`, `endDt` | `r.START_DT`, `r.END_DT` |
| `passengerCnt` | `r.PASSENGER_CNT` |
| `reqStat` | `r.DATA_STAT` |
| `estTotalServicePrice` | `COALESCE(r.REQ_AMT, 0)` |
| `expireDt` | `r.EXPIRE_DT` |
| `regDt` | `r.REG_DT` |
| `busType` | 서브쿼리: `GROUP_CONCAT(BUS_TYPE_CD)` from `TB_AUCTION_REQ_BUS`·`REQ_ID` |
| `busCnt` | 서브쿼리: `COUNT(*)` 해당 `REQ_ID` 버스 행 수 |
| `reqBusSeq` | 서브쿼리: 해당 `REQ_ID`에서 **`ORDER BY REQ_BUS_SEQ ASC LIMIT 1`** (목록→상세 시 **첫 버스 슬롯**으로 상세 연결) |
| `waypointCount` | `TB_AUCTION_REQ_VIA` 중 `VIA_TYPE IN ('START_WAY','END_WAY')` 개수 |

- **WHERE (필수):**
  - `r.DATA_STAT = 'AUCTION'`
  - `DATE(r.START_DT) > CURDATE()` — **당일 출발 제외**, 내일 이후만
- **ORDER BY:** `r.REG_DT DESC`

### 6.3 `driverId` 있을 때 추가 조건 (`sameDayBlock`)

아래를 만족하는 견적은 **목록에서 제외**된다.

- 존재: `TB_BUS_RESERVATION res` + `TB_AUCTION_REQ ar`
- `res.DRIVER_ID = :driverId`
- `res.DATA_STAT = 'CONFIRM'`
- `DATE(ar.START_DT) = DATE(r.START_DT)` (다른 요청 `ar`의 출발 **일**이 현재 행 `r`과 같음)
- `ar.REQ_ID <> r.REQ_ID`

**의미:** 같은 출발일에 **이미 다른 견적으로 확정 예약**이 있는 기사에게는, 그날짜의 다른 역경매 카드가 안 보이게 막는다.

### 6.4 응답

- **200:** `{ total: number, items: Array<object> }` — 구현상 `total === items.length`
- **500:** `{ error: string }`

### 6.5 `waypointCount`·목록 필터

- `waypointCount`는 **SELECT에만** 있고 **WHERE 조건은 아님** (0건이어도 마스터만 맞으면 노출).
- 주석의 “waypointCount 조건”과 실제 SQL이 다를 수 있으니 **코드 기준**을 본다.

---

## 7. 프론트 데이터 로딩·오류 처리

- **요청:** `GET ${API_BASE}/api/list-of-traveler-quotations?driverId=...` (`encodeURIComponent`)
- **본문:** `res.text()` 후 `JSON.parse` — HTML/비JSON이 오면 사용자 메시지로 분기.
- **성공:** `items = json.items || []`, `total = json.total ?? items.length`
- **마운트·`currentUser` 변경 시** `useEffect`로 `fetchList()` 자동 실행.

---

## 8. UI·접근성·인터랙션

### 8.1 z-index·중첩

| 레이어 | z-index | 역할 |
|--------|---------|------|
| 목록 모달 | `z-[150]` | 기사 앱 메인 위 |
| `TravelerQuoteRequestDetails` | `z-[200]` | 목록 위 (카드 클릭 시) |
| 상세 내 409 등 | `z-[310]` | (`TravelerQuoteRequestDetails` 내부) |

### 8.2 닫기 동작

- **배경 클릭으로 닫히지 않음** (오버레이에 `stopPropagation` 없음; 실질적으로 카드에만 전파 차단). **X**, **하단「닫기」**, **Escape**로 목록 닫기.
- **Escape:** 상세가 열려 있으면(`selectedQuote`) 목록은 닫지 않음 — 상세 먼저 닫도록 함.
- **상세 닫기:** `setSelectedQuote(null)` 만; 목록은 유지.
- **「전체 닫기」:** X / 하단 닫기 시 `selectedQuote` 초기화 후 `close()`.

### 8.3 카드 클릭 → 상세

```text
setSelectedQuote({ reqId, reqBusSeq: pickReqBusSeq(item) })
```

- `pickReqBusSeq`: `item.reqBusSeq` 가 0 이상 유한수면 사용, 아니면 **1**.

`TravelerQuoteRequestDetails` 에게:

- `onBidInsertSuccess={fetchList}` — **신규 예약 INSERT** 후 상세가 닫히기 전 목록 갱신.

### 8.4 헤더·제목 (`presentation`)

| `presentation` | 영문 서브타이틀 | 한글 제목 | `aria-labelledby` id |
|----------------|-----------------|-----------|----------------------|
| `traveler` | List of Traveler Quotations | 여행자 견적 목록 | `traveler-quotation-list-title` |
| `quoteRequest` | Quote Request List | 견적 요청 목록 | `quote-request-list-title` |

### 8.5 카드 표시 필드·라벨

- **상태 배지:** `reqStat` → `AUCTION_REQ_STAT_LABEL` 맵 (AUCTION→입찰대기 등).
- **제목:** `tripTitle` 없으면 `` `${startAddr} → ${endAddr}` ``.
- **`tripTitle` 있을 때만** 보조 한 줄: `startAddr → endAddr`.
- **날짜 포맷:** 로컬 `YYYY/MM/DD HH:mm` (`formatDt`).
- **차량:** `busType × busCnt대` — **API가 넘기는 `GROUP_CONCAT(BUS_TYPE_CD)` 원코드** 그대로(상세 화면의 `TB_COMMON_CODE` 한글명과 **불일치할 수 있음**).

### 8.6 요약 바 문구

- `total > 0`: `현재 입찰 가능한 역경매 견적 요청이 N건 있습니다.`
- `total === 0`: `현재 입찰 가능한 견적 요청이 없습니다.`

---

## 9. `GET /api/auction-list` 와의 차이 (참고)

- **위치:** `server.js` 별도 핸들러.
- **필수:** `driverId` 없으면 **400**.
- **마스터:** `DATA_STAT = 'AUCTION'` 등 목록과 유사하나 **SELECT 컬럼·추가 비즈니스(예: `resId`)** 가 다를 수 있음.
- **여행자 견적 목록 모달**은 **`/api/list-of-traveler-quotations` 만 사용**한다.

---

## 10. 개발 시 정합성 체크리스트

- [ ] `driverId` = 세션 기사 `custId` / `CUST_ID`와 DB `TB_BUS_RESERVATION.DRIVER_ID` 규칙 일치
- [ ] 마스터 `AUCTION` + 출발일 **내일 이후**만 노출되는지
- [ ] 동일 출발일 `CONFIRM` 타 요청 있을 때 카드 제외되는지
- [ ] 카드 `reqBusSeq`가 **첫 버스 슬롯**이라 복수 버스 견적에서 상세·입찰 슬롯이 의도와 맞는지 (필요 시 API/카드에서 슬롯 선택 설계)
- [ ] 상세 모달 z-order·Escape 동작
- [ ] 신규 청약 후 `fetchList`로 목록 갱신·마스터가 `BIDDING`이면 **카드가 사라지는** 현상 이해 (조회 조건이 `AUCTION`만)
- [ ] `VITE_API_BASE_URL`·CORS·JSON 응답 여부

---

## 11. 확장 아이디어 (명세 외)

- 목록 `busType`도 상세와 같이 **`TB_COMMON_CODE`(GRP_CD=`BUS_TYPE`)로 한글명** 표시.
- `presentation: 'quoteRequest'` 를 다른 메뉴에서 열기.
- `waypointCount`를 필터·정렬에 사용할지 제품 정책 결정.

---

*문서는 저장소의 `ListOfTravelerQuotations.jsx` 및 `server.js` 해당 구간 기준으로 정리하였다.*
