# 기사님 청약 취소 — 개발 정본

버스기사가 **청약(응찰)·예약 확정** 상태의 예약 행을 **기사 사유로 취소**할 때 사용하는 **단일 모달**과 그에 딸린 **프론트·백엔드·REST API**를 한 문서에서 재구현할 수 있도록 정리한다.

| 항목 | 값 |
|------|-----|
| 화면 제목(모달 헤더) | **버스 청약 취소** |
| 기능·API·JSON `screenId` | **`CancellationOfBid`** |
| 라우팅 | **`router.js` 없음** — `busTaams_server/server.js` 에서 `require('./routes/cancellationOfBid')(app, pool)` 로 마운트 |
| 스키마 정본(참고) | `BusTaams_Project 테이블 설계.md` — `TB_BUS_RESERVATION`, `TB_USER_CANCEL_MANAGE`, `TB_USER_CANCEL_HIST`, `TB_FILE_MASTER` |

---

## 1. 기능 범위

- **대상 상태:** `TB_BUS_RESERVATION.DATA_STAT` 가 **`BIDDING`** 또는 **`CONFIRM`** 인 행만 취소 가능.
- **처리 결과:** 동일 행 `DATA_STAT → DRIVER_CANCEL`.
- **미수정(정책):** `TB_AUCTION_REQ`, `TB_AUCTION_REQ_BUS` 는 이 흐름에서 **갱신하지 않음** (`driverBidCancellation.js` 주석과 동일).
- **누적 제한:** 기사 청약 취소 누적 `CANCEL_BUS_DRIVER_CNT` 가 **2**이면 추가 청약 취소 불가(모달에서 제출 비활성 + 서버에서 거절).
- **낙관적 일관성:** 모달 오픈 시 조회한 `CANCEL_BUS_DRIVER_CNT` 를 **`cancelBusDriverCntSnapshot`** 으로 POST 하며, 트랜잭션 내 `FOR UPDATE` 로 읽은 값과 **반드시 일치**해야 한다. 불일치 시 `CANCEL_SNAPSHOT_STALE`.
- **증빙:** multipart 로 PDF/JPEG/PNG 업로드(선택). 이전에 제출한 증빙은 요약 API의 `lastProofAttachments` 로 표시하고 **CommonView** 로 조회.

---

## 2. 프론트엔드

### 2.1 파일

| 경로 | 역할 |
|------|------|
| `busTaams_web/src/components/CancellationOfBid/CancellationOfBid.jsx` | 모달 UI·요약 조회·POST·CommonView 연동 |
| `busTaams_web/src/components/CommonView/CommonView.jsx` | 문서 모드(`fileId`+`custId`+API 경로 오버라이드) |

### 2.2 모달 진입점(부모가 넘기는 props)

동일 컴포넌트를 두 화면에서 사용한다.

| 진입 | 파일 | 비고 |
|------|------|------|
| 기사 청약 목록 | `DriversListOfBids.jsx` | 행별 **청약취소** → `resId`, `reqId`, `reqBusSeq`, `travelerId`, `driverId`, `busId` |
| 여행자 견적 요청 상세 | `TravelerQuoteRequestDetails.jsx` | `resId` 는 상세 API(`data.resId`), `reqBusSeq` 는 **0 이상** 정수 허용 |

**props 계약**

| prop | 필수 | 설명 |
|------|------|------|
| `open` | ○ | 모달 표시 |
| `onClose` | ○ | 닫기 |
| `driverId` | ○ | 기사 `CUST_ID`(요약/POST 의 `driverId`) |
| `reqId` | ○ | `REQ_ID` |
| `resId` | ○ | `RES_ID` |
| `reqBusSeq` | △ | `TB_BUS_RESERVATION.REQ_BUS_SEQ` (**0부터 허용**) |
| `travelerId` | △ | 있으면 POST에 포함, 서버에서 예약 행과 대조 |
| `busId` | △ | 있으면 POST에 포함, 서버에서 예약 행과 대조 |
| `onSuccess` | △ | 성공 시 콜백 후 부모에서 목록/상세 재조회 |

`VITE_API_BASE_URL` 이 없으면 기본 `http://127.0.0.1:8080` 과 같이 **슬래시 없는** base URL 로 `fetch` 한다.

### 2.3 모달 오픈 시 시퀀스

1. `open === true` 이면 폼·스냅샷·증빙 목록 초기화.
2. **`GET {API_BASE}/api/CancellationOfBid/summary?driverId={driverId}`**
   - `CancellationOfBid.cumulativeCancelCount` → 상태 및 `cancelBusDriverCntSnapshotRef`
   - `lastProofAttachments` → 이전 증빙(있을 때만 UI)
3. **`cumulativeCancelCount === 2`** 이면 **청약 취소 신청** 버튼 비활성(서버도 막음).
4. Escape: CommonView 가 열려 있으면 먼저 CommonView만 닫고, 아니면 `onClose`.

### 2.4 UI 요소(구현 기준)

- 제목: **버스 청약 취소**
- 상단 박스: **청약 취소 누적 건수** (빨간 강조), `tradeRestrictYn === 'Y'` 이면 거래 제한 문구
- **취소 사유 선택** (`cancellationReasonCode`): 셀렉트, 옵션 값은 서버 `ALLOWED_REASON_CODES` 와 **동일 문자열**이어야 함
- **상세 사유** (`detailReason`): 선택
- **증빙 서류 첨부**
  - `lastProofAttachments` 있으면: 라디오 선택 + `ORG_FILE_NM`+`FILE_EXT` 라벨, 목록 영역 `max-height`+세로 스크롤, **파일보기** → CommonView
  - 점선 영역: 새 파일 드래그/클릭, 최대 **5개**, 파일당 **10MB**, `accept` PDF·JPEG·PNG
- 버튼: **청약 유지** / **청약 취소 신청**
- 특정 `errorCode` 는 인라인이 아니라 **안내 모달**(단일 닫기)

**프론트 취소 사유 옵션(서버와 일치해야 함)**

- `CHANGE_OF_MIND`, `VEHICLE_DAMAGE`, `HOSPITALIZATION`, `OUTPATIENT_SAME_DAY`, `LEGAL_CUSTODY`, `DEATH_KIN_SPOUSE`, `DEATH_SELF`, `OTHER`

### 2.5 POST 제출

- **`POST {API_BASE}/api/CancellationOfBid`**, **`multipart/form-data`**
- 필드:
  - `reqId`, `driverId`, `resId`, `reqBusSeq` (문자열로 전달 가능)
  - `cancellationReasonCode`, `detailReason`
  - `cancelBusDriverCntSnapshot` — 요약 조회 직후의 **정수 문자열** (`ref` 우선)
  - 선택: `travelerId`, `busId`
  - 파일: 필드명 **반복** `attachments` (multer `array('attachments', 5)`)
- 성공: HTTP 200, body `CancellationOfBid.success === true` → `onSuccess` 후 `onClose`
- 실패 시 `errorCode` 가 아래 중 하나면 안내 모달:  
  `NOT_BIDDING_OR_CONFIRM`, `MAX_DRIVER_BID_CANCELS`, `CANCEL_SNAPSHOT_STALE`, `BAD_CANCEL_SNAPSHOT`

### 2.6 CommonView(이전 증빙 조회)

`commonViewProof.fileId` 가 있을 때만 마운트.

| prop | 값 |
|------|-----|
| `custId` | `driverId` |
| `fileId` | 선택한 첨부의 `fileId` |
| `docTitle` | 예: `청약·취소 증빙` |
| `metaPath` | `/api/common-view/driver-cancel-proof/meta` |
| `streamPath` | `/api/driver/driver-cancel-proof/file` |
| `downloadPath` | `/api/common-view/driver-cancel-proof/download` |

쿼리는 CommonView 내부에서 `custId`, `fileId` 를 붙인다.

---

## 3. 백엔드 구조

### 3.1 등록 위치(router 없음)

`busTaams_server/server.js` (상단부, 다른 라우트와 같이):

```javascript
require('./routes/cancellationOfBid')(app, pool);
```

별도 **Express Router 파일**로 분리하지 않고, `cancellationOfBid.js` 가 `app.get` / `app.post` 를 직접 등록한다.

### 3.2 모듈 일람

| 경로 | 역할 |
|------|------|
| `busTaams_server/routes/cancellationOfBid.js` | `GET /summary`, `POST /api/CancellationOfBid`, multer, 트랜잭션 시작·커밋 |
| `busTaams_server/lib/driverBidCancellation.js` | `executeDriverBidCancellation` — 핵심 DB·GCS·이력 |
| `busTaams_server/lib/driverCancelProofAccess.js` | 최신 이력 증빙 메타, `canAccessDriverCancelProofFile`, GCS 객체 키 해석 |
| `busTaams_server/server.js` | CommonView용 **3개 GET** (`driver-cancel-proof`) |

### 3.3 환경

- GCS: `GCS_BUCKET_NAME`(기본 `bustaams-secure-data`), `GOOGLE_APPLICATION_CREDENTIALS` 등 기존 Storage 설정
- `routes/cancellationOfBid.js` 는 `@google-cloud/storage` 로 **별도** `bucket` 인스턴스를 만들어 `executeDriverBidCancellation` 에 넘김

---

## 4. REST API 명세

### 4.1 `GET /api/CancellationOfBid/summary`

**Query**

- `driverId` (필수): 기사 `CUST_ID`

**성공 200 — body**

```json
{
  "CancellationOfBid": {
    "screenId": "CancellationOfBid",
    "cumulativeCancelCount": 0,
    "tradeRestrictYn": "N",
    "tradeRestrictLabel": "정상 (N)",
    "lastProofAttachments": [
      {
        "fileId": "…",
        "orgFileNm": "…",
        "fileExt": "pdf",
        "fileSizeBytes": 12345
      }
    ]
  }
}
```

- `cumulativeCancelCount`: `TB_USER_CANCEL_MANAGE.CANCEL_BUS_DRIVER_CNT` 를 **0~2로 클램프**; 행 없으면 **0**
- `lastProofAttachments`: `fetchLastHistProofFileMetas` — **최신** `TB_USER_CANCEL_HIST` 행의 `REASON_DOC_FILE_NM`(콤마 구분 `FILE_ID`) 에 대해 `TB_FILE_MASTER` 조회, `FILE_CATEGORY = DRIVER_CANCEL_REPORT`

**스키마 오류 폴백** (`ER_BAD_FIELD_ERROR` 등): `TB_USER_CANCEL_MANAGE` 조회 실패 시  
`TB_BUS_RESERVATION` 에서 `DRIVER_ID` + `DATA_STAT = 'DRIVER_CANCEL'` 인 건수로 `cumulativeCancelCount` 를 대체하고, `tradeRestrictYn` 은 `N` 고정.

### 4.2 `POST /api/CancellationOfBid`

**Content-Type:** `multipart/form-data`

**폼 필드**

| 이름 | 필수 | 설명 |
|------|------|------|
| `reqId` | ○ | |
| `driverId` | ○ | |
| `resId` | ○ | |
| `reqBusSeq` | ○ | 정수, **0 이상** |
| `cancellationReasonCode` | ○ | 허용 집합(아래) |
| `cancelBusDriverCntSnapshot` | ○ | **0, 1, 2** 중 하나(정수) |
| `detailReason` | △ | 없으면 서버에서 코드 문자열로 대체 |
| `travelerId` | △ | 전달 시 예약 행 `TRAVELER_ID` 와 일치 검증 |
| `busId` | △ | 전달 시 예약 행 `BUS_ID` 와 일치 검증 |
| `attachments` | △ | 파일 배열, 최대 5, 각 10MB, MIME: PDF/JPEG/PNG |

**허용 `cancellationReasonCode`**

`CHANGE_OF_MIND`, `DEATH_SELF`, `VEHICLE_DAMAGE`, `LEGAL_CUSTODY`, `DEATH_KIN_SPOUSE`, `HOSPITALIZATION`, `OUTPATIENT_SAME_DAY`, `OTHER`

**처리 순서(라우트)**

1. `TB_BUS_RESERVATION` 단건 조회로 행 존재·`BIDDING|CONFIRM`·`travelerId`/`busId` 검증 (트랜잭션 **밖**에서 404/409 가능)
2. `beginTransaction`
3. `executeDriverBidCancellation(connection, gcsBucket, { … })`
4. 실패 시 `rollback` + 해당 `status`/`errorCode`
5. 성공 시 `commit`

**성공 200**

```json
{
  "CancellationOfBid": {
    "screenId": "CancellationOfBid",
    "success": true,
    "message": "응찰 취소가 처리되었습니다.",
    "attachmentCount": 0,
    "cancellationReasonCode": "CHANGE_OF_MIND"
  }
}
```

**오류 응답 공통**

- JSON 에 `error`(문자열), 선택 `errorCode`, `CancellationOfBid: { "screenId": "CancellationOfBid" }`

**`errorCode` 표**

| code | HTTP | 의미 |
|------|------|------|
| `BAD_CANCEL_SNAPSHOT` | 400 | 스냅샷 형식 오류 |
| `NOT_BIDDING_OR_CONFIRM` | 409 | 상태 불가 또는 예약 갱신 실패 |
| `MAX_DRIVER_BID_CANCELS` | 409 | 이미 2회 |
| `CANCEL_SNAPSHOT_STALE` | 409 | 스냅샷≠DB 또는 관리 행 갱신 0건(경쟁) |

---

## 5. `executeDriverBidCancellation` 트랜잭션 로직

파일: `busTaams_server/lib/driverBidCancellation.js`

1. **`cancelBusDriverCntSnapshot` 검증** (0~2 정수).
2. **`TB_USER_CANCEL_MANAGE`** `WHERE CUST_ID = ? FOR UPDATE`
   - `prevDb = COALESCE(CANCEL_BUS_DRIVER_CNT,0)` (0~2 클램프)
   - **`snap !== prevDb` → `CANCEL_SNAPSHOT_STALE`**
   - **`snap >= 2` → `MAX_DRIVER_BID_CANCELS`**
   - `nextCnt = prevDb + 1` (최대 2)
3. **`TB_BUS_RESERVATION`** 해당 키로 `FOR UPDATE`, 상태 `BIDDING|CONFIRM` 만 허용 → 아니면 `NOT_BIDDING_OR_CONFIRM`
4. **UPDATE** `DATA_STAT = 'DRIVER_CANCEL'`, `MOD_ID` 등 — **`affectedRows === 1`** 필수
5. **`TB_USER_CANCEL_MANAGE`**
   - 행 없음: **INSERT** — `CANCEL_BUS_DRIVER_CNT = nextCnt`, `TRADE_RESTRICT_*`, `CANCEL_CNT` 등은 설계대로(현재 코드: `CANCEL_CNT=0`, 제한 종료일은 1회차 7일·2회차 14일 등)
   - 행 있음: **UPDATE**  
     `CANCEL_BUS_DRIVER_CNT = COALESCE(...)+1`  
     `WHERE CUST_ID=? AND COALESCE(CANCEL_BUS_DRIVER_CNT,0)=?` (**낙관적 잠금**, `prevDb` 일치 시만 1행 갱신)  
     → 0행이면 `CANCEL_SNAPSHOT_STALE`
6. **첨부 파일:** `allocateSequentialFileIds` → GCS 업로드 경로 기본  
   `DRIVER_CANCEL_REPORT/{CUST_ID}/{fileId}/{fileId}` 역할의 `GCS_PATH`/`FILE_ID` 규칙은 `resolveGcsObjectKey` 와 동일 계열
7. **`TB_FILE_MASTER`** INSERT — `FILE_CATEGORY = DRIVER_CANCEL_REPORT`
8. **`TB_USER_CANCEL_HIST`** INSERT — `HIST_SEQ = MAX+1`, `REASON_DOC_FILE_NM` = 새 파일 ID들 콤마(없으면 NULL), 사유 코드/텍스트 저장

**메시지 상수 (`MSGS`)**는 `NOT_BIDDING_OR_CONFIRM`, `MAX_CANCELS`, `SNAPSHOT_STALE` 문구로 사용자에게 그대로 노출될 수 있다.

---

## 6. 증빙 조회 API (server.js)

접근 제어: `driverCancelProofAccess.canAccessDriverCancelProofFile`

- 최신 `TB_USER_CANCEL_HIST.REASON_DOC_FILE_NM` 에 포함된 `fileId` 인지
- 해당 `TB_FILE_MASTER.FILE_CATEGORY === DRIVER_CANCEL_REPORT`
- `GCS_PATH` 가 `DRIVER_CANCEL_REPORT/{custId}/` 접두

| 메서드 | 경로 | 용도 |
|--------|------|------|
| GET | `/api/common-view/driver-cancel-proof/meta?custId=&fileId=` | JSON 메타 |
| GET | `/api/common-view/driver-cancel-proof/download?custId=&fileId=` | 다운로드 |
| GET | `/api/driver/driver-cancel-proof/file?custId=&fileId=` | 인라인 스트림(CommonView) |

---

## 7. 데이터 모델 메모

- 애플리케이션 코드 기준 **조인·식별자는 `CUST_ID`** (`TB_USER_CANCEL_MANAGE`, `TB_USER_CANCEL_HIST`, 예약의 `DRIVER_ID` 등).
- DDL 문서가 `USER_UUID`/`USER_ID` 만 언급하는 경우가 있으면, **실제 배포 스키마와 코드의 컬럼명을 반드시 맞출 것**.

---

## 8. 개발·테스트 체크리스트

- [ ] 모달 오픈 시 summary 호출, 누적 2면 제출 버튼 비활성
- [ ] 스냅샷: summary 직후 값이 POST 에 그대로 전달되는지(탭 전환·재오픈 시 재조회)
- [ ] `REQ_BUS_SEQ = 0` 인 예약 행이 있으면 목록→모달→POST 전 구간 일치
- [ ] 성공 후 `TB_BUS_RESERVATION.DATA_STAT === DRIVER_CANCEL`
- [ ] `CANCEL_BUS_DRIVER_CNT` 가 요청당 정확히 +1 (동시 요청 시 하나는 STALE)
- [ ] 첨부 0건 / 5건 / 초과 크기·MIME 거절
- [ ] 이전 증빙 `lastProofAttachments` + CommonView 3 API (403·200)
- [ ] `CANCEL_SNAPSHOT_STALE` 은 모달 닫고 다시 열도록 안내

---

## 9. 관련 문서·화면

- `기사님 청약 목록.md` — `DriversListOfBids` 및 청약취소 버튼
- `CommonView.md` — 문서 모드 props·동작
- `BusTaams_Project 테이블 설계.md` — 테이블 의미

---

*본 문서는 저장소 내 구현(`CancellationOfBid.jsx`, `cancellationOfBid.js`, `driverBidCancellation.js`, `driverCancelProofAccess.js`, `server.js` 의 해당 구간)을 기준으로 작성하였다.*
