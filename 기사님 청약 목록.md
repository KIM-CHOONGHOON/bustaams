# 기사님 청약 목록

운전기사(버스기사)가 본인이 **응찰 등록(`BIDDING`)** 또는 **예약 확정(`CONFIRM`)** 한 건만 모아 보는 **모달**과, 이를 채우는 **REST API** 구현 명세이다. 이 문서만으로 동일 기능을 프론트·백엔드에 재구현할 수 있도록 **식별자, 쿼리, 응답 DTO, 파일 경로, 마운트 순서**까지 맞춰 기술한다.

**스키마 정본:** [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) — 특히 `TB_BUS_RESERVATION`, `TB_AUCTION_REQ`.

---

## 1. 식별자·이름 규칙

| 구분 | 값 | 비고 |
|------|-----|------|
| 화면 제목(한글) | 기사님 청약 목록 | 모달 헤딩 |
| 영문 ID / `screenId` | **`DriversListOfBids`** | JSON 루트 키·컴포넌트 모듈과 동일 철자 |
| Express 핸들러 함수명 | **`DriversListOfBids`** | `routes/driversListOfBids.js` 내부 |
| 프론트 exported 상수 | **`SCREEN_ID`** = `'DriversListOfBids'` | `DriversListOfBids.jsx` |
| HTTP API 경로 | **`GET /api/DriversListOfBids`** | 쿼리스트링만 사용 |

**별도 `router.js` 없음.** Express `app` 에 핸들러를 **직접** `app.get(API_PATH, handler)` 로 등록한다.

---

## 2. 사용자 흐름(UX)

1. 기사가 **운전기사 대시보드**에서 **「기사님 청약 목록」** 진입(퀵 메뉴 등) → 모달 `open=true`.
2. 모달이 열리면 **`driverId`** 기준으로 목록 API를 호출한다. 모달이 닫혔다 열릴 때 **`page`는 1로 리셋**한다.
3. 테이블에 행이 나오면 **상태·일자·제목·운임**을 표시한다.
4. **「청약취소」** 클릭 → **별도 모달** [`CancellationOfBid`](./버스기사%20응찰%20취소.md) (`버스 청약 취소` UI) 를 연다. 취소 성공 시 목록을 **다시 조회**한다.
5. **Escape** 로 모달 닫기: **청약 취소 서브모달이 열려 있으면** 목록 모달만 닫지 않도록 처리(서브모달 우선).
6. 바깥 오버레이 `z-index`: 목록 모달 약 **115**, 청약 취소 모달은 그보다 위(**120** 등) — 겹침 정책 유지.

---

## 3. 데이터 모델·조회 조건(백엔드)

### 3.1 조인

- **FROM** `TB_BUS_RESERVATION` **`r`**
- **LEFT JOIN** `TB_AUCTION_REQ` **`ar`** **`ON ar.REQ_ID = r.REQ_ID`**

### 3.2 `driverId` 매칭

- **`r.DRIVER_ID = ?`** — 쿼리 파라미터 `driverId` (기사 **`TB_USER.CUST_ID`** 를 넣는 정책이 목표).  
- 클라이언트는 `App.jsx` 등에서 `user.custId || user.userId` 를 넘길 수 있으므로, 운영에서는 **예약 테이블의 `DRIVER_ID`와 동일한 키**로 통일할 것.

### 3.3 상태 컬럼(스키마 호환)

- **우선** `r.DATA_STAT` 사용.
- 첫 시도에서 **알 수 없는 컬럼 오류**(MySQL `ER_BAD_FIELD_ERROR` / `errno 1054`)이면 **`r.RES_STAT`** 로 동일 조건·동일 SELECT 를 **한 번 재시도**한다.  
  (레거시 DDL `sql/tb_bus_reservation.sql` 등에서 상태 컬럼명이 다를 수 있음.)

### 3.4 WHERE (공통 패턴)

아래에서 **`${statCol}`** 은 `DATA_STAT` 또는 `RES_STAT` 이다.

```text
r.DRIVER_ID = ?
AND r.${statCol} IN ('BIDDING', 'CONFIRM')
AND NOT (
  r.${statCol} = 'CONFIRM'
  AND ar.START_DT IS NOT NULL
  AND DATE(ar.START_DT) = CURDATE()
)
```

- **포함:** 응찰 등록·예약 확정만.
- **제외:** **오늘 출발일**인 **예약 확정** 건 — `CONFIRM` 이면서 **`DATE(ar.START_DT) = CURDATE()`** 인 행은 목록에서 **빼기**.

### 3.5 정렬·페이지

- **ORDER BY** `r.RES_ID DESC`
- **LIMIT / OFFSET:** 서버에서 정수 검증 후 **리터럴 삽입** (`LIMIT ${limitSql} OFFSET ${offsetSql}`). 일부 MySQL/MariaDB + prepared statement 조합 이슈 회피용.
- **page:** 최소 1, 기본 1 — `parseInt` 실패 시 1.
- **pageSize:** 최소 1, 최대 **100**, 기본 **10**. 프론트 모달은 **6**을 쓸 수 있음.

### 3.6 SELECT 컬럼(목록 1행)

| DB/alias | 응답 필드 | 비고 |
|----------|-----------|------|
| `r.RES_ID` | `resId` | 예약 키 |
| `r.REQ_ID` | `reqId` | 견적 요청 키 |
| `r.REQ_BUS_SEQ` | `reqBusSeq` | 숫자, 없으면 프론트에서 **1** |
| `r.TRAVELER_ID` | `travelerId` | 문자열, null → `''` |
| `r.DRIVER_ID` | `driverId` | |
| `r.BUS_ID` | `busId` | null → `''` |
| `r.DRIVER_BIDDING_PRICE` | `driverBiddingPrice` | 숫자, null → `0` |
| `r.DATA_STAT` 또는 `r.RES_STAT` | `dataStat` | 대문자 정규화는 표시용으로 프론트 가능 |
| `r.REG_DT` | `regDt` | 응찰(청약) 등록일 |
| `ar.REG_DT` | `quoteRequestDt` | 견적 요청일 |
| `ar.TRIP_TITLE` | `tripTitle` | |
| `ar.START_DT` | `tripStartDt` | 출발 일시 |
| `ar.END_DT` | `tripEndDt` | 도착 일시 |

### 3.7 상태 표시 매핑(백엔드 `statusPresentation`)

| `dataStat` | `statusLabelKo` | `statusCategory` |
|------------|-----------------|------------------|
| `BIDDING` | 응찰 등록 | `bidding` |
| `CONFIRM` | 예약확정 | `confirm` |
| 그 외 | 원문(대문자 등) | `other` |

프론트 뱃지 색: `bidding` → 주황 계열, `confirm` → 짙은 틸 계열(현재 Tailwind 커스텀 색).

---

## 4. REST API 명세

### 4.1 요청

- **Method / URL:** `GET /api/DriversListOfBids`
- **Query**

| 이름 | 필수 | 기본 | 제약 |
|------|------|------|------|
| `driverId` | 예 | — | trim, 비어 있으면 **400** |
| `page` | 아니오 | `1` | ≥ 1 |
| `pageSize` | 아니오 | `10` | 1 ≤ pageSize ≤ 100, 아니면 **400** |

**예:**  
`GET {API_BASE}/api/DriversListOfBids?driverId=0000000001&page=1&pageSize=6`

환경: 프론트 `VITE_API_BASE_URL`, 미설정 시 보통 `http://127.0.0.1:8080`.

### 4.2 성공 응답 `200` — JSON

최상위 **`DriversListOfBids`** 객체:

```json
{
  "DriversListOfBids": {
    "screenId": "DriversListOfBids",
    "items": [
      {
        "resId": "0000000001",
        "reqId": "0000000001",
        "reqBusSeq": 1,
        "travelerId": "0000000002",
        "driverId": "0000000003",
        "busId": "0000000001",
        "driverBiddingPrice": 350000,
        "dataStat": "BIDDING",
        "statusLabelKo": "응찰 등록",
        "statusCategory": "bidding",
        "quoteRequestDt": "2026-05-01T12:00:00.000Z",
        "regDt": "2026-05-02T08:00:00.000Z",
        "tripTitle": "제주 패키지",
        "tripStartDt": "2026-05-10T09:00:00.000Z",
        "tripEndDt": "2026-05-12T18:00:00.000Z"
      }
    ],
    "page": 1,
    "pageSize": 6,
    "total": 42
  }
}
```

- **`total`:** 필터·조인 **전체 건수**(COUNT), 페이지와 무관.
- **`items`:** 현재 `page` / `pageSize` 만큼 잘린 배열.

### 4.3 오류 응답

| HTTP | 조건 | 본문 힌트 |
|------|------|-----------|
| 400 | `driverId` 없음, 또는 page/pageSize 비정상 | `error` 문자열, `DriversListOfBids: { screenId }` |
| 500 | DB 기타 오류 | `error`, `DriversListOfBids: { screenId }` |

---

## 5. 프론트엔드 구현 체크리스트

### 5.1 파일·컴포넌트

- **경로:** `busTaams_web/src/components/DriversListOfBids/DriversListOfBids.jsx`
- **export:** `default` 컴포넌트, **`export const SCREEN_ID = 'DriversListOfBids'`**

### 5.2 Props

| Prop | 타입 | 필수 | 설명 |
|------|------|------|------|
| `open` | boolean | 예 | 모달 표시 여부 |
| `onClose` | function | 아니오 | 닫기 콜백 |
| `driverId` | string | 예(표시 시) | API `driverId` — trim 후 빈 문자열이면 호출 생략 가능 |

### 5.3 상태·네트워크

- **`page`:** 모달 `open` 시 **1**으로 리셋.
- **`pageSize`:** 현 구현 **6**(고정 state).
- **`fetchList`:** `open && driverId` 일 때  
  `GET ${API_BASE}/api/DriversListOfBids?driverId&page&pageSize`
- 응답에서 **`j.DriversListOfBids`** 를 페이로드로 저장.
- **`total` / `pageSize`로 `pageCount` 계산**, 페이지 버튼: 최대 **5개 번호** 슬라이딩 윈도우(현 구현과 동일하면 됨).

### 5.4 테이블 컬럼(표시 규칙)

| 헤더 | 데이터 소스 | 포맷 |
|------|-------------|------|
| 상태 | `statusLabelKo` / `dataStat` | 뱃지 |
| 견적 요청일 | `quoteRequestDt` | **YYYY-MM-DD** (로컬 날짜 파싱 주의) |
| 응찰 등록일 | `regDt` | **YYYY-MM-DD** |
| 여행제목 | `tripTitle` | truncate |
| 출발일 / 도착일 | `tripStartDt` / `tripEndDt` | **YYYY-MM-DD** |
| 운임 가격 | `driverBiddingPrice` | **₩** + `toLocaleString('ko-KR')` |
| (액션) | — | **청약취소** 버튼 |

- 빈 목록: **「응찰 내역이 없습니다.」** (문구는 제품 정책에 따라 「청약 내역…」으로 바꿀 수 있음.)
- 로딩: 1행 colspan 메시지.

### 5.5 「청약취소」 활성 조건

- **`dataStat`** 가 **`BIDDING`** 또는 **`CONFIRM`** (대소문자 무시 비교 권장).

### 5.6 `CancellationOfBid` 연동

**`setCancelBid({ resId, reqId, reqBusSeq, travelerId, driverId, busId })`** 후 자식 모달 `open={Boolean(cancelBid)}`.

| CancellationOfBid prop | 값 |
|------------------------|-----|
| `driverId` | `cancelBid.driverId` 또는 목록용 `sid` |
| `reqId`, `resId`, `reqBusSeq` | 행 데이터 (`reqBusSeq ?? 1`) |
| `travelerId`, `busId` | 행 또는 빈 문자열 |
| `onSuccess` | 서브모달 닫기 + **`fetchList()`** |

청약 취소 API·스냅샷· multipart는 **[`버스기사 응찰 취소.md`](./버스기사%20응찰%20취소.md)** (화면 카피는 「버스 청약 취소」) 참고.

### 5.7 앱에서의 진입

- **`App.jsx`:** `showDriversListOfBids` state, **`DriversListOfBids`** 렌더.
- **`driverId={user?.custId || user?.userId}`**
- **`DriverDashboard`** 에서 **`onDriversListOfBids`** 콜백으로 `setShowDriversListOfBids(true)` 연결.

---

## 6. 백엔드 구현 체크리스트

### 6.1 파일

- **`busTaams_server/routes/driversListOfBids.js`**
  - `createDriversListOfBidsHandler(pool)` → `async function DriversListOfBids(req, res)`
  - `registerDriversListOfBids(app, pool)` → **`app.get('/api/DriversListOfBids', DriversListOfBids)`**
  - exports: 모듈 default 또는 `module.exports = registerDriversListOfBids`, 부가로 `SCREEN_ID`, `API_PATH`, `createDriversListOfBidsHandler` 노출 가능.

### 6.2 `server.js` 등록 순서

```javascript
require('./routes/driversListOfBids')(app, pool);
require('./routes/cancellationOfBid')(app, pool);
```

- **반드시 `cancellationOfBid` 보다 앞**에 둔다(주석 정책). multer·다중 파트 라우트와의 충돌을 피하기 위함.

### 6.3 트랜잭션

- 현재 목록 API는 **단일 연결**에서 SELECT만 수행; 트랜잭션 필수는 아님.

---

## 7. 구현 완료 검증 시나리오

1. `driverId` 없이 호출 → **400**.
2. `DATA_STAT` 만 있는 DB → `DATA_STAT` 경로로 목록 정상.
3. `RES_STAT` 만 있는 DB → 첫 시도 실패 후 재시도로 목록 정상.
4. `CONFIRM` + **오늘 출발** 건 → 응답 `items` / `total` 에 **없음**.
5. `BIDDING` / `CONFIRM`(오늘 출발 아님) → 포함, **RES_ID 내림차순**.
6. 프론트: 두 번째 페이지 이동 후 모달 재오픈 → **page 1**.
7. 청약 취소 성공 후 목록 **갱신** 확인.

---

## 8. 관련 문서

- [`버스기사 응찰 취소.md`](./버스기사%20응찰%20취소.md) — `POST /api/CancellationOfBid`, `TB_BUS_RESERVATION` 청약(응찰) 취소
- [`운전기사 MainDashBoard.md`](./운전기사%20MainDashBoard.md) — 대시보드·퀵메뉴 배선
- [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) — 테이블·컬럼 정본

---

**변경 이력 (요약)**

| 일자 | 내용 |
|------|------|
| 2026-05-07 | 문서 전면 재작성: 「청약」 명칭 통일, API/응답 JSON/프론트·백엔드·마운트·검증 시나리오까지 단일 정본화. |
