# 버스 여행 완료 상세 화면 (`BusOperationCompletionDetails`)

**스키마 정본:** [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) — `TB_BUS_RESERVATION`, `TB_AUCTION_REQ`, `TB_AUCTION_REQ_BUS`, `TB_AUCTION_REQ_VIA`, `TB_USER`. API·응답 식별자는 **`TRAVELER_ID`**, **`DRIVER_ID`**, **`RES_ID`**, **`REQ_ID`** 및 요청 버스 복합키 **`REQ_ID` + `REQ_BUS_SEQ`** 기준이다.

---

## 0. 식별자·필드 매핑 (DB ↔ REST/JSON)

| 구분 | DB (정본) | JSON / 쿼리 |
|------|-----------|-------------|
| 기사(예약) | `TB_BUS_RESERVATION.DRIVER_ID` | `driverId` / `userId` / `driverUuid` 중 1 (`resolveCustIdByUserKey`) |
| 완료 예약 | `RES_ID` | `resId` |
| 견적 마스터 | `TB_AUCTION_REQ.REQ_ID` | `reqId` |
| 여행자 연계 | `TB_BUS_RESERVATION.TRAVELER_ID`, `TB_AUCTION_REQ.TRAVELER_ID` + `TB_USER` (`CUST_ID`·`USER_NM` 등) | 응답 `travelerName` (복호화) |
| 견적 버스 | `TB_AUCTION_REQ_BUS`: `REQ_ID`, `REQ_BUS_SEQ`, `BUS_TYPE_CD`, 금액·대수 관련 컬럼 | `auctionReqBuses[]` |
| 경유·타임라인 | `TB_AUCTION_REQ_VIA`: `REQ_ID`, `VIA_SEQ`, `VIA_TYPE`, `VIA_ADDR` | `timeline[]` — 요소에 `viaOrd`(정렬·표시용 순서, 원천은 `VIA_SEQ` 등 서버 매핑) |
| 상태 | `TB_BUS_RESERVATION.DATA_STAT` = `'DONE'` | `dataStat` |

**채번:** `RES_ID`·`REQ_ID` 등 `varchar(10)` 0패딩은 설계 문서 채번 규칙을 따른다.

---

## 1. 식별자(코드)

| 구분 | 경로 |
|------|------|
| **프론트** | `busTaams_web/src/components/BusOperationCompletionDetails/BusOperationCompletionDetails.jsx` |
| **백엔드** | `busTaams_server/routes/busOperationCompletionDetails.js` — `registerBusOperationCompletionDetails(pool, app)` |
| **REST** | `GET /api/bus-operation-completion-details` |
| **등록** | `busTaams_server/server.js` — `registerBusOperationCompletionList` / `registerBusOperationCompletionDetails` 연속 호출 (**`router.js` 미사용**) |

---

## 2. 개요

| 항목 | 내용 |
|------|------|
| **화면명** | 버스 여행 완료 상세 정보 (모달 상단 제목) |
| **쉘** | `fixed inset-0 z-[120]`, 반투명 배경, 좌 제목 · 우 닫기(X) |
| **접근** | 버스 여행 완료 **목록** 행 선택 → `driverId` + `resId` (필요 시 `reqId`) — [`버스 여행 완료 목록 화면.md`](./버스%20여행%20완료%20목록%20화면.md) |

---

## 3. 프론트엔드

| 파일 | 역할 |
|------|------|
| `BusOperationCompletionDetails.jsx` | 상세 모달, 상세 API 호출 |
| `vehicleLabels.js` | `getVehicleLabel` — `BUS_TYPE_CD` 표시 |
| `BusOperationCompletionList.jsx` | 목록에서 `driverId`·`resId`·`reqId` 전달 |

### 3-1. 레이아웃 (요약)

- **좌:** 「여행자 등록 정보」+「요청 차량」
- **우:** 「왕복 운행 타임라인」(`timeline[]` — `START_NODE` / `END_NODE` 등 강조)

### 3-2. 표시 데이터

| 화면 라벨 | 데이터 |
|-----------|--------|
| 여행자 성명 | `travelerName` |
| 탑승 인원 | `passengerCnt` — `PASSENGER_CNT` |
| 여정 제목 | `tripTitle` |
| 출발/도착 일시 | `startDt`, `endDt` |
| 여행자 요청 금액 | `travelerRequestKrw` — 마스터 `REQ_AMT` 또는 버스 행 합산 등 서버 규칙 |
| 버스기사 금액 | `driverBidKrw` — `DRIVER_BIDDING_PRICE` |
| 요청 차량 | `auctionReqBuses[]` |

**비표시:** `startAddr` / `endAddr`·지도·결제 상세 등은 본 API 범위 밖.

### 3-3. Props

`open`, `onClose`, **`driverId`**, **`resId`**, **`reqId`**.

---

## 4. REST API

| **GET** | `/api/bus-operation-completion-details` |
| **구현** | `busTaams_server/routes/busOperationCompletionDetails.js` |

### 4-1. Query

| 파라미터 | 필수 | 설명 |
|----------|------|------|
| `driverId` | 1개 필수* | `userId`·`driverUuid` 동의어 |
| `userId` | ↑ | |
| `driverUuid` | ↑ | |
| `resId` | `resId` 또는 `reqId` | `TB_BUS_RESERVATION.RES_ID` |
| `reqId` | 중 하나 | `TB_AUCTION_REQ.REQ_ID` |

\* `driverId`·`userId`·`driverUuid` 중 **하나**.

**400:** 기사 식별·`resId`/`reqId` 누락.  
**404:** `DONE` + 기사·키 일치 행 없음.

### 4-2. Response 200 (필드)

| 필드 | 설명 |
|------|------|
| `travelerName` | 여행자 표시명 |
| `tripTitle` | `TRIP_TITLE` |
| `startDt`, `endDt` | 일시 |
| `passengerCnt` | 인원 |
| `travelerRequestKrw`, `driverBidKrw` | 금액(원) |
| `auctionReqBuses` | 요청 버스 배열 |
| `timeline` | 경유·노드 배열 (`viaType`, `viaAddr`, `lineText`, `nodeKind` 등) |
| `resId`, `reqId`, `dataStat` | 식별·상태 |

---

## 5. BackEnd / `server.js`

- **SELECT만** 수행 (목록·상세 공통).
- **공용:** `lib/resolveCustIdByUserKey.js`.

구현 세부(폴백·에러 코드)는 **`busOperationCompletionDetails.js`** 소스를 따른다.

---

## 6. 문제 해결

| 증상 | 점검 |
|------|------|
| **Cannot GET /api/bus-operation-completion-details** | `server.js` 등록, 서버 재시작, `VITE_API_BASE_URL` |
| **404** | 목록과 동일 `driverId`, 응답의 `resId`/`reqId` 사용 |
| **VIA 오류** | `TB_AUCTION_REQ_VIA` DDL — 정본 컬럼 `REQ_ID`, `VIA_SEQ`, `VIA_TYPE`, `VIA_ADDR` |

---

*정본 스키마: BusTaams_Project 테이블 설계.*
