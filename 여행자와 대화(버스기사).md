# 여행자와 대화 (CommonLiveChat)

버스기사 대시보드 **「여행자와 대화」** 에서 열리는 **실시간 채팅 모달**의 화면·데이터·REST 정책 정본이다.  
**기능·화면의 영문 식별자(screenId·API 네임스페이스):** **`CommonLiveChat`** — 프론트·백·REST·`server.js` 에서 동일하게 사용한다. (구 `LiveChatBusDriver` / `/api/live-chat-bus-driver` 는 폐지.)

**테이블·컬럼 정본:** [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) 의 `TB_CHAT_LOG`, `TB_CHAT_LOG_PART`, `TB_CHAT_LOG_HIST`, `TB_USER`, `TB_BUS_RESERVATION`, `TB_AUCTION_REQ`.  
방 PK·FK 컬럼명은 **`CHAT_LOG_SEQ`** (`tb_chat_log_room_hist_part.sql`).  
**본 문서에는 SQL 예시를 두지 않는다.**

---

## 1. 식별·구성요소

| 항목 | 내용 |
|------|------|
| `screenId` | **`CommonLiveChat`** |
| 프론트 | `busTaams_web/src/components/CommonLiveChat/CommonLiveChat.jsx` |
| REST 베이스 | **`/api/CommonLiveChat`** — `busTaams_server/routes/commonLiveChat.js` |
| 서버 등록 | `server.js` — `app.use('/api/CommonLiveChat', createCommonLiveChatRouter(pool))` |
| **Router** | 별도 `router.js` 없음 — 팩토리 모듈을 `server.js`에서 마운트 |
| 메시지·방 로직 | `busTaams_server/lib/insertTbChatLogMessage.js` |
| 푸시 | `busTaams_server/services/chatPush.js` |

---

## 2. 왼쪽 — 대화 상대 목록 조회

### 2.1 예약 필터 (`TB_BUS_RESERVATION`)

| 조건 | 값 |
|------|-----|
| `DRIVER_ID` | 로그인 사용자의 `CUST_ID` |
| `DATA_STAT` | **`BIDDING`**, **`CONFIRM`** 만 |

### 2.2 조인·이름

- **`TB_AUCTION_REQ`**: `TB_BUS_RESERVATION.REQ_ID` = `TB_AUCTION_REQ.REQ_ID` 로 조인·SELECT.
- **`TB_USER`**: `TB_BUS_RESERVATION.TRAVELER_ID` → **`USER_NM`** (복호화 후) **여행자 이름** 임시 변수.  
  **`TB_BUS_RESERVATION.DRIVER_ID` → `USER_NM`** **기사 이름** 임시 변수.

동일 `(REQ_ID, DRIVER_ID)` 에 예약이 여러 건이면 **한 목록 행**으로 보일 정책(예: `MOD_DT` 최신 `RES_ID` 1건)은 서버 `commonLiveChat.js` 와 UI가 동일해야 한다.

### 2.3 목록 카드에 쓰는 컬럼

| 소스 | 컬럼 |
|------|------|
| `TB_AUCTION_REQ` | `TRIP_TITLE`, `START_ADDR`, `END_ADDR`, `START_DT`, `END_DT` |
| `TB_USER` | 여행자·기사 표시명(위 2.2) |

**날짜 출력 형식:** `START_DT`, `END_DT` 를 **`YYYY-MM-DD (HH:MM)`** (24시간 `HH:MM`).

**제목(라벨) 조합 규칙**

```text
첫 줄 :  TRIP_TITLE (여행자이름)
둘째 줄:  START_ADDR. YYYY-MM-DD (HH:MM)   ← START_DT
셋째 줄:  END_ADDR.   YYYY-MM-DD (HH:MM)   ← END_DT
```

(문서상 `.` 은 구분용; 화면에서는 주소와 일시를 한 줄에 자연스럽게 배치하면 된다.)

### 2.4 방 생성 시점

**`TB_CHAT_LOG`**, **`TB_CHAT_LOG_PART`** 생성은 **왼쪽 목록에서 행을 선택했을 때** 수행한다.

- **선택 직후** `POST /api/CommonLiveChat/ensure-room` 로 방·참가자 행을 준비한다(기존 방이 있으면 재사용).
- **`TB_CHAT_LOG_HIST`**(메시지)는 **최초 전송 시** 등 기록 시점에만 추가한다(빈 방 유지 가능).

---

## 3. 오른쪽 — 대화창

### 3.1 대화방 제목

- 헤더에는 **`TB_CHAT_LOG.CHAT_TITLE`** 을 표시한다.
- 방 **최초** 개설 시 `CHAT_TITLE` 은 **`TB_AUCTION_REQ.TRIP_TITLE`** 로 채운다.
- 이후 개설자가 제목을 바꾸는 경우 **`TB_CHAT_LOG` UPDATE** — (제목 수정 UI·API는 별도 구현 시 본 정책에 맞출 것).

### 3.2 대화방 개설자(DB만)

- **`TB_CHAT_LOG.CREATED_BY_CUST_ID`** ← 해당 **`TB_BUS_RESERVATION.DRIVER_ID`** (`CUST_ID`).
- **화면에는 출력하지 않는다.**

### 3.3 대화창 썸네일

- **`CHAT_COVER_FILE_ID` 가 없을 때**(또는 방 최초·미설정): 썸네일 영역에 **말풍선 이모티콘**(`💬` 등) 표시.
- **썸네일 변경 UI는 두지 않는다.** (값이 있으면 추후 파일 URL 정책에 따른 표시만 허용.)

### 3.4 메시지 저장

- 개설자(기사)와 상대(여행자)의 대화는 **`TB_CHAT_LOG_HIST`** 에 저장하고, **`TB_CHAT_LOG.LAST_MSG_DT`** 등은 구현 정책에 맞게 갱신한다.

### 3.5 이전 대화 표시

- 이미 **`TB_CHAT_LOG_HIST`** 에 행이 있으면 대화창에 모두 표시한다.
- 목록 조회 후 **스크롤은 맨 아래(가장 최근 메시지)** 가 보이도록 한다.

---

## 4. REST API — `/api/CommonLiveChat`

**공통:** 응답에 `screenId: "CommonLiveChat"` 를 둘 수 있다. 쿼리·바디 필드명의 정본은 `commonLiveChat.js` 이다.

| 메서드 | 경로 | 역할 |
|--------|------|------|
| GET | `/chat-partners?driverId=` | 2.1~2.3 목록 |
| POST | `/ensure-room` | body: `driverId`, `reqId`, `resId` — 2.4 방·참가자 준비 |
| GET | `/messages?driverId=&reqId=&resId=&afterHistSeq=` | 메시지 목록 + `CHAT_TITLE` / `CHAT_COVER_FILE_ID` 메타 |
| POST | `/messages` | body: `driverId`, `reqId`, `resId`, `msgBody` — 메시지 저장 |

**여행자 클라이언트** 대칭 API는 별도 **`/api/live-chat-traveler`** (`liveChatTraveler.js`) 로 유지할 수 있다(동일 `CHAT_LOG_SEQ` 스키마 전제).

---

## 5. 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-05-07 | **`CommonLiveChat`** 로 통일. 목록 필터 `BIDDING|CONFIRM`, 목록 표시 형식, `ensure-room`, `CHAT_LOG_SEQ`, 개설자·제목·썸네일 정책 반영. `LiveChatBusDriver` 폐지. |

---

*구현 갱신 시 `CommonLiveChat.jsx`, `commonLiveChat.js`, `insertTbChatLogMessage.js` 를 함께 맞춘다.*
