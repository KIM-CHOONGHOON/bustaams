# 버스기사 신용카드 등록 화면 (`BusDriverCreditCardRegistration`)

기사가 **카드 및 월회비** 흐름에서 결제수단을 추가할 때 사용하는 모달 화면입니다. 화면·DOM·JSON·서버 상수가 모두 동일한 식별 문자열 **`BusDriverCreditCardRegistration`** 을 사용합니다.

| 구분 | 값 |
|------|-----|
| **컴포넌트(영문)** | `BusDriverCreditCardRegistration` |
| **프론트 파일** | [`busTaams_web/src/components/BusDriverCreditCardRegistration/BusDriverCreditCardRegistration.jsx`](./busTaams_web/src/components/BusDriverCreditCardRegistration/BusDriverCreditCardRegistration.jsx) |
| **프론트 상수** | `BUS_DRIVER_CREDIT_CARD_REGISTRATION_SCREEN_ID === 'BusDriverCreditCardRegistration'` (컴포넌트 파일 `export`) |
| **DOM `id` · `data-screen-id`** | `BusDriverCreditCardRegistration` |
| **별칭 입력 `id` · `name`** | **`CARD_NICKNAME`** (`TB_PAYMENT_CARD.CARD_NICKNAME` 에 매핑) |
| **REST 경로** | `POST /api/bus-driver-credit-card-registration` |
| **응답 최상위 키** | `BusDriverCreditCardRegistration` (객체), 성공 시 갱신된 `BillingSubscription` 동시 반환 |
| **서버 상수** | [`busTaams_server/lib/busDriverCreditCardRegistrationId.js`](./busTaams_server/lib/busDriverCreditCardRegistrationId.js) 의 `BUS_DRIVER_CREDIT_CARD_REGISTRATION_ID` |
| **등록 비즈니스 로직** | [`busTaams_server/lib/busDriverCreditCardRegistration.js`](./busTaams_server/lib/busDriverCreditCardRegistration.js) |
| **라우트 등록** | [`busTaams_server/server.js`](./busTaams_server/server.js) — `express.json` 직후 [`busDriverCreditCardRegistrationRoute.js`](./busTaams_server/routes/busDriverCreditCardRegistrationRoute.js) 마운트 |

---

## 1. 카드번호·CVC·유효기간 검증

### 1.1 카드 번호 — Luhn 알고리즘 (체크섬)

카드 번호 **마지막 한 자리**는, 앞선 숫자들을 특정 공식으로 계산했을 때 맞는지 확인하는 **체크섬(Check Digit)** 입니다. 이를 **Luhn 알고리즘**(모듈러스 10, **ISO/IEC 7812** 계열에서 널리 사용)이라고 하며, **PG·카드사 서버와 통신하지 않아도** 번호가 **유효한 형식인지** 곧바로 판별할 수 있습니다.

| 항목 | 설명 |
|------|------|
| **동작 원리** | 오른쪽부터 **한 자리 걸러**(두 번째 자리마다) **2를 곱**하고, 그 결과가 **10 이상이면 각 자릿수를 더해** 한 자리로 만듭니다. 이렇게 처리한 **모든 자리의 합**이 **10의 배수**여야 합니다. |
| **용도 (본 화면)** | 사용자가 카드 번호를 **잘못 입력(오타)** 했을 때, **서버 요청 전에** 입력칸에 **빨간 테두리·안내 문구**로 경고합니다. **16자리를 모두 입력한 순간**에는 체크섬을 바로 적용하고, **13~15자리** 등 짧은 번호는 **필드 포커스를 벗어난 뒤**에 검사해, 입력 중 불필요한 경고를 줄입니다. |
| **구현 위치** | **프론트:** [`BusDriverCreditCardRegistration.jsx`](./busTaams_web/src/components/BusDriverCreditCardRegistration/BusDriverCreditCardRegistration.jsx) 의 `luhnValid` (`digits` 13~19자, 본 UI는 4×4칸으로 **최대 16자리**까지 입력). **서버:** [`busDriverCreditCardRegistration.js`](./busTaams_server/lib/busDriverCreditCardRegistration.js) 의 동일 로직으로 재검증. |

Luhn 은 **번호 형식·오타 검출**용이며, **해당 PAN이 실제로 발급·유효한 카드인지**, **CVC·만료일이 그 카드와 일치하는지**는 **카드사·PG** 만이 확정할 수 있습니다.

### 1.2 CVC·유효기간·카드사 정합성

| 검증 종류 | 구현 | 설명 |
|-----------|------|------|
| **카드사별 PAN+CVC+만료 “발급 원장” 일치** | **아니오** | 네트워크·발급사와 직접 연동된 검증은 하지 않습니다. |
| **Luhn** | **예** | 위 **1.1** (클라이언트 즉시 피드백 + 서버 재검증). |
| **PAN 길이** | **예** | Luhn 전제: 숫자만 **13~19자리**(화면은 최대 16자리 입력). |
| **유효기간(월/년)** | **예** | 달력상 만료 여부(서버 정책). |
| **CVC/CVV** | **길이만** | **저장하지 않음.** 전달 시 **3~4자리**만 허용. **실제 일치**는 **PG 승인** 단계에서 수행하는 것이 일반적입니다. |

**정리:** 본 화면만으로 “카드사에 등록된 카드와 **CVC·만료가 완전히 일치**한다”는 검증은 불가합니다. 운영에서는 **PG 카드등록·토큰화**를 권장합니다.

---

## 2. UI 개요

- **제목:** 카드 정보 및 자동 결제 설정  
- **배치:** 데스크톱 기준 좌측 약 1/3(카드 미리보기·보안 안내), 우측 약 2/3(입력 폼).  
- **색상:** 포인트 다크 그린 약 `#004D40`, 입력·박스 배경 약 `#F2F2F2`.  
- **좌측 카드 미리보기:** 칩 그래픽, **카드 별칭**(`CARD_NICKNAME`), `busTaams` 로고, 번호·소유자명/만료일 반영.  
- **보안 안내:** TLS·암호화·CVV 미저장·운영 시 PG 토큰 권장 문구.  
- **우측 필드(위→아래 순):**  
  1. **카드 별칭** — DOM **`id` / `name` = `CARD_NICKNAME`**, 최대 50자, `TB_PAYMENT_CARD.CARD_NICKNAME` 에 저장.  
  2. 카드번호 4구간(각 4자리) — **Luhn 실패 시 빨간 테두리·체크섬 안내 문구** (§1.1)  
  3. 카드 소유자 성명(미리보기용, DB 별도 컬럼 없음)  
  4. 유효기간(MM/YY), CVV  
  5. **기본 결제 수단** 토글  
  6. 제출 버튼 문구: **「신용 카드 등록」**  

### 중복 카드 안내 모달

동일 `CUST_ID` 로 **이미 등록된 카드 번호**(복호화 후 PAN 동일)를 다시 등록하려 하면 HTTP **409** 및 문구:

**「이미 등록된 카드 번호 입니다. 다른 카드 번호를 입력하세요!」**

를 **안내 모달**로 띄우고, **닫기** 로만 닫습니다.

---

## 3. 진입 경로

- [`BillingSubscription`](./busTaams_web/src/components/BillingSubscription/BillingSubscription.jsx) 모달 **결제 수단** 영역의 **「새 카드 등록」** 버튼.  
- `driverId`(세션의 `custId` 또는 `userId`)가 있을 때만 활성화됩니다.

---

## 4. `TB_PAYMENT_CARD` 신규 등록 절차

### 4.1 중복 여부

- 요구사항 상 `CUST_ID` 와 **입력 카드번호**로 기존 행을 찾는 설명이 있으나, `CARD_NO_ENC` 는 **AES-256-GCM** 이며 **IV(초기화 벡터)가 매번 달라져** 같은 PAN 이라도 **암호문 문자열이 매번 다릅니다.**  
- 따라서 **`WHERE CARD_NO_ENC = :입력암호문` 형태의 동등 검색은 사용하지 않습니다.**  
- 구현: 해당 `CUST_ID` 의 모든 `CARD_NO_ENC` 를 조회한 뒤 **복호화**하여 PAN(숫자만)을 비교하고, **일치하는 행이 있으면 중복(409)** 처리합니다.

### 4.2 신규 행 컬럼 조립 (구현 기준)

| 컬럼 | 조립 규칙 |
|------|-----------|
| **`CUST_ID`** | 로그인한 버스기사의 `TB_USER.CUST_ID` (`driverId` 로 사용자 조회 후 결정). |
| **`CARD_SEQ`** | 동일 `CUST_ID` 에서 `MAX(CARD_SEQ)+1` (1부터 증가). |
| **`CARD_NICKNAME`** | 모달 **`CARD_NICKNAME`** 필드 값, 앞뒤 공백 제거 후 최대 50자. **비어 있으면 400.** |
| **`CARD_NO_ENC`** | 모달에 입력한 카드번호(PAN)를 [`crypto.js`](./busTaams_server/crypto.js) `encrypt()` 로 암호화한 문자열. |
| **`EXP_MONTH`** | 모달 유효기간 **월** (두 자리 `MM`). |
| **`EXP_YEAR`** | 모달 유효기간 **연도** 두 자리 `YY`. |
| **`IS_PRIMARY`** | 모달 **기본 결제 수단** 토글이 켜져 있으면 `Y`, 꺼져 있으면 `N`. **등록 카드가 한 장도 없으면** 첫 카드는 메인으로 저장(`Y`). 메인으로 지정 시 동일 `CUST_ID` 의 다른 카드 `IS_PRIMARY` 는 `N` 으로 갱신. |
| **`AUTO_PAY_START_DT`** | **`IS_PRIMARY = 'Y'`** 일 때만 **등록 시각(서버 현재 시각)** 을 설정. 비메인이면 `NULL`. |
| **`AUTO_PAY_END_DT`** | 신규 INSERT 시 **`NULL`.** DB `CHK_TB_PAYMENT_CARD_AUTO_PAY_RANGE` 제약상 `AUTO_PAY_END_DT` 가 `NULL` 이 아니면 `AUTO_PAY_START_DT` 가 필수이므로, “비메인 등록 시 종료 시각만 기록” 패턴은 이 스키마와 맞지 않아 사용하지 않음. *(기존 메인 카드 ‘해제’ 시각이 필요하면 **갱신(UPDATE)** 시나리오에서 별도 설계.)* |
| **`AUTO_PAY_DAY`** | 항상 **`1`**. |
| **`REG_ID`** | `TB_USER.USER_ID` 앞 10자(없으면 `CUST_ID` 등 보조). |

---

## 5. REST API — `POST /api/bus-driver-credit-card-registration`

| 항목 | 설명 |
|------|------|
| **Method / URL** | `POST /api/bus-driver-credit-card-registration` |
| **Content-Type** | `application/json` |

### 요청 본문

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `driverId` | string | 예 | `TB_USER.CUST_ID` 또는 `TB_USER.USER_ID` |
| **`CARD_NICKNAME`** | string | 예 | 카드 별칭 → `TB_PAYMENT_CARD.CARD_NICKNAME` |
| `cardNumber` | string | 예 | 카드번호 숫자(Luhn·길이 검사) |
| `expMonth` | string | 예 | `MM` |
| `expYear` | string | 예 | `YY` |
| `setAsDefault` | boolean | 아니오 | 기본 결제 수단(메인 카드) |
| `cvv` | string | 아니오 | 3~4자리만 허용. **저장·로깅 없음** |

### 성공 응답 `200`

```json
{
  "BusDriverCreditCardRegistration": {
    "screenId": "BusDriverCreditCardRegistration",
    "success": true,
    "custId": "…",
    "cardSeq": 1
  },
  "BillingSubscription": { }
}
```

`BillingSubscription` 은 [`GET /api/billing-subscription`](./카드및월회비.md) 과 동형입니다.

### 오류

| 코드 | 조건 |
|------|------|
| `400` | `driverId`/별칭/카드번호/유효기간/CVV 형식 오류, Luhn 실패 등 |
| `404` | `TB_USER` 미조회 |
| **`409`** | **동일 `CUST_ID` 에 동일 PAN 이미 등록** |
| `503` | `TB_PAYMENT_CARD` 테이블 없음 등 |

---

## 6. 보안·운영 참고

- PAN 은 **암호화 저장**, CVV 는 **저장하지 않음.**  
- 카드사·PG 수준의 **실제 승인 전 검증**은 이 API 를 **PG 토큰 발급**으로 교체할 때 수행하는 것이 맞습니다.

## 7. UI 레퍼런스 이미지

`assets/image-68b10192-d115-47ae-b9fa-fb6c8cca4838.png` (레이아웃 참고용)
