# 버스탐스 월 카드 결재

기사 월 회비를 **등록 카드로 자동 결제**하기 위한 설계·운영 메모입니다.  
원본 참고: Downloads 폴더의 `카드 월 결제 처리.md`(타 프로젝트의 포트원/Iamport 정기결제·배치 구현 정리). 여기서는 **버스탐스에 그대로 옮길 수 있는 개념**만 추려서 반영합니다.

---

## 1. 버스탐스 문서·코드와의 대응

| 구분 | 상세 |
|------|------|
| 화면·API 요약 | [`카드및월회비.md`](./카드및월회비.md) |
| 카드 등록 화면·유효성·PAN 저장 | [`버스기사 신용카드 등록 화면.md`](./버스기사%20신용카드%20등록%20화면.md), `busTaams_server/lib/busDriverCreditCardRegistration.js` |
| 테이블 정의 | [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) — `TB_PAYMENT_CARD`, `TB_DRIVER_PAYMENT_HIST`, `TB_SUBSCRIPTION` 등 |
| 빌링 스크린 페이로드 | `GET /api/billing-subscription`, `PATCH /api/billing-subscription/primary-card` — `busTaams_server/lib/billingSubscriptionPayload.js` |

**현재 상태(요약)**  
- 카드는 `TB_PAYMENT_CARD`에 저장되며, 월 자동이체와 관련된 **`AUTO_PAY_DAY`, `AUTO_PAY_START_DT`, `AUTO_PAY_END_DT`** 및 메인 카드(`IS_PRIMARY`)가 있습니다.  
- 결제 이력은 `TB_DRIVER_PAYMENT_HIST` 등으로 적재하는 방향이 문서·DDL과 맞습니다.  
- **PG 빌링키로 매달 승인을 치는 배치(스케줄러)** 는 참고 문서에 있는 것처럼 별도 프로세스로 두는 것이 일반적이며, 버스탐스 저장소 기준으로는 **그 배치 본문은 아직 타 프로젝트 수준의 참고 사항**으로 둡니다. 연동 시 KG 이니시스·포트원 등 선택은 [`카드및월회비.md`](./카드및월회비.md) §9와 정합시키면 됩니다.

---

## 2. 참고 문서에서 버스탐스에 적용할 내용

### 2.1 빌링키와 “목록용 카드” 분리

- PG 정기결제는 **빌링키(또는 customer_uid 등)** 가 있어야 매달 `again`/재결제 API로 승인할 수 있습니다.  
- **카드 번호만 저장·목록만 관리**하는 행과, **실제로 빌링에 묶인 카드**를 구분하는 설계가 안전합니다.  
- 버스탐스에서는 테이블 설계상 `CARD_BILLING_KEY_ENC`(또는 PG별 토큰 컬럼) 확장·운영 규칙을 정하면 참고 문서의 “빌링 연동 카드 vs 단순 CRUD 카드” 구분과 동일한 효과를 낼 수 있습니다.

### 2.2 정기 승인 플로우(연동 후)

참고 문서의 흐름을 버스탐스 관점으로만 옮기면 다음과 같습니다.

1. **대상 선별**: 자동이체 기간(`AUTO_PAY_START_DT` ~ `AUTO_PAY_END_DT`) 안이고, 결제일(`AUTO_PAY_DAY`)이 오늘과 일치하며, 메인 카드·빌링키가 유효한 기사.  
2. **금액 결정**: 월 회비는 `TB_COMMON_CODE`(FEE_POLICY 등) 또는 정책 테이블과 동일 규칙으로 산정 — `billingSubscriptionPayload.js`의 `resolveMonthlyFeeKrw`와 맞출 것.  
3. **PG 승인**: 선택한 PG의 재결제·정기결제 API 호출.  
4. **결과 저장**: 성공 시 `TB_DRIVER_PAYMENT_HIST`(및 카드·구독 관련 테이블)에 반영, 실패 시 실패 코드·메시지 보존.  
5. **아이템포턴시**: 동일 기사·동일 청구월(또는 동일 `merchant_uid`)에 대해 **중복 승인·중복 이력**이 나지 않도록 유니크 제약 또는 선조회 후 스킵.

### 2.3 배치·스케줄

- 참고 문서는 **일 단위 배치**(예: 매일 특정 시각)로 “오늘이 결제일인 건만” 처리합니다.  
- 버스탐스도 Node 환경에서는 **cron / 워커 프로세스 / 외부 스케줄러** 중 하나로 동일 패턴을 적용하면 됩니다.  
- **타임존·영업일**: 서버 TZ와 “결제일이 주말·공휴일일 때 전영업일로 당기기” 같은 규칙이 있으면 배치 쿼리와 함께 문서화합니다.

### 2.4 웹훅 vs 동기 응답

- PG가 **비동기 상태**(가상계좌·일부 카드사)를 줄 수 있으면 웹훅으로 최종 상태를 맞추는 패턴이 참고 문서와 같습니다.  
- 카드 즉시 승인만 쓰면 동기 응답만으로도 가능하나, **최종 상태는 DB 한 곳**으로 모으는 원칙은 동일합니다.

### 2.5 설정·식별자

- 상점 ID, 시크릿, 빌링 최소 간격 등은 **코드 상수에 박지 않고** 환경변수·설정 테이블로 분리하는 것이 참고 문서와 같습니다.  
- 매 결제마다 **고유 주문번호(`merchant_uid` 등)** 를 생성해 PG와 이력 테이블에 동일하게 남기면 추적·멱등 처리에 유리합니다.

### 2.6 실패·재시도·알림

- 일시 오류와 한도 초과·카드 정지 등 **재시도 가능 여부**를 구분합니다.  
- 사용자 알림(앱 푸시·SMS)과 관리자 모니터링은 정책에 맞게 [`카드및월회비.md`](./카드및월회비.md)의 화면 메시지·결제 실패 UX와 연계합니다.

### 2.7 구독·노출 기간과의 연계

- 참고 문서의 “결제 성공 시 이용 만료일 연장”에 대응하는 것이 버스탐스에서는 **`TB_SUBSCRIPTION`**, 로그인 후 노출되는 구독 정보(`loginPayload` 등)와의 일관성입니다.  
- 월 결제 배치가 들어가면 **결제 성공 → 구독월/상태 갱신**을 한 트랜잭션 또는 명확한 순서로 묶는 것을 권장합니다.

---

## 3. 구현 체크리스트(버스탐스용)

참고 문서의 운영 체크 항목을 이 저장소 기준으로 줄인 목록입니다.

- [ ] 자동이체 대상 SQL: `AUTO_PAY_*`, `IS_PRIMARY`, 빌링키 유무, 구독/중복결제 제외 조건 검증  
- [ ] 월 회비 금액이 화면(`billing-subscription`)과 배치에서 **동일 규칙**인지 확인  
- [ ] PG 연동 후 실패 코드 매핑 및 `TB_DRIVER_PAYMENT_HIST`(및 카드 결제 이력 테이블) 컬럼 정합  
- [ ] 동일 청구 주기·동일 주문번호 중복 방지  
- [ ] 웹훅 URL·서명 검증·멱등 처리(있을 경우)  
- [ ] 로그에 카드 전번·빌링키 평문 금지, 기존 AES PAN 처리 방침 준수  

---

## 4. 타 프로젝트 전용으로 버스탐스에 직접 복사하지 않은 것

다음은 참고 저장소(Java 서비스명·특정 테이블명 등)에 묶인 내용이라, **이름만 가져오고 구현은 버스탐스에 맞게 새로 짜야** 합니다.

- `CharnelService`, `tb_settlement_regular` 등 **해당 프로젝트 전용 클래스·테이블**  
- 포트원 REST 경로 그대로가 아닌 **실제 선택할 PG SDK/REST** (버스탐스는 KG 이니시스 등으로 바뀔 수 있음)

---

## 5. 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-05-15 | 최초 작성. Downloads `카드 월 결제 처리.md`의 일반 원칙을 버스탐스 테이블·API와 대응하여 반영. |
