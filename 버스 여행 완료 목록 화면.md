# 버스 여행 완료 목록 화면 (`BusOperationCompletionList`)

**스키마 정본:** [`BusTaams_Project 테이블 설계.md`](./BusTaams_Project%20테이블%20설계.md) — 본 화면·API가 다루는 테이블은 `TB_BUS_RESERVATION`, `TB_AUCTION_REQ`, `TB_AUCTION_REQ_BUS`, `TB_USER` 및 (상세 연계에서) `TB_AUCTION_REQ_VIA` 이다.

---

## 0. 식별자·필드 매핑 (정본 컬럼)

| 구분 | DB (정본) | REST/JSON |
|------|-----------|-----------|
| 기사(예약 행) | `TB_BUS_RESERVATION.DRIVER_ID` (`varchar(10)`, `TB_USER.CUST_ID`와 동일 정책) | 쿼리 `driverId` / `userId` / `driverUuid` 중 하나 — `lib/resolveCustIdByUserKey.js`로 `CUST_ID` 정합 |
| 완료 예약 | `TB_BUS_RESERVATION.RES_ID` | `resId` |
| 견적 마스터 | `TB_AUCTION_REQ.REQ_ID` | `reqId` |
| 견적 버스 행 | `TB_AUCTION_REQ_BUS`: `REQ_ID`, `REQ_BUS_SEQ`, `BUS_TYPE_CD`, `TOLLS_AMT`, `FUEL_COST`, `RES_BUS_AMT` 등 | `auctionReqBuses[]` — `reqBusId`, `busTypeCd`, `reqBusCnt`, `reqAmtKrw`, `sortOrder` 등 (서버 매핑) |
| 상태 | `TB_BUS_RESERVATION.DATA_STAT` = `'DONE'` | `dataStat` |
| 완료 시각(표시용) | `TB_BUS_RESERVATION.MOD_DT` | `completedAt` |
| 마스터 표시 필드 | `TB_AUCTION_REQ`: `TRIP_TITLE`, `START_ADDR`, `END_ADDR`, `START_DT`, `END_DT`, `PASSENGER_CNT` 등 | 동명 JSON |

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| **화면** | 버스 여행 완료 목록 (모달 `z-[100]`) |
| **진입** | 기사 MainDashBoard 등 —바로가기 「버스 여행 완료 목록」(연결은 프로젝트에 따름) |
| **상세** | 행 선택 → `BusOperationCompletionDetails` (`z-[120]`) — `driverId`·`resId`·`reqId` 전달 |
| **기간** | `TB_AUCTION_REQ.END_DT` **일자**가 `from`~`to` (포함), 최대 **365일** |
| **정렬** | `END_DT` 내림차순 |

---

## 2. FrontEnd

| 파일 | 역할 |
|------|------|
| `busTaams_web/src/components/BusOperationCompletionList/BusOperationCompletionList.jsx` | From/To·조회·테이블·`driverId`/`driverUuid`로 `GET` 호출 |
| `busTaams_web/src/components/BusOperationCompletionDetails/BusOperationCompletionDetails.jsx` | 상세 모달 — `GET /api/bus-operation-completion-details` |
| `busTaams_web/src/utils/vehicleLabels.js` | `formatAuctionReqBusesLine` |

**목록 → 상세:** `driverId`(또는 props `driverUuid`) = 세션 기사 키, `resId`·`reqId` = 목록 API 응답 값.

---

## 3. REST API

### 3-1. 목록

| | |
|---|---|
| **GET** | `/api/bus-operation-completion-list` |
| **구현** | `busTaams_server/routes/busOperationCompletionList.js` |
| **등록** | `busTaams_server/server.js` — `registerBusOperationCompletionList(pool, app)` (**별도 `router.js` 없음**) |

**Query**

| 파라미터 | 필수 | 설명 |
|----------|------|------|
| `driverId` | 1개 필수* | 기사 식별 — `resolveCustIdByUserKey` 적용 |
| `userId` | ↑ 동의어 | |
| `driverUuid` | ↑ 동의어 | |
| `from` | 예 | `YYYY-MM-DD` |
| `to` | 예 | `YYYY-MM-DD` |

\* `driverId`·`userId`·`driverUuid` 중 **하나**.

**검증 오류 (400):** 날짜 형식, `to < from`, 기간 &gt; 365일 → `조회 범위는 1년 이내만 가능합니다.`

**Response 200 (예시 구조)**

```json
{
  "total": 0,
  "items": [
    {
      "resId": "0000000001",
      "reqId": "0000000001",
      "tripTitle": "string",
      "startAddr": "string",
      "endAddr": "string",
      "endDt": "ISODate",
      "endDtDate": "YYYY-MM-DD",
      "startDt": "ISODate",
      "passengerCnt": 0,
      "driverBiddingPrice": 0,
      "dataStat": "DONE",
      "completedAt": "ISODate",
      "auctionReqBuses": [
        {
          "reqBusId": "string",
          "busTypeCd": "string",
          "reqBusCnt": 1,
          "reqAmtKrw": 0,
          "sortOrder": 1
        }
      ]
    }
  ]
}
```

### 3-2. 상세 (연동)

| **GET** | `/api/bus-operation-completion-details` |
| **구현** | `busTaams_server/routes/busOperationCompletionDetails.js` |

쿼리·응답 필드 요약은 [`버스 여행 완료 상세 화면.md`](./버스%20여행%20완료%20상세%20화면.md) 참고.

---

## 4. BackEnd

- `busTaams_server/lib/resolveCustIdByUserKey.js` — 조회 키 → `TB_USER.CUST_ID` 정합
- `busTaams_server/server.js` — 라우트 등록

**조회 조건(정본):** `TB_BUS_RESERVATION.DATA_STAT = 'DONE'`, `INNER JOIN TB_AUCTION_REQ` (`REQ_ID`), 기간 `DATE(r.END_DT)` between `from` and `to`, `DRIVER_ID` = 해석된 기사 `CUST_ID`.

---

## 5. 문제 해결

- **Cannot GET** — 서버 재기동, `registerBusOperationCompletionList` 등록 여부
- **빈 목록** — `END_DT` NULL 제외, From/To·`DRIVER_ID` 일치, 마스터 조인 누락 시 제외

---

*정본 스키마: BusTaams_Project 테이블 설계. 구현상 폴백·에러 처리는 저장소 내 `busOperationCompletionList.js`를 따른다.*
