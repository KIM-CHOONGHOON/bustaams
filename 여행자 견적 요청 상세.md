# 여행자 견적 상세 모달 — 개발 명세

화면명 **「여행자 견적 상세」**, 컴포넌트 **`TravelerQuoteRequestDetails`**.  
기사가 견적·노선·차량 정보를 **조회**하고 **「청약 정보 등록」**으로 입찰(예약 행 생성/갱신)을 수행한다.

데이터·DDL 정본은 `BusTaams 테이블.md`, `BusTaams_Project 테이블 설계.md`를 참고한다.

---

## 1. 라우팅·백엔드 구조

| 항목 | 설명 |
|------|------|
| **`router.js`** | 본 저장소에는 **별도 `router.js` 없음**. REST는 **`busTaams_server/server.js`** 에 `app.get` / `app.put` 으로 등록된다. |
| **상세·입찰 API** | `GET /api/traveler-quote-request-details`, `PUT /api/traveler-quote-request-details/bid`, `PUT /api/traveler-quote-request-details/bid-cancel` |
| **월건수(청약 후처리)** | `busTaams_server/lib/driverBidMomMember.js` — `applyMomMemberAfterBid` (`bid` 트랜잭션 내 호출) |

---

## 2. 관련 파일

| 구분 | 경로 |
|------|------|
| **모달 본체** | `busTaams_web/src/components/TravelerQuoteRequestDetails/TravelerQuoteRequestDetails.jsx` |
| **목록에서 중첩 진입** | `busTaams_web/src/components/ListOfTravelerQuotations/ListOfTravelerQuotations.jsx` — `onBidInsertSuccess={fetchList}` |
| **백엔드** | `busTaams_server/server.js` (상기 3 엔드포인트) |
| **MOM 회원 건수** | `busTaams_server/lib/driverBidMomMember.js`, SQL: `busTaams_server/sql/tb_mom_member.sql` 등 |

> **참고:** 다른 문서에 `App.jsx`의 `openTravelerQuoteDetail` / `travelerQuoteSel` 언급이 있을 수 있으나, **현재 코드 기준** 상세 모달은 **`ListOfTravelerQuotations` 내부에서만** 직접 렌더된다 (`grep` 기준).

---

## 3. 컴포넌트 Props

| Prop | 타입 | 필수 | 설명 |
|------|------|------|------|
| `reqId` | `string` | 예 | `TB_AUCTION_REQ.REQ_ID` |
| `reqBusSeq` | `number` | 아니오 | `TB_AUCTION_REQ_BUS.REQ_BUS_SEQ`. 유효하지 않으면 기본 **1** |
| `close` | `() => void` | 예 | 상세 모달만 닫기 |
| `currentUser` | `object` | 예 | `custId` 또는 `CUST_ID` — API·입찰용 기사 식별 |
| `onBidInsertSuccess` | `() => void` | 아니오 | **`isNewReservation === true`** 일 때, 약 5초 후·`close()` 직전 호출 (목록 재조회 등) |

---

## 4. 환경 변수

- **`VITE_API_BASE_URL`**: API 베이스, **끝 슬래시 제거** 후 사용.
- 비어 있으면 컴포넌트 기본값 **`http://127.0.0.1:8080`** (상대 경로가 아님).

---

## 5. UI 요약 (현재 구현)

### 5.1 입력

- **조회 전용**: 금액·노선·날짜 등 **직접 입력 필드 없음**.
- **액션**: **「청약 정보 등록」** 단일 버튼 (금액은 서버가 `TB_AUCTION_REQ_BUS`에서 읽어 반영).

### 5.2 z-index

| 요소 | z-index |
|------|---------|
| 메인 상세 오버레이·카드 | `200` |
| 안내 모달 (409·`modalLines` 등) | `310` |
| (참고) 여행자 견적 **목록** 모달 | `150` |

### 5.3 레이아웃

- 다이얼로그: `max-w-6xl`, **`min-h-[600px]`**, `max-h-[95vh]`, 본문 세로 스크롤.
- **좌 8 / 우 4** 그리드 (`lg`).
- **요청 요약 → 노선**: `TB_AUCTION_REQ_VIA`를 `viaSeq` 오름차순, 한 줄 형식  
  - 접두: `출발지 :`, `출발 경유지 :`, `목적지 :`, `도착 경유지 :`, `도착지 :` (그 외 타입은 `${타입} :` / `지점 :`)  
  - **주소만 색**: `START_WAY` → 파랑(`text-blue-600`), `END_WAY` → 빨강(`text-red-600`), 나머지 주소는 `text-gray-900`  
  - 목록 영역: `min-h-[14rem]`, `max-h-72`, **세로 스크롤**
- **경유지 섹션 분리**: 없음 (노선 목록에 통합).
- **차량 및 경유지**: 날짜, 차량 유형, 인원.
- **청약 정보 등록**: `updating || !editOk || !custId || bidLockedAfterMom` 이면 비활성.

### 5.4 접근성

- 메인: `role="dialog"`, `aria-modal="true"`, `aria-labelledby="traveler-quote-detail-title"`.
- 안내: `role="alertdialog"`, `aria-labelledby="auction-gate-title"`.
- **Escape**: 안내 모달 열리면 안내만 닫음 → 그다음 Escape 시 `close()`.

---

## 6. 프론트 로직

### 6.1 `canEditBid(custId, resStat)`

- `custId` 없으면 `false`.
- `resStat` 없음/`''` → `true` (해당 기사 예약 없음으로 간주).
- 그 외 `BIDDING` / `AUCTION` / `REQ` 만 `true`.

### 6.2 `RES_STAT_LABEL` (예약 상태 배지)

`AUCTION`→입찰대기, `BIDDING`→응찰등록, `REQ`→요청, `CONFIRM`→예약확정, `DONE`→운행종료, `TRAVELER_CANCEL`→여행자 취소, `DRIVER_CANCEL`→응찰취소, `BUS_CHANGE`→버스변경, `BUS_CANCEL`→버스취소.

### 6.3 조회

`GET ...?reqId=&custId=&reqBusSeq=` — `custId`·`reqBusSeq`는 있으면 항상 전송.

### 6.4 「청약 정보 등록」 (`PUT .../bid`)

1. **409 + `code === 'REQ_BUS_NOT_AUCTION'`** 또는 **`error`에 `견적 응찰` 포함** → 문자열 안내 모달 (`auctionGateModal`).
2. **HTTP 실패 + `modalLines` 배열** (예: `TB_MOM_MEMBER` 월건수 등) → `modalLines.join('\n')` 동일 모달.
3. **그 외 실패** → `updateError` 인라인.
4. **성공**  
   - 성공 메시지: **`momMember.ackMessage`** 우선, 없으면 `message`.  
   - `momMember.disableFurtherBid === true` → **`bidLockedAfterMom`** 로 버튼 비활성.  
   - **`isNewReservation === true`**: 약 **5초** 후 `onBidInsertSuccess?.()` → `close?.()`.  
   - **아니면**: 약 **3초** 후 성공 배너 제거 + `fetchData()`.

`reqId`/`reqBusSeq` 변경 시 `bidLockedAfterMom` 초기화.

### 6.5 차량 유형 표시

- **`busTypeName`** 이 비어 있지 않으면 표시.
- 없으면 **`여행 🚌`**.
- `busCnt > 1`이면 **` × {busCnt}대`** 접미.

---

## 7. REST API

### 7.1 `GET /api/traveler-quote-request-details`

**Query**

| 파라미터 | 필수 | 설명 |
|-----------|------|------|
| `reqId` | 예 | 마스터 `REQ_ID` |
| `custId` | 아니오 | 기사 `CUST_ID`. 있으면 `TB_BUS_RESERVATION` 조회 |
| `reqBusSeq` | 아니오 | **버스 슬롯** `REQ_BUS_SEQ`. 없거나 무효면 해당 `REQ_ID`의 **최소 `REQ_BUS_SEQ` 행**으로 보정 |

**처리 요약 (서버)**

1. `TB_AUCTION_REQ` 마스터 1건 → 없으면 **404**.
2. **`busSeqResolved`**: 쿼리 `reqBusSeq` 또는 첫 버스 행의 `REQ_BUS_SEQ`.
3. **`TB_AUCTION_REQ_BUS`**: `REQ_ID` + `REQ_BUS_SEQ = busSeqResolved` → **`BUS_TYPE_CD`**.
4. **`TB_COMMON_CODE`**: `GRP_CD = 'BUS_TYPE'`, `DTL_CD = BUS_TYPE_CD`, `USE_YN = 'Y' OR NULL` → **`CD_NM_KO` → `busTypeName`** (없으면 null).
5. **`busCnt`**: 해당 `REQ_ID`의 `TB_AUCTION_REQ_BUS` 행 수 (최소 1).
6. **`TB_BUS_RESERVATION`** (`custId` 있을 때):  
   - `reqBusSeq`가 유효하면 **`REQ_BUS_SEQ` 일치** 행 우선.  
   - 없고 `reqBusSeq !== 0`이면 **`REQ_BUS_SEQ = 0`** 재시도 (**신규 INSERT가 0으로 들어가는 기존 로직과의 호환**).  
   - `reqBusSeq` 쿼리 없으면 `REG_DT DESC` 최신 1건.
7. **`TB_AUCTION_REQ_VIA`**: `REQ_ID`, **`VIA_SEQ ASC`**.

**200 응답 (주요 필드)**

- 마스터: `reqId`, `travelerId`, `tripTitle`, `startAddr`, `endAddr`, `startDt`, `endDt`, `passengerCnt`, `reqStat`, `estTotalServicePrice`, `expireDt`, `regDt`
- 버스/표시: `busType` (= 코드, 호환용), `busTypeCd`, `busTypeName`, `busCnt`
- 예약: `resId`, `driverBiddingPrice`, `resStat`
- `viaPoints[]`: `viaSeq`, `viaType`, `viaAddr`

**오류:** `400` (`reqId` 없음), `404`, `500`.

---

### 7.2 `PUT /api/traveler-quote-request-details/bid`

**Body (JSON)**

| 필드 | 필수 | 설명 |
|------|------|------|
| `reqId` | 예 | 견적 ID |
| `custId` | 예 | 기사 `CUST_ID` |
| `reqBusSeq` | 예 | **0 이상 정수**, `TB_AUCTION_REQ_BUS.REQ_BUS_SEQ` 와 일치해야 함 |

**입찰 금·수수료**는 요청 본문에 없음. 서버가 **`TB_AUCTION_REQ_BUS`** 의 `RES_BUS_AMT`, `RES_FEE_*` 를 읽어 **`TB_BUS_RESERVATION`** 에 반영.

**핵심 순서 (트랜잭션)**

1. `TB_AUCTION_REQ` 확인 (**404** 시 롤백).
2. **`TB_AUCTION_REQ_BUS`** (`REQ_ID`, `REQ_BUS_SEQ`) — 없으면 **404**.
3. **`TB_BUS_RESERVATION`**: 동일 `REQ_ID`+`DRIVER_ID` 최신 1건. 상태가 `BIDDING`/`AUCTION`/`REQ` 이면 **갱신 분기**.
4. **게이트**: 해당 버스 행 **`DATA_STAT === 'AUCTION'`** 아니면 **409**, `code: 'REQ_BUS_NOT_AUCTION'`, 고정 메시지.  
   → 다른 기사가 먼저 청약해 `BIDDING`으로 바뀐 **동일 슬롯**은 재청약 불가 (**슬롯 단위 선착순**에 가깝다).
5. `fetchBusRowForUser` → **`TB_BUS_RESERVATION.BUS_ID`** (`SERVICE_CLASS`).
6. **갱신**: 버스 행 `BIDDING`, 예약 금액·수수료·`BUS_ID` 갱신.
7. **신규**: 막힌 `DATA_STAT` 있으면 **409**. 버스 `BIDDING` 후 `RES_ID` 채번, **`TB_BUS_RESERVATION` INSERT** (`REQ_BUS_SEQ = 0`, `DATA_STAT = 'BIDDING'`).
8. 같은 `REQ_ID`의 **모든** `TB_AUCTION_REQ_BUS`가 `BIDDING`이면 **`TB_AUCTION_REQ.DATA_STAT = 'BIDDING'`**.
9. **`applyMomMemberAfterBid(connection, custId)`** — 실패 시 **전체 롤백**, HTTP 상태 + `{ error, errorCode, modalLines }`.
10. **커밋** → `{ success, message, resId, isNewReservation, momMember }`.

**성공 `momMember` (요지)**  
`ackMessage`, `disableFurtherBid`, `remainingCnt`, `useCnt`, `basicCnt` 등 — 월별 **`TB_MOM_MEMBER`** 처리 결과.

**스키마:** `TB_MOM_MEMBER` 미적용 시 **503** 등으로 `modalLines` 가능. 관련 SQL 레포 내 `tb_mom_member.sql`, `insert_tb_common_code_fee_policy_cnt.sql` 참고.

---

### 7.3 `PUT /api/traveler-quote-request-details/bid-cancel`

- **화면 버튼 없음** (다른 화면/API 연동용).
- Body: `{ reqId, custId }`.
- 최신 `TB_BUS_RESERVATION`: **404** 없음, 상태 **BIDDING·AUCTION만** 허용 → **`DATA_STAT = 'DRIVER_CANCEL'`** 갱신.

---

## 8. 설계상 주의 (REQ_BUS_SEQ 불일치)

- **`PUT /bid` 신규 INSERT** 는 현재 코드상 **`TB_BUS_RESERVATION.REQ_BUS_SEQ = 0`** 고정.
- 상세·목록은 사용자가 고른 **`reqBusSeq`(버스 슬롯)** 로 **`TB_AUCTION_REQ_BUS`** 를 본다.
- **GET** 은 예약 조회 시 **`reqBusSeq` 매칭 후 실패하면 `0`** 으로 한 번 더 찾도록 해 두었음.  
  DB·정책을 맞출 때는 **예약 행의 `REQ_BUS_SEQ`를 실제 슬롯과 통일**하는 편이 장기적으로 안전하다.

---

## 9. 목록과의 연동

- 여행자 견적 **목록**은 **`TB_AUCTION_REQ.DATA_STAT = 'AUCTION'`** 만 노출.
- 청약 성공 후 마스터가 **`BIDDING`** 이 되면 해당 견적은 **목록에서 빠질 수 있음** (`GET /api/list-of-traveler-quotations` 조건).

---

## 10. 개발 체크리스트

- [ ] `reqId` / `reqBusSeq` 목록·상세·`PUT` 본문 일치
- [ ] `GET` 응답 `viaPoints`, `busTypeName` / 기본 `여행 🚌`
- [ ] `custId` 없을 때 경고·`PUT` 미호출
- [ ] 409 `REQ_BUS_NOT_AUCTION`·`modalLines` 모달 (닫기만)
- [ ] `momMember`·`bidLockedAfterMom`·성공 메시지 우선순위
- [ ] 신규 예약 시 5초·`onBidInsertSuccess`·`close` 순서
- [ ] `TB_MOM_MEMBER`·`TB_COMMON_CODE(BUS_TYPE)` 스키마·시드
- [ ] `server.js` 단일 라우트와 로컬 `VITE_API_BASE_URL` 일치

---

*본 문서는 `TravelerQuoteRequestDetails.jsx`, `server.js` 해당 핸들러, `driverBidMomMember.js` 기준으로 정리하였다.*
